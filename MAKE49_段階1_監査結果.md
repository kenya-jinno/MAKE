# MAKE49 段階 1 — 先行研究・実装・ログの監査結果

対象: `MAKE49_査読対応方針.md` §10 段階 1（U3–U7, U10, U11・U13–U16 の実装監査）
実施日: 2026-09-12
完了条件: 実装上の修正点が確定し、B6a/B6b を再実装できる状態にすること

---

## 総括

段階 1 で**新たに 3 件の P0 級の問題**が確定した。いずれも方針 v3 の時点では「要コード確認」にとどまっていたものである。

| # | 事項 | 判定 | 影響範囲 |
|---|---|---|---|
| **A1** | VAE 損失の KL 項が潜在次元でも平均されており、**実効 β が β/m** になっている | **確定・P0** | VAE を使う全実験（12 スクリプト） |
| **A2** | AU の m 依存性は A1 の副作用であることを対照実験で確認 | **確定・P0** | 本論文の中心的な経験的主張 |
| **A3** | 原稿 Figure 4 右パネルは VAE の潜在 TwoNN だが、キャプション・本文は参照 AE と記述 | **確定・P0** | §4.3 の参照モデル設計の根拠 |
| A4 | 合成多様体データが未標準化かつ座標スケールが異方的 | 確定・P1 | Table 4、トーラスの AU 議論 |
| A5 | 学習環境の CUDA/cuDNN/driver 版は未記録。補足資料の TODO が未処理 | 確定・P1 | R1-8、再現性の記述 |
| A6 | MSE の reduction 規約が実験間で不統一 | 確定・P1 | 表間の数値比較 |
| A7 | Table 9・Table 11 の数値は元ログと完全一致 | 問題なし | — |
| A8 | FONDUE は査読付き採録なし（arXiv のみ） | 確定 | 引用書式 |

**結論**: A1・A2 により、**現行の全 VAE 実験は β の指定が文献と比較不能であり、かつ m 掃引に交絡がある**。
段階 3 以降の本実験は、損失を標準規約に修正したうえで再実行する必要がある。
これは方針 §4.5 が予告した「既存の主張が成立しなくなる」事態が、実験前の段階で確定したことを意味する。

---

## A1【P0】VAE 損失の KL 項が潜在次元でも平均されている（U3）

### 実装の実態

`src/models/vae.py:118-120`:

```python
recon_loss = F.mse_loss(recon, x, reduction='sum') / x.size(0)
kl_loss = -0.5 * torch.mean(1 + logvar - mu.pow(2) - logvar.exp())
return recon_loss + beta * kl_loss
```

再構成項は**サンプルあたりの入力次元にわたる和**（per-sample SSE）である。
一方 KL 項の `torch.mean` は形状 $(B, m)$ のテンソル全体、すなわち**バッチと潜在次元の両方**で平均する。
標準的な $\beta$-VAE の規約ではサンプルあたり KL は潜在次元について**和**をとる。したがって

$$\mathrm{KL}_{\text{実装}} = \frac{1}{m}\,\mathrm{KL}_{\text{標準}},\qquad
\mathcal{L} = \mathrm{SSE} + \beta\cdot\frac{\mathrm{KL}_{\text{標準}}}{m}
\;\Longrightarrow\;
\boxed{\beta_{\mathrm{eff}} = \beta/m}$$

### 数値確認

同一の $(\mu,\log\sigma^2)$ に対し 2 通りの規約で KL を計算した（$B=128$, $D=784$）:

| m | KL（実装） | KL（標準） | 比 |
|---|---|---|---|
| 4 | 0.118317 | 0.473268 | 4.00 |
| 8 | 0.125798 | 1.006386 | 8.00 |
| 16 | 0.124722 | 1.995553 | 16.00 |
| 32 | 0.128405 | 4.108945 | 32.00 |
| 64 | 0.124681 | 7.979568 | 64.00 |

比は厳密に $m$ に一致する。

### 波及範囲

同一の規約が**全 VAE 実験に一貫して存在する**。

- `mse_loss(..., reduction='sum') / x.size(0)` 形式の再構成項: **12 ファイル**
- `torch.mean(1 + logvar - ...)` 形式の KL 項: **12 ファイル**
- 潜在次元について和をとる正しい形式: **0 ファイル**

該当: `src/models/vae.py`, `EG_conv_vae.py`, `EH_conv_vae_300ep.py`, `EI_cifar10_conv_vae.py`,
`EJ_svhn_conv_vae.py`, `EK_conv_vae_annealing_vs_fixed.py`, `EL_dense_grid_mknee.py`,
`EN_conv_vae_extended_m.py`, `EN_extended_m512.py`, `Exp_ConvV_HighBeta.py`,
`Exp_ConvV_HiBeta_MS.py`, `Exp_NatImg_AU.py`。

`Exp_Fashion_Multiseed.py`・`E4_300ep_multiseed.py` 等は `src/models/vae.py` の `vae_loss` を呼ぶため同じ規約である。

### 帰結

1. **原稿記載の β は文献の β と比較できない。** 「β=4」は実際には $m$ に応じて $\beta_{\mathrm{eff}}=4/m$ である。
2. **m 掃引に交絡がある。** $m$ を増やすと KL 圧が $1/m$ で弱まる。Fashion の掃引 $m\in\{4,\dots,64\}$ では $\beta_{\mathrm{eff}}$ が 1.0 から 0.0625 まで **16 倍**変化する。
3. water-filling 条件 $\lambda_j > \beta\tau^2$ の $\beta$ も同じ読み替えが必要になる。
4. 「Conv-VAE では標準的な β で AU 遮断が働かない」という報告は、$m$ が大きい Conv 条件ほど $\beta_{\mathrm{eff}}$ が小さいことと区別できていない。
5. 「β 増強で遮断が回復する」（β=10, 20）も、$1/m$ の希釈を補償しただけの可能性がある。$m=64,\ \beta=20$ の $\beta_{\mathrm{eff}}=0.31$ は、$m=8,\ \beta=4$ の $\beta_{\mathrm{eff}}=0.5$ をなお下回る。

---

## A2【P0】AU の m 依存性は A1 の副作用である（対照実験）

### 実験

MNIST、FC-VAE（784–512–256–m）、β=4、60 epochs、seed 42、他の条件をすべて固定し、
**KL の規約だけ**を入れ替えて比較した。

| m | $\beta_{\mathrm{eff}}$（現行） | AU（現行） | MSE（現行） | AU（修正） | MSE（修正） |
|---|---|---|---|---|---|
| 8 | 0.5000 | 8 | 0.01891 | **3** | 0.03895 |
| 16 | 0.2500 | 11 | 0.01516 | **4** | 0.03810 |
| 32 | 0.1250 | 16 | 0.01188 | **3** | 0.04159 |
| 64 | 0.0625 | 22 | 0.00977 | **3** | 0.04248 |

### 読み取り

- 現行規約では AU が $m$ とともに 8 → 22 と増加する。原稿はこれを「容量に応じた活性次元の増加と飽和」として報告している。
- **標準規約に直すと AU は $m$ によらずほぼ一定（3–4）**になる。$m$ 依存性は消える。
- したがって原稿が観測している AU の $m$ 依存性は、**$\beta_{\mathrm{eff}}=\beta/m$ という規約由来の人工物と区別できていない**。

### 決定的確認: 旧結果は $\beta_{\mathrm{eff}}$ から定量的に再現できる

修正損失のもとで $\beta$ を振ると、**AU は $m$ にほとんど依存せず $\beta$ のみの関数**になる
（MNIST FC-VAE, 60 epochs, seed 42）:

| $\beta$（標準規約） | AU at $m=16,32,64$ | 平均 | SD |
|---|---|---|---|
| 0.05 | 16, 25, 24 | 21.7 | 4.0 |
| 0.10 | 16, 17, 19 | 17.3 | 1.2 |
| 0.25 | 11, 12, 12 | 11.7 | 0.5 |
| 0.50 | 9, 8, 8 | 8.3 | 0.5 |
| 1.00 | 6, 6, 6 | 6.0 | 0.0 |

$\beta \ge 0.1$ では $m$ を 4 倍にしても AU はほぼ動かない（SD $\le 1.2$）。
$\beta=0.05$ の $m=16$ は AU $=16=m$ の天井効果である。

この曲線を使い、旧実装（$\beta=4$）の AU を $\beta_{\mathrm{eff}}=4/m$ から予測すると:

| $m$ | $\beta_{\mathrm{eff}}=4/m$ | 旧実装の AU（実測） | $\beta_{\mathrm{eff}}$ からの予測 | 差 |
|---|---|---|---|---|
| 8 | 0.5000 | 8 | 8.3 | −0.3 |
| 16 | 0.2500 | 11 | 11.7 | −0.7 |
| 32 | 0.1250 | 16 | 16.0 | ±0.0 |
| 64 | 0.0625 | 22 | 20.3 | +1.7 |

**旧実装の「AU が $m$ とともに増加し飽和する」曲線は、修正損失の「AU が $\beta$ とともに減少する」曲線を
$\beta_{\mathrm{eff}}=4/m$ で読み替えたものと、$\pm 1.7$ 以内で一致する。**

すなわち、原稿が報告している AU の $m$ 依存性は、**$m$ の効果ではなく $\beta$ の効果**である。
$\beta=4$ で $m$ を 4 から 64 まで振る操作は、標準規約では **$\beta$ を 1.0 から 0.0625 まで振る操作**に等しい。
固定 $\beta$ のもとでの $m$ 掃引ではない。

### 注意（過大解釈しないこと）

- この対照は 60 epochs・単一 seed・MNIST FC のみである。300 epochs・複数 seed での確認は段階 3 で行う。
- 標準規約の β=4 は MNIST に対して強い罰則であり、AU=3–4 はその設定での事後崩壊である。「VAE が壊れている」ことを示すものではない。
- 正しい結論は「**β を標準規約で選び直したうえで、m 掃引をやり直す必要がある**」であって、「AU 診断が無効である」ではない。
- 既公表の NOLTA2025 は AU を扱っておらず、この問題の影響を受けない。

### 既存結果に現れる整合的な痕跡

`results/tables/*.json` の全 548 レコードのうち **AU = m が 242 件（44.2%）**。
特に小さい $m$ では AU = m がほぼ常に成立し、遮断が全く働いていない。
CIFAR-10 Conv-VAE（`EI_cifar10_conv_vae.json`）は **15/15 レコードすべてで AU = m** である。
これは $\beta_{\mathrm{eff}}=\beta/m$ の下で大きい $m$ ほど KL 圧が弱まることと整合する。

---

## A3【P0】Figure 4 右パネルの対象がキャプションと一致しない（U6）

- 原稿 `main_paper_MAKE47.aux:223` より `fig:ep` = Figure 4。実体は `results/figures/EP_twonn_stability.pdf`。
- 生成元 `src/experiments/EP_twonn_stability.py:134-135` は `E4_extended.json` の **`vae_results`** を読んでいる（`ae_results` ではない）。
- 数値照合: 図の注釈座標 `xy=(64, 9.16)`（`:192`）は VAE の `id_twonn`=9.155 と一致し、AE の 9.537 とは一致しない。
- しかしキャプション（`main_paper_MAKE47.tex:1830-1832`）は横軸を $m_{\mathrm{ref}}$（参照 AE のボトルネック）と記述し、本文 `:1818-1822` はこの図を**参照 AE を $m_{\mathrm{ref}}=256$ に設計することの根拠**として引用している。
- 図中タイトル（`:197-198`）自体が `reference AE, κ≈10^9; β=4, 300ep` と AE と VAE を混在させている。

**付随して確認された 2 点**:

1. キャプションの「$m_{\mathrm{ref}} \ge 48$」はグリッド上に存在しない値である（グリッドは …, 32, 64, 128, 256）。元データから直接は導けない。
2. `κ≈10^9` は計算値ではなく `EP_twonn_stability.py:150` の `'kappa_ref': 1e9` というハードコード定数である。Table 12 は MNIST の AE について $\kappa(m_{\mathrm{ref}}=10d)=4.1\times10^7$ と報告しており、桁が合わない。

**対応**: 図・キャプション・本文を、実際にプロットしている量（VAE 潜在の TwoNN と $m$）に合わせて書き換える。
参照 AE の設計根拠が必要なら、AE 系列で別途図を作る。$\kappa$ の値はログから再計算する。

---

## A4【P1】合成多様体データの標準化と出力層（U4）

**Sigmoid との不整合は存在しない。** `src/models/ae.py:50` および `src/models/vae.py:55` のデコーダ最終層は
`nn.Linear` であり、Sigmoid は付いていない。Sigmoid は画像用 Conv デコーダ 12 箇所のみで使われている。
方針 §2.10 が懸念した「合成データに Sigmoid 出力」は該当しない。

**別の問題が確認された。合成データは未標準化かつ座標スケールが著しく異方的である。**

| 多様体 | min | max | 座標別 std | 全分散 |
|---|---|---|---|---|
| Swiss Roll | −11.048 | 14.130 | [6.566, **0.289**, 6.990] | 31.442 |
| Torus | −4.001 | 4.001 | [2.193, 2.164, **0.710**] | 3.332 |

帰結:

- **MSE を多様体間で比較できない**（全分散が 9.4 倍違う）。Table 4 の並記は尺度が揃っていない。
- Swiss Roll の第 2 座標（高さ）は std 0.289 で、全分散への寄与が約 0.2% である。この座標をほぼ無視しても MSE は小さく保てるため、「$m \ge d_{\mathrm{emb}}$ が必要」という主張の MSE による裏づけは弱い。
- `noise=0.01` は第 1・第 3 座標に対しては無視できるが、第 2 座標に対しては 3.4% に相当する。ノイズ水準が座標間で不均一である。
- AU の閾値 $\delta=0.01$ は `np.var(Z)` に対する**絶対**閾値（`src/metrics/structure.py:26-27`）であり、データスケールに依存する。未標準化の合成データと $[0,1]$ の画像データで同じ $\delta$ を使うことは正当化されていない。

**対応**: 合成データは標準化してから学習・評価する。$\delta$ は標準化後の潜在分散に対して定義する。Table 4 を再計算する。

---

## A5【P1】学習環境のバージョンが未記録（U5）

- 環境情報は `supplementary_MAKE46/ENVIRONMENT.md` の 1 ファイルのみ。
- 記載は `PyTorch 2.0` / `RTX 3070` のみで、**CUDA・cuDNN・driver の版は記録なし**。
- 同ファイル 24–31 行に投稿前 TODO が**未処理のまま残っている**（`torch.version.cuda`, `nvidia-smi`, `pip freeze` の記録を指示）。
- `requirements_freeze.txt` はプロジェクト全体に存在しない。
- `results/tables/*.json` 全 47 ファイルに環境・版情報のキーは 0 件。
- 同ファイル 28–31 行の Python 3.11.8 / PyTorch 2.5.1 等は**解析機（CPU、図の再生成用）**の版であり、学習環境ではない旨が明記されている。

**原稿記述との不一致**: `main_paper_MAKE47.tex:1188-1192` は補足資料の内容物として
「environment information (random seeds, library and framework versions)」を含むと述べているが、
実態は学習環境の版を記録していない。**記述が実態を上回っている。**

**参考（本調査機の実測値）**: Python 3.12.3 / PyTorch 2.7.1+cu118 / NVIDIA GeForce RTX 3070。
ただしこれは**今回の再実行環境**であり、既存結果を生成した環境ではない。既存結果の環境は復元できない。

**対応**: 段階 3 以降の再実行時に環境を機械的に記録する仕組みを設定ファイルに入れる（→ 実験設定表 §7）。
既存結果については「記録なし」と正直に書く。

---

## A6【P1】MSE の reduction 規約が実験間で不統一

- `results/tables/E4_results_mnist.json` の MSE は 141.58, 122.32, 95.04, … （784 画素の**和**）
- `results/tables/E4_300ep_multiseed.json` の MSE は 0.0283, 0.0183, … （画素**平均**）

同じ MNIST・同じ AE/VAE でありながら 2 つの尺度が混在している。
表をまたいで MSE を比較・引用する際に誤りが生じうる。

**対応**: 全実験で**画素平均 MSE**に統一する（→ 実験設定表 §4）。既存の表は再計算する。

---

## A7 Table 9・Table 11 は元ログと一致（U7・問題なし）

- **Table 9**（`tab:fashion_s3`, Exp-Fashion-MS）: **3 seed（42, 123, 777）の平均**。
  JSON から再計算した 7 行すべてが原稿値と一致。キャプションの `mean±std, 3 seeds` は正しい。
  軽微な改善候補として、MSE 列のみ std が併記されていない（std は小さく、m=8 で 0.00026）。
- **Table 11**（`tab:downstream`, Exp-Downstream）: **左（AE）は単一 seed 42、右（VAE）は 3 seed 平均**の混在表。
  ただしキャプションと本文 `:1147-1150` が seed 数の違いを明記しており、記述は正確。
  7 行すべてで原稿値と JSON が一致。raw-pixel baseline 91.6% も一致。

**査読上の追加対応は不要。**

---

## A8 FONDUE の掲載状況（U10）

- arXiv:2209.12806、**v1 のみ**（2022-09-26 投稿）。Comments 欄・Journal reference 欄ともに**なし**。
- DOI は arXiv の DataCite DOI（10.48550/arXiv.2209.12806）のみ。
- OpenReview に投稿記録（`x9S5kdaQkkY`）が存在するが、採録を示す記載は確認できなかった。

**対応**: **preprint として引用する**。「査読付き論文」として扱わない。
ただし**査読を経ていないことを論拠に内容を軽んじる書き方はしない**（方針 §3.7 の姿勢を維持）。

参照: [arXiv:2209.12806](https://arxiv.org/abs/2209.12806) / [OpenReview x9S5kdaQkkY](https://openreview.net/forum?id=x9S5kdaQkkY)

---

## U11・U13–U16（FONDUE 再実装の監査）

方針 v3 §13 で commit `f6d6e66b536bcbe635392d3438338949d4dc8730` に基づき確定済み。
本段階での追加確認事項はなく、実験設定表 §6 で再実装の設定を固定する。

---

## 段階 1 の完了判定

| 項目 | 状態 |
|---|---|
| U3（損失の reduction） | **完了・修正必要** → A1 |
| U4（合成データの出力層・標準化） | **完了・修正必要** → A4 |
| U5（CUDA/cuDNN/driver） | **完了・記録なしと確定** → A5 |
| U6（Figure 4 右パネル） | **完了・不整合確定** → A3 |
| U7（Table 9/11 の seed） | **完了・問題なし** → A7 |
| U10（FONDUE の掲載状況） | **完了** → A8 |
| U11, U13–U16（FONDUE 実装） | v3 §13 で確定済み |
| NOLTA 公刊版の照合（§3.4） | v3 §13 U2 で確定済み |

**B6a/B6b を再実装できる状態に到達した。**
ただし A1 により、**提案法側のベースラインも損失を修正して再実行する必要がある**。
これは方針 §10 段階 3 の前提条件として実験設定表に反映した。
