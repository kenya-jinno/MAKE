# MAKE49 改訂稿の確認結果と再投稿前の修正事項

確認日：2026-09-18

対象：

- main_paper_MAKE49.pdf：英語版、15ページ、2026-09-18版。
- main_paper_NN49.pdf：日本語版、10ページ。
- 比較資料：MAKE47、添付NOLTA2025稿、添付FONDUE論文、MAKE49_査読対応方針(2).md、査読者3名のコメント。
- 原典確認：ARD-VAE、GECO＋L0、関連書誌の一次資料。

本報告のページ・行番号は、特記しない限り英語版PDFに印刷されたものを指す。実験コード、seed別ログ、事前指定記録、補足資料、査読回答書は今回の確認対象に含まれていない。「本文にない」と「実験していない」は区別して評価した。英語版の表・図・参考文献はPDFを画像としても確認した。

## 1. 総合判断

**比較研究として再構成する方向性は支持できる。ただし、現稿のまま再投稿することは勧めない。中心的な結論と数値・原典の不整合を修正し、査読者が要求した実験仕様と結果を提示する必要がある。**

ID誘導やAUの優位性が支持されなかった場合に、その結果を正直に報告すること自体は妥当である。NOLTA2025の着想を繰り返すだけでなく、既存選択法、品質条件、費用、失敗時の挙動を比較する研究には意味がある。

しかし、現稿は旧版を大幅に短縮した結果、実験を検証するための情報まで削られている。また、新たな主要貢献に据えた「感度の再現可能な順位」は、現状のTable 3からは十分に支持できない。**提案法の優位性を取り下げた後の貢献を、別の過大な主張で支えないことが重要である。**

再投稿前の優先順位は次のとおり。

| 優先度 | 修正事項 | 主な作業 |
|---|---|---|
| 最優先 | β感度と閾値感度の混同、ARD-VAEの説明 | 原典・設定ログの確認、再集計、本文修正 |
| 最優先 | 「順位がデータセット間で再現」「2桁」の主張 | Table 3に沿った修正。順位の一般性を残すなら追加解析 |
| 最優先 | AUと無作為特徴量の比較からの結論 | 対応を保った差の解析、結論の限定 |
| 最優先 | 実験仕様・疑似コード・査読要求結果の不足 | 設定表、学習曲線、選択基準別結果、最終test結果を追加 |
| 高 | 共通品質目標、終了状態、費用の解釈 | 目標値・達成率・費用内訳・出力モデルの定義を明示 |
| 高 | NOLTA・MAKE47・既存法についての不正確な記述 | 原典・旧版と照合して訂正 |
| 中 | 英日版、図表、書誌、文章の整理 | 参照番号、図の可読性、著者情報・DOI等を修正 |

大規模な全実験のやり直しを、現時点で一律に求めるものではない。まず保存済みの設定・ログと本文の対応を確認し、結論を変え得る不備が判明した条件に絞って再実行する。

## 2. 改善された点

1. **Abstractから未定義の変数を取り除いた。** Reviewer 1の最初の指摘には対応できている。
2. **FONDUE、FONDUE-VAR、GECO＋L0、ARD-VAEを比較対象に加えた。** 全探索だけだった比較を拡充した点は、Reviewer 2への重要な前進である。
3. **ID誘導と昇順走査を同じ品質条件で比べた。** Table 2のMNISTでは、同じ選択次元6・同じ表示MSEに対して715秒と182秒であり、当該設定でID誘導の費用上の利点が確認できないという記述は表に対応している。
4. **ID推定値の表現・標本数・推定器への依存を示した。** Reviewer 3のモデル依存性への指摘に直接関係する。
5. **幾何学的な背景を実用上の保証から切り離した。** 旧Proposition 2から非線形VAEへ強く一般化する問題は改善している。
6. **検証データによる選択と最終test評価を区別する方針を明記した。** ただし、その実施と結果を追える情報の追加が必要である。
7. **旧実装のKL reductionの問題を開示し、再実験したと明記した。** 開示は適切であり、今後は影響範囲と修正後の根拠を表で示すとよい。
8. **表の数値表記は旧版より整理された。** 旧Table 4の括弧崩れ・小数と指数表記の混在という問題は、新Table 2・3では同じ形では残っていない。

## 3. 中心的な結論について修正すべき点

### 3.1 βを変えた結果と、選択規則の閾値を変えた結果を区別する

該当：Abstract 9–12行、Introduction 45–53行、§6.3、Table 3、Figure 3。

本文は、各規則の自由パラメータを変えたときの移動量を「threshold sensitivity」と呼ぶ。しかしTable 3の説明とFigure 3の横軸は正則化重みβである。

少なくとも以下は別の操作である。

| 操作 | 変わるもの | 主に評価する性質 |
|---|---|---|
| 学習時のβを変える | 学習目的と得られる表現 | 正則化設定への依存 |
| 同一checkpointでAU閾値δを変える | 活性座標の判定 | 読み取り閾値への依存 |
| FONDUEのID差の許容値を変える | 探索の停止規則 | 選択規則の閾値への依存 |
| 再構成許容幅を変える | 許容する品質と選択次元 | 品質・容量・費用のトレードオフ |
| ARD-VAEの関連度累積比率を変える | 関連軸の採択基準 | 関連度読み取りへの依存 |

βを変えれば「再構成と正則化のどちらを重く見るか」が変わる。選択次元が動くことは、それだけで選択法の欠陥を意味しない。また、§3.1の参照モデルをβごとに学習するなら、品質目標自体もβとともに変わる可能性がある。現在の移動量には、学習目的・参照品質・探索規則の影響が混在する。

**修正案**

- Table 3が実際にβを振った結果なら、「指定したβ範囲における選択次元の変動幅」と記述する。
- βの具体的な5値を示す。
- 変動幅が「seed平均の最大値−最小値」なのか、「seedごとの変動幅の平均」なのかを数式で定義する。
- βごとの品質目標を表に出し、固定目標での介入とは区別する。
- δ、FONDUE閾値、品質許容幅の感度は、それぞれ何を固定して何を変えたかを示す。
- p.10の「βを固定すれば最適次元が定まるという前提が成立しない」（277–279行）は削除する。βを変えた実験から、固定βにおけるこの命題を否定することはできない。

新しい統計量として強く売り出すより、測定対象を明確にした感度分析として提示する方が、現在の証拠に忠実である。

### 3.2 ARD-VAEが平坦な理由の説明は原典と合っていない

該当：§2.3 110–112行、Table 1、§6.3 270–275行、Figure 3内の説明。

現稿はARD-VAEについて、自由な閾値がないため全設定で同一の次元を返すと説明する。しかし、以下の確認が必要である。

- 現稿のTable 1自体に、関連度スコアの99%を説明する軸を採ると書かれている。
- 原論文§3.3はβを調整すると明記している。
- 原論文§3.4は関連度スコアの累積比率99%を用いる。
- 原論文§3.3・式(17)はGaussian近似による閉形式KLを説明し、実験でその閉形式を使うと述べている。したがって、Table 1の「原典はStudent’s tを使用し、我々だけがGaussianを使う」という逸脱の説明は不正確である。[ARD-VAE原論文](https://arxiv.org/html/2501.10901v1)

**必要な確認**

1. MAKE49のARD-VAEで、βを本当に5通り変えて再学習したか。
2. 5条件に同一設定の結果を複製しただけであれば、感度0として他法と順位づけしない。比較対象外、またはβ固定の参照結果とする。
3. 実際に変えたうえで同一の次元になったなら、観測結果として報告する。原因を「閾値が存在しないから」とは説明しない。
4. 原典との差分は、事前分布の更新式、更新用データ、更新頻度、関連度スコア、β、初期容量を対応づけて確認する。Gaussian近似という一点だけでは逸脱とは言えない。

ここは単なる名称の訂正ではない。現稿が最も明確な結果として強調する比較の解釈に関わる。

### 3.3 「順位がデータセットをまたいで再現する」はTable 3に対して強すぎる

該当：Abstract、Introduction、§6.3 268–269行、§7.1 350–353行、§7.3 370–371行、Conclusions。

Table 3には明確な順位逆転がある。

| 比較 | MNIST | dSprites／CIFAR-10 | 含意 |
|---|---|---|---|
| ID-guided 対 GECO＋L0 | 6.0 < 8.8 | CIFAR-10：214.4 > 34.2 | 順位逆転 |
| Ascending scan 対 FONDUE＋Q | 6.0 > 3.6 | dSprites：15.2 < 28.0 | 順位逆転 |
| ID-guided 対 FONDUE | 6.0 > 3.8 | dSprites：0.0 < 0.8 | 順位逆転 |

一部の傾向が共通する可能性はあるが、平均値で並べた表だけでは「転移する順位」は示せない。また、ID-guidedのdSpritesでの0は、推定が信頼性検査に失敗した結果と併読する必要があり、有効に作動した選択法の頑健性を意味するとは限らない。

**修正案**

- 現状では「感度は手法とデータセットによって異なった」とする。
- 順位の一貫性を主要貢献として残す場合は、順位相関・一致度と不確実性を示す。設定を変えていない行、固定次元の行、フォールバックによる値の扱いを事前に定義する。
- 異なるグリッド幅の生の変動幅をそのまま平均した指標は、CIFAR-10に大きく影響される。データセット別値を中心に解釈し、必要なら正規化した補助値を示す。

「2桁」の表現にも訂正が必要である。平均値列の非ゼロ最小値3.5と最大値72.2の比は約20.6倍であり、100倍ではない。0との比は定義できない。全セルをまとめれば0.8と214.4など100倍を超える組合せはあるが、それは「平均で2桁」「同一データセットで同じ順位」と別の主張である。

**最も安全な記述は、比喩的な桁数を外し、観測した変動幅と条件をそのまま示すことである。**

### 3.4 AUと無作為特徴量の比較から「追加情報がない」とは結論できない

該当：§6.4 293–305行、§7.1、Conclusions 393–394行。

報告されているAUC改善量は次のとおり。

- AU追加：+0.108、bootstrap区間 [+0.059, +0.157]。
- 無作為特徴量3個追加：+0.098、区間 [−0.025, +0.227]。

これらの区間の重なりだけでは、両者の差の検定や同等性の判断はできない。改善量の点推定差は0.010だが、その区間は対応する予測・再標本化結果から求める必要がある。別々の区間からは復元できない。

また、単に差を確認できなかったことを「検出力不足」と断定するのも避けたい。検出力や精度を評価するには、標本単位、条件数、ばらつき、検出対象の効果量を定義する必要がある。

**修正案**

- 同じ未使用条件における「基底＋AU」と「基底＋無作為特徴量」のAUC差を直接評価する。
- 同一学習runの複数checkpointを独立標本と数えず、条件・run等の依存を保って再標本化する。
- 基底特徴量、予測対象のラベル、予測器、学習／評価条件の分離、条件数、無作為特徴量の生成法と反復数、区間の信頼水準を示す。
- 対照が事後追加なら、その時点と目的を明示する。事前指定した主解析の結果も残す。
- 現時点の結論は「追加価値は確立できなかった」とする。「情報を持たない」「無作為特徴量と同等」とは書かない。

英語の差し替え例：

> The AU-augmented predictor improved on the baseline in the prespecified analysis. A matched random-feature control also improved on the baseline. These results do not establish an AU-specific benefit; a paired comparison is needed to assess the difference.

これは実験でAUの効果が否定されたと装うことなく、追加価値について慎重な結論を示す表現である。

## 4. 再現性と査読要求への対応

### 4.1 「記録した」と書くだけでなく、実際の設定値を提示する

該当：§3、§5全体、とくに§5.4。

現在の本文では、FC／CNN、グリッド点数、5 seedsなどは分かる。しかし、次の主要設定が表として示されていない。

| 必要な情報 | 記載すべき内容 |
|---|---|
| データ | 学習・検証・testの件数、画像サイズ、抽出法、固定分割とseedの役割 |
| モデル | 層構成、幅・チャネル数、活性化、出力層、Gaussian尤度の分散・スケーリング |
| 学習 | optimizer、学習率、batch size、epoch数／更新数、早期停止・収束判定 |
| 探索 | グリッドの全要素、主βと感度解析の5値、予算、打ち切り規則 |
| 品質条件 | 相対許容率、絶対floor、anchor値、seed SDの求め方、条件別の最終目標値 |
| ID推定 | 参照AEの構造・容量、使用分割、標本数、TwoNN/MLE設定、0.15・0.25の計算式 |
| 各比較法 | FONDUEのデータ別epoch、VARの2設定の意味、GECOの制約・更新、ARDの更新設定 |
| 環境 | 実測のPython・PyTorch・CUDA・cuDNN・driver・GPU、CPU利用の有無 |
| 追試 | コード版、設定・分割ID、run ID、表図の生成手順、補足資料の対応先 |

Reviewer 1の「CUDAは何版か」には、「各runで版を記録する」という文章では回答できていない。再実験に使った実際のバージョンを本文または明示した補足表に載せる。

また、§5.2の「公式train/test分割」という一括表現は、dSpritesにもそのまま当てはめない。実際に使用した独自分割の比率・件数・分割seed・重複防止を記載する。公式配布のデータ形式と、自分たちの分割を区別する。[dSprites公式配布](https://github.com/google-deepmind/dsprites-dataset)

事前指定・事前登録については、日時、凍結した設定、対象実験、その後の変更を確認できる資料を付ける。実際の登録記録を示せない場合は、pre-registeredを「追加実験前に指定した」など実態に合う表現にする。MAKE47で既に結果を見たデータを、完全な未使用条件として扱わない。

### 4.2 疑似コードがなく、ID誘導の手順を復元できない

該当：Table 1、§3.3、§6.5。Reviewer 3-7への対応。

Table 1の短い選択規則は改善だが、入力から出力までのアルゴリズムではない。特に「2×推定値以下のwindow」「declared expansion rule」「信頼性検査」「fallback」の具体的手順が分からない。

少なくとも以下を1つのAlgorithmに示す。

1. 入力：候補集合、分割、anchor、品質許容幅、ID参照設定、各閾値、探索予算。
2. anchorと品質目標の確定。
3. 参照AE学習、ID推定、信頼性検査。
4. 信頼できる場合の候補順序、丸め方、範囲拡張、停止。
5. 信頼できない場合のfallback規則。
6. 品質未達・予算切れ・次元0・圧縮なしの場合の出力。
7. 最終モデル、次元、診断値、品質達成、終了状態、費用を返す。

AUを測定するだけなら、選択次元を変更しないことも明示する。旧V1/V2を手順から外したなら、査読回答書でその変更を説明する。

### 4.3 結果が「実施した」という文章にとどまっている箇所を補う

次の表図が現PDFでは確認できない。

| 内容 | 現稿の状態 | 最低限必要な提示 |
|---|---|---|
| 過学習確認 | Figure 1–4はいずれもepoch別学習曲線ではない | train/validation MSEのepoch曲線、seed間の幅、選択checkpoint |
| 最終汎化性能 | Table 2はvalidation MSE | 選択確定後のtest MSEと不確実性 |
| ELBOでの次元選択 | Table 1で変種として言及 | 選択次元、品質、費用、test性能 |
| 下流検証での次元選択 | linear probeをTable 1で言及 | 学習・選択分割、ラベル使用、選択次元、test精度 |
| AUと変数型の学習初期／後期比較 | §6.4に結論のみ | 同一checkpointにおける両指標の軌跡とseed間ばらつき |
| FONDUE-VARの2設定 | 実施と次元0の記述あり | 設定別結果、失敗数、出力の扱い |
| 合成多様体 | §7.4に0.2%未満等の結論のみ | データ生成・標準化・容量・閾値・除去前後MSEの表 |

testを何度も見て選択する設計へ戻す必要はない。Reviewer 1への回答では、train/validation曲線と一度だけ行う最終test評価で過学習・汎化を確認する設計を説明すればよい。

AUと変数型については、「終期に数が近い」ことと「同じ程度に安定している」ことは別である。事前に定めた早期・後期の時点について、読み取り値、seed間SD、同一run内の時間変動を区別して示す。

補足資料に結果があるなら、本文から対応する表図番号を明示し、主結論を支える最小限の結果は本文に置く。

### 4.4 データセットの実施範囲を統一する

Abstract、§5.2、Figure 1・3、Table 3は4データセットを扱う。一方、Table 2はMNIST・dSprites・CIFAR-10の3つで、§6.5はID誘導を実行したMNIST以外の2つで失敗したと述べる。

しかし、Figure 3とTable 3にはFashion-MNISTのID-guided結果がある。したがって、「ID誘導を実行したのは3データセット」という読み方と整合しない。

**修正案**

- データセット×手法×β×seedの実施範囲を補足表にする。
- Fashion-MNISTの主設定の結果と、適用／fallbackの件数を載せる。
- 「4データセット全体の評価」と「ID誘導の特定実験3データセット」を区別するなら、その理由を説明する。
- dSpritesは合成画像であり、CIFAR-10と一括して自然画像の根拠にしない。§6.5の見出しは内容に合わせる。
- 「開発に用いたデータセットでしか成功しなかった」は、Fashion-MNISTの扱いが確定するまで保留する。

## 5. 共通品質・費用・比較法の解釈

### 5.1 品質条件の実数値と達成率を出す

§3.1の品質目標は合理的な比較枠組みになり得るが、許容率とfloorが不明なため、Table 2からどの方法が目標を達成したか判定できない。

例えばdSpritesでは、FONDUEのMSEは0.00550、昇順走査は0.00126で、誤差に約4.4倍の差がある。FONDUEが早いことは示されるが、同じ品質を達成して早いとは、この表だけでは言えない。

Table 2に次の列または補足表を追加する。

- 条件別のanchor誤差と品質目標。
- 品質達成数／全実行数。
- MSEのseed間SDまたは適切な区間。
- 所定品質に達した場合の費用と、未達時に実際に消費した費用。

「success」はアルゴリズムが正常終了した意味か、品質条件を達成した意味かを分ける。通常のFONDUEの正常終了が、外部から与えた品質目標の達成を保証するとは限らない。

### 5.2 終了状態・予算切れ・次元0の集計方法を明示する

§3.3の終了状態は相互に重なる可能性がある。IDが信頼不能でfallbackした後、品質を満たす小さいモデルを得る場合もある。

「ID推定の判定」「選択手順の終了理由」「最終品質達成」を別々の列にすると明確になる。

また、Table 2のdSpritesのFONDUE＋Qはsuccess 4・budget 1だが、次元とMSEは全5回の値として表示されている。予算切れ時に返すモデルが定義されているなら、その規則を書く。定義されていなければ、値の分母と欠測の扱いを示す。

次元0でモデルを学習できない場合は、費用と失敗件数には含める一方、存在しないモデルのMSEを通常の数値として平均しない。失敗を隠さないことと、未定義量を無理に平均することは別である。

### 5.3 参照AEと品質anchorを分けて記述する

§3.1では品質判定に最大容量の参照モデルが必要である。他方、Table 1と§6.1は昇順走査が参照モデルを使わないと書く。

ID推定用のreference AEを使わないという意味なら、そのように限定する。品質anchorを使う以上、単に「no reference models」と書くと費用計上と矛盾して見える。

用語を次のように統一するとよい。

- ID-reference AE：推定用の表現を作るモデル。
- Quality anchor：共通品質目標を定義するモデル。
- Selected model：評価対象として最終的に返すモデル。

主張した715秒対182秒を支えるため、費用をanchor、ID参照AE、ID計算、探索、最終モデル準備、評価に分解した小表を追加する。

### 5.4 選択された「次元」と評価した「最終モデル」を定義する

Table 1は全方法に選択次元での最終学習を課す。一方、GECO＋L0とARD-VAEは、学習中の剪定や異なる事前分布を方法の一部としている。

次を区別する必要がある。

- 各法が出力したモデル自体を評価する比較。
- 各法から次元だけを受け取り、共通の通常VAEをその次元で学習する比較。

後者も次元選択規則の比較として可能だが、元の剪定法・ARD-VAEのモデル性能そのものではない。残った座標数と新規ネットワークのbottleneck幅も必ずしも同じ対象ではない。

同じ次元の最終モデルを方法間で共有しているなら、MSEが一致する理由、実測時間と各手順に帰属させた費用、キャッシュの扱いを明記する。現在の表の数値が誤りだと断定するものではなく、比較対象を読者が特定できるようにするための修正である。

### 5.5 既存法の目的と、今回の実装との差を正確に書く

GECO＋L0の原論文は、再構成制約の下でKLとゲート数の項を含む目的を最適化し、ARMによる更新を行う。MAKE49のhard-concreteへの変更はTable 1で開示されており、この点はよい。ただし、変更実装の挙動を原著法の性質として一般化しない。[GECO＋L0原論文](https://arxiv.org/pdf/2003.10901)

また、既存法に再構成制約があることと、今回の「validation anchor＋許容幅」という具体的な設定手順が完全に同一であることは区別する。非凸最適化で学習された剪定法を、常に大域的な最小容量を返す方法とも表現しない。

FONDUEについて、公刊値との不一致を開示したことは妥当である。しかし、異なる条件のdSprites結果が一致しないだけでは再実装の誤りとは限らない。原著条件の再現確認と、共通条件に移植した比較を分ける。原典値を参考欄に置いたと書く§7.3に対応する欄は現本文に見当たらないため、実際の補足表への参照を加える。

FONDUE-VARは、今回採用した記述仕様、keep-mixed設定、variance/log-varianceの処理、参照したコード版を明記する。「忠実に再現」という表現だけでは、前回方針で特定した実装経路の違いを解消できない。

## 6. 旧版・先行論文との関係

### 6.1 NOLTA2025を全結合モデルだけの研究と要約しない

該当：§2.5 120–125行、日本語版§2.5。

現稿は先行会議論文を「単一データセットの全結合モデル」での報告として要約している。しかし、添付NOLTA稿p.1の実験節は、全結合4構造と畳み込み2構造、4 random seedsを明示している。

最終会議版が添付稿と異なるなら、実際に引用する版で確認する。添付稿に基づく限り、この要約は修正が必要である。

Reviewer 2への回答では、以下の3列を持つ差分表を追加する。

| 項目 | NOLTA2025 | MAKE47 | MAKE49での追加・変更 |
|---|---|---|---|
| 問い | 入力IDと潜在表現・容量の関係 | ID誘導とAU検証 | 共通条件での選択法比較と限界の検証 |
| 比較対象 | 原稿に記載のモデル・容量 | 旧稿の比較 | FONDUE等と共通品質条件の対照 |
| 学習時間と読み取り | 原稿の実施範囲を記す | 旧稿のAU観察 | 同一checkpointでの統制比較 |
| 品質・費用 | 原稿の実施範囲を記す | 旧稿の指標 | anchorを含む費用と品質達成の分離 |
| 実装 | 元の実装 | 問題の影響範囲 | 修正内容・検証・再実験 |

CNNやmulti-seedを今回初めて導入したようには説明しない。NOLTA稿と現在の参考文献35は題名も異なるため、公刊版の正式書誌に統一する。

### 6.2 「旧版は環境を何も報告していなかった」は事実と異なる

該当：§5.4 207–211行。

MAKE47の§5.1にはPyTorch 2.0、NVIDIA GeForce RTX 3070、CUDAが使えない場合のCPU fallbackが記載されている。したがって、環境情報を一切報告していなかったという現在の文章は訂正する。

適切な説明は、「旧版の記載・保存記録ではCUDA等を含む完全な実行環境を特定できなかった。今回の再実験の環境を明示する」である。

§7.4の標準化についても、MAKE47は本文で標準化を行ったと記載している。実装監査で未実施が分かったなら、**旧本文の記述と実装が一致していなかった**ことを明確にする。本文の記載だけでは、今回述べる原因を確定できない。

### 6.3 3件すべてを同じsum／meanの実装欠陥として括らない

該当：§6.6 317–338行、Abstract、Introduction、Conclusions。

1件目はKLのreduction、2件目は再構成制約のスケールなので、記載どおりならsum／mean問題に属する。一方、3件目はFONDUE-VARのある設定で次元0が返るという現象であり、本文にはsum／meanの誤りが原因と示す根拠がない。

次のように整理する。

- 2件の実装上の正規化・スケール不整合。
- 1件の特定設定におけるアルゴリズムの退化出力。

sumとmeanのどちらかが常に正しいわけではない。目的関数の定義に対して、β、制約閾値、乗数のスケールが整合しているかを確認し、どの実装で何が不整合だったかを記述する。

旧版の結果を1.7 units以内で説明できるという主張も、比較条件と誤差指標を表で示す。β/mという計算上の関係と、異なる容量の学習結果が実際に再現されることは別の主張である。

### 6.4 理論は背景として残し、観測の保証にしない

§4で主張を限定した方向は妥当である。さらに以下を修正すると明確になる。

- Whitneyの埋め込みに関する記述には、その定理を直接確認できる出典を対応させる。現状のHatcher一冊への参照では、読者が該当命題を追いにくい。
- 線形Gaussianの活性化閾値については、実際のモデルと仮定を扱う出典を対応させる。Cover–Thomasだけを引用するなら、どの結果を使うかを具体化する。
- §7.4でAUが埋め込み次元に飽和した観測を、位相がAUの軸数を予言したかのように説明しない。AUの座標別分散は埋め込みの最小次元そのものではない。
- 最小容量と再構成の関係を調べる場合も、連続写像による厳密再構成と有限標本上の平均誤差を区別する。

## 7. 査読コメントごとの判定

「不足」は、現在のPDFで回答を確認できないという意味を含む。補足資料がある場合はその内容と参照を照合する。

| 指摘 | 現稿の評価 | 再投稿前の対応 |
|---|---|---|
| R1-1 Abstractの変数 | 対応済み | 現在の読みやすい方針を維持 |
| R1-2 32／64の使用例の引用 | 元の主張を削除 | 削除で対応したと回答。代わりの「通常grid search」も必要以上に一般化しない |
| R1-3 Introductionの前方参照 | 改善 | 通常の構成案内程度に抑える |
| R1-4 Introductionへの結論混在 | 部分対応 | 45–63行の詳細な結論・順位の反復を縮め、研究課題と貢献を中心にする |
| R1-5 未定義変数 | 読みやすさは改善 | 本文の品質条件・AU・感度は正式に定義する |
| R1-6 単純／graph系手法 | 関連研究は改善 | 指定文献への採否と理由を回答書に記す。graph法の追加実装は自動的な必須事項ではない |
| R1-7 Q.E.D.記号 | 証明削除で対象が消失 | 理論節の再構成を説明 |
| R1-8 CUDA版 | 不足 | 実際の値を示す |
| R1-9 分割・学習曲線・反復 | 5 seedsと分割方針は改善、曲線等が不足 | train/validation曲線、最終test結果、学習条件を追加 |
| R1-10 DOI統一 | 未完 | DOIと書誌を実物で確認し統一 |
| R2-1 NOLTAからの増分 | 比較研究への変更は前進 | NOLTAの記述を訂正し3段階差分と新しい実証結果を示す |
| R2-2 ad hocで貢献が弱い | 比較研究として回答可能だが主結論に問題 | β／閾値・順位・AU統計の誤解釈を修正し、測定可能な貢献を残す |
| R2-3 既存選択／剪定法との比較 | 大きく改善 | 変更実装の範囲、品質達成、失敗、費用を追える形にする |
| R2-4 粗い体裁・数値表記 | 表記は改善 | 小さすぎる図、作業用図名、文献のはみ出しを修正 |
| R3-1 2×ID・V1・V2の根拠 | 保証の撤回は適切 | 手順変更を明示し、係数・品質閾値・信頼性閾値の定義と感度を示す |
| R3-2 追加データ／自然画像 | dSprites・CIFAR-10追加は前進 | Fashionを含む実施範囲を統一し、自然画像での失敗を適切に限定 |
| R3-3 参照AEへの依存 | 直接分析した点はよい | 参照AEの仕様、使用分割、推定値・ばらつきの全表を追加 |
| R3-4 線形から非線形への一般化 | 概ね改善 | 出典対応とAU／位相の表現を整える |
| R3-5 MSE／ELBO／下流baseline | MSEは提示、他は不足 | 実際の選択結果と最終性能を示す |
| R3-6 conditionalの反復 | 改善 | 今度は「同じ着想」「同じ構成」「本研究のものではない」の反復を整理 |
| R3-7 Algorithm | 未対応 | 全分岐と必要閾値を含む疑似コードを追加 |

Reviewer 2の「新しい構造や目的関数がない」という指摘に対し、無理にそれらを追加する必要はない。ただし、比較研究として回答するなら、実装の忠実性、公平な比較、再現可能性、結論の正確さが主要な価値になるため、上記の不備を残せない。

## 8. 英日版・図表・引用の確認

### 8.1 英日版の一致

日本語版の主要な数値と結論は英語版に概ね対応する。英語版の科学的な問題もほぼそのまま日本語版に引き継がれているため、両方を直す。

確認できた具体的な相違・誤り：

| 箇所 | 現在 | 修正 |
|---|---|---|
| 日本語版§5.3 | 比較法を「英語版の表2」と参照 | 英語版Table 1 |
| 日本語版§6.1 | 主要結果を「英語版の表1」と参照 | 英語版Table 2 |
| 日本語版§5.3 | 「前者3データ」 | 先に2群を記述しているため不明瞭。MNIST・Fashion-MNIST・dSpritesを列挙 |
| 日本語版全体 | 図表を英語版に依存 | 単独で読む版なら同じ図表を収録。対訳メモなら英語版併読を明示 |
| 両版§3.3 | 失敗の扱いを「無様」等で表現 | 失敗例の除外が推定に与える影響を中立的に述べる |

### 8.2 図表

- Figure 1・3・4は横に多数のパネルを並べ、本文サイズでは軸と凡例が小さい。2×2配置や2図への分割で読める大きさにする。
- 図内にFigure H／C／G／D、B1／B6a／B8、proposed (ID+Q)など作業時の名称が残る。本文のFigure 1–4およびTable 1の名称に統一する。
- Figure 2の右パネルはPCA次元を横軸とする。本文でいう参照AEによるID推定も根拠にするなら、別の点・パネル・小表で実際に表示する。
- Table 2では選択次元の平均±SDが複数行に分割される。列幅を調整し、MSEと費用の不確実性も読めるようにする。
- 「尤度をすべての表図に明記」とする§5.1の記述に対して、現在の各captionでは明記されていない。表注の共通凡例を追加するか、本文の表現を実際の表示に合わせる。
- p.14の参考文献21（Taming VAEs）のDOIが右端にはみ出し、ページ外で切れている。URLの改行を修正する。

### 8.3 引用・書誌

参考文献27の著者列は誤っている。正式にはHao Fu、Chunyuan Li、Xiaodong Liu、Jianfeng Gao、Asli Celikyilmaz、Lawrence Carinであり、DOIは10.18653/v1/N19-1021である。現在のTan、Peng、Zhao等の列から訂正する。[ACL Anthologyの正式書誌](https://aclanthology.org/N19-1021/)

arXiv DOIを付ける方針なら、付与のある文献とない文献の扱いを統一する。NOLTAの正式題名・頁・著者、会議／arXiv／正式出版版の使い分けも確認する。

Reviewer 1が挙げたDOIへの応答は回答書に明記する。指定文献を必ず採用する必要はないが、現稿の「3つ目の推定器を加えても解決しない」という説明は未検証の断定である。今回の範囲をTwoNN/MLEに限ったという説明に改める方がよい。

FONDUE-VARとAUについては、どちらも活性に関係するが、posterior varianceに基づく型分類とposterior meanのデータ間分散は異なる統計量である。「一方が他方の精緻化」「全く同じ構成」という反復を減らし、何が共通し何が異なるかを1回明確に定義する。

## 9. 実施順序

### 第1段階：既存資料だけでできる訂正

1. ARD-VAEのβ・99%基準・Gaussian近似の説明を訂正する。
2. β感度と閾値感度を区別し、Table 3に合わない「2桁」「順位の転移」を弱める。
3. AUについて「追加情報がない」を「追加価値を確立できない」に変更する。
4. NOLTAのCNN、MAKE47の環境、3件の失敗分類を訂正する。
5. 日本語の表番号、参考文献27、図内の作業用名称を修正する。

### 第2段階：ログ・予測結果の再集計と本文への追加

1. ARD-VAEの各βの実設定とrun IDを照合する。
2. 全手法のanchor、品質目標、達成率、費用内訳、終了状態を整理する。
3. Fashion-MNISTを含む実施範囲を確定する。
4. AU追加対無作為特徴量追加の対応差と不確実性を計算する。
5. 同一checkpointのAU／変数型比較、train/validation曲線、ELBO・probe選択、最終test結果を提示する。
6. 設定表・Algorithm・補足資料一覧・事前指定と変更の記録を作る。

### 第3段階：不足が判明した条件だけを実行する

- βが実装に反映されていなかったなど、設定不整合が確認された条件。
- 必須の学習曲線や選択基準別比較が未実施だった条件。
- AU予測課題に学習・評価条件の混在が判明した場合の再評価。
- 変更実装のままでは主要結論を評価できない場合の、原典に沿った比較。

現在の結果に合わないからという理由で、自然画像の信頼性閾値を後から緩めることはしない。探索的に調べる場合は、事前指定した主解析とは分けて報告する。

## 10. 改訂稿の貢献をどう表現するか

現段階で最も支持しやすい主張は、次のような条件付きの実証結果である。

> 本研究は、潜在次元選択に用いられる複数の信号と既存手法を、品質と計算費用を明示した条件で比較した。報告したMNISTの設定では、ID誘導による選択は単純な昇順走査に対する費用上の利点を示さなかった。ID推定値とAUの判定は、表現・推定条件・判定閾値に依存した。これらの観測は、IDやAUのみから、対象とする再構成品質に必要な容量を決めることの限界を示す。

この記述も、設定・品質達成・費用の確認を終えた範囲で使う。未確認の全データセットへ広げない。

タイトルの「Revisited」「Common-Protocol Comparison」という方向は維持できる。論文の価値を明確にするには、Introductionの詳細な自己否定と過去の修正経緯を減らし、研究課題、比較設計、測定結果、適用限界を順に示すとよい。実装の修正履歴は再現性節・補足資料・査読回答書にまとめられる。

**次の原稿で必要なのは、主張を強くすることより、読者が表・設定・解析から同じ結論を確認できるようにすることである。上記の最優先事項を解消すれば、現行の比較研究という方向で再投稿原稿を整えられる。**
