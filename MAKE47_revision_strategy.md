# main_paper_MAKE47.pdf：査読対応と採録に向けた原稿修正計画

作成日：2026年9月12日  
対象：MDPI *Machine Learning and Knowledge Extraction*（MAKE）投稿原稿、全33ページ、およびReviewer 1・2・3のコメント

## 0. 本報告書の位置づけ

本報告書は、査読に回答するために「どの主張を修正し、何を追加検証し、その結果をどこに掲載するか」を決めるための計画書である。追加実験や原稿の改訂を実施済みとするものではない。

原稿のページ・行番号は、添付されたMAKE47のPDFに印刷されている番号に対応する。表・図には独立した行番号がないため、ページと表・図番号を指定した。原稿の本文・付録を確認し、Table 4とFigure 4は表示も確認した。

今回提供された資料には、NOLTA2025の会議論文本体、実験コード、学習ログ、補足資料のアーカイブ、Editorの判定通知、改訂期限は含まれていない。このため、会議論文との厳密な差分、コードの正否、実験数値の再現性、CUDAの実使用バージョンは未確認である。本文から確認できる事実と、ログ・コードによる確認が必要な事項を区別して記載する。

## 1. 総合判断：文章修正だけでは不十分

**採録を目指すには、Reviewer 2の新規性・比較不足への対応を中心に、Reviewer 3の検証設計への懸念を解消する実質的な改訂が必要である。** Reviewer 1への対応だけを丁寧に行っても、採否に直結する問題は残る。

評価欄は、Reviewer 1が主に導入・説明・実験の補強、Reviewer 3が手順の根拠・汎化・結論の妥当性、Reviewer 2が論文の貢献自体を問題にしていることを示している。多数決的に「2名はおおむね肯定的だから軽微な修正でよい」とは判断できない。ただし、Editorの正式判定は未提示であり、本報告書の「大幅な改訂が必要」は内容面の判断である。

優先順位は以下とする。

| 優先度 | 課題 | 改訂の到達点 |
|---|---|---|
| P0 | 新規性と先行研究の整理 | NOLTA2025との差分に加え、FONDUEなど直接競合する研究との差分を明示する |
| P0 | 選択手順の評価方法 | train/validation/testを分離し、選択性能を独立したtestで評価する |
| P0 | 比較実験 | 検証MSE・ELBO・下流検証、FONDUE、既存の次元削減・剪定法と比較する |
| P0 | AU判定と性能保証の混同 | AUの条件適合と、再構成品質を維持した次元選択の成功を分ける |
| P0 | 本文・数値・理論の不整合 | Table 4、探索費用、自然画像のStep 1、付録の数学的記述を修正する |
| P1 | 追加データ・参照表現依存性 | 自然画像で全手順を評価し、新しいデータ条件と独立した参照モデルで検証する |
| P1 | 閾値・再現性 | 閾値を開発条件で固定し、未使用条件で検証する。主要結果は原則5 seedsとする |
| P2 | 可読性・体裁 | Abstract、Introduction、アルゴリズム、図表、引用、実行環境を整える |

### 1.1 推奨する論文の中心課題

現状の「内在次元から適切な次元を決め、AUで確認する」という説明を、次の検証可能な問いに絞り直す。

> 内在次元推定を候補次元の探索順序に利用すると、所定の再構成品質を満たすボトルネックを、既存の選択法より少ない総計算費用で見つけられるか。また、AUの診断は、品質低下や手順の適用困難を発見する上で追加的な情報を与えるか。

この問いなら、既存のAE/VAEを使うこと自体は問題にならない。新しいアーキテクチャや損失関数がなくても、既存手法に対する有用性と再現可能な選択手順が示されれば、実証研究として主張を組み立てられる。ただし、**既存要素を組み合わせたという説明だけでは、独立した貢献の証明にならない。**

改訂後も「真の内在次元」「最小埋め込み次元」「最適な潜在次元」「AU」は区別する。圧縮、再構成、生成、分類で最適化対象が異なるため、すべてに共通する唯一の正しい次元を求める研究としては説明しない。

## 2. 最重要課題：新規性をNOLTA2025との差分だけで説明しない

### 2.1 現在の説明が弱い理由

原稿p.3、103–112行では、先行研究を主に「学習済み表現の次元を測定する研究」と位置づけ、本研究が設計と検証に利用する点を強調している。しかし、次元推定を実際の潜在次元選択に使う先行研究があるため、この対比は広すぎる。

特に重要なのは、Bonheme and Grzesの **FONDUE** である。VAEの平均表現とサンプリング表現の内在次元の差を利用して、潜在次元数を探索する。さらに付録FのAlgorithm 3では、データの推定IDの2倍から開始し、変数の活性状態に基づいて次元を調整する派生手順を扱っている。これは本原稿の「IDを基準に余裕を持たせ、活性を確認する」という発想に近い。[FONDUE原論文、§4.3・Appendix F](https://arxiv.org/pdf/2209.12806)

したがって、**「IDを選択に利用した」「2倍から開始した」「活性変数を確認した」という要素を、そのまま新規性として主張することは困難である。** FONDUEの変数分類と本原稿のAUは同一定義ではないが、その違いだけで十分な貢献になるわけでもない。

### 2.2 Related Workと比較実験に加える候補

| 先行研究・方法 | 本原稿との関係 | 推奨する対応 |
|---|---|---|
| FONDUE（Bonheme and Grzes, 2022公開） | IDを利用した潜在次元探索。付録に活性状態を使う派生手順もある | **最優先の直接比較対象**。主手法と付録の双方との差分を説明 |
| Dynamic Narrowing of VAE Bottlenecks Using GECO and L0 Regularization（De Boom et al.） | 再構成誤差の制約下で、学習中に潜在次元を削減 | **既存剪定法として優先**。同じ品質条件で比較 |
| ARD-VAE（Saha et al., 2025） | 階層事前分布で潜在軸の関連性を推定 | 統計的な次元適応法として比較候補。最低でも関連研究に追加 |
| RENs（Iyer et al., 2022） | ARD事前分布を用いるVAE系の次元適応 | 関連研究に追加し、ARD-VAEとの重複を考えて実験対象を絞る |
| Bayesian PCA / ARD-PCA | 線形モデルにおける次元決定 | 軽量な対照。これだけで非線形VAEの比較を代替しない |
| PCA、Isomap、LLE | 線形・グラフに基づく次元削減の背景 | Reviewer 1向けの背景補強。次元削減法と次元数選択法を区別 |

GECOによる手法は、再構成の上限制約を保ちながら潜在空間を縮小するため、今回推奨する「品質と圧縮を両立した選択」の比較対象として適している。[原論文](https://arxiv.org/abs/2003.10901)

ARD-VAEは階層事前分布、RENsはARDを用いた関連性推定を扱う。両者の導入により、現在のp.5、186–195行の線形・確率モデル中心の説明を、非線形VAEの次元適応まで拡張できる。[ARD-VAE](https://arxiv.org/abs/2501.10901)、[RENs](https://arxiv.org/abs/2205.13061)

実装負担を考慮した最低限の直接比較は、**FONDUE＋GECO/L0系またはARD-VAEの1手法**とする。採録に向けてはFONDUE＋GECO/L0系を優先し、ARD-VAEまで加えられると比較の説得力が増す。自作の簡易剪定だけでは、Reviewer 2の「established methods」に十分対応したとは言いにくい。

### 2.3 NOLTA2025との差分表を作る

会議論文の書誌は、原稿Reference 9と著者研究室の業績一覧で確認できる。ただし、その本文は今回未確認である。以下の「会議論文」欄を推測で埋めて返答書に使用してはいけない。[著者研究室の2025年度業績一覧](https://www.comm.tcu.ac.jp/nel/list_j_2025.html)

| 比較軸 | NOLTA2025 | MAKE47で確認できる内容 | 改訂版で追加する実質的な内容 |
|---|---|---|---|
| 基本的な着想 | 本体照合が必要。査読者は既出と指摘 | IDとAE/VAEの容量・活性の関係 | 既出の着想として明示的に継承 |
| 選択規則 | 本体照合が必要 | 3段階、V1/V2、MSEの目視確認 | 有限の候補集合、品質条件、停止・拡張・適用見送りを定義 |
| 競合手法比較 | 本体照合が必要 | 主に同じモデルの全探索 | 直接競合する選択・剪定法との品質・費用比較 |
| 汎化検証 | 本体照合が必要 | MNIST中心。自然画像は未解決 | 未使用条件で固定した手順を実行し、成功・失敗双方を評価 |
| 診断の検証 | 本体照合が必要 | 観測値を失敗モード表に対応づけ | 診断と独立した性能基準、介入前後の結果で検証 |
| 理論 | 本体照合が必要 | 位相的必要条件と線形代理モデル | 既知結果として整理し、誤った一般化を除去 |
| 再現性 | 本体照合が必要 | 3–5 seedsと補足資料の記載 | 全手順の再現性、独立分割、費用計測、実行可能なコード |

差分の価値はページ数や実験回数だけで説明せず、**会議論文では答えていなかった問いに、どの新しい証拠で答えたか**で説明する。再利用したデータ・図・文章は対応表で明示する。

### 2.4 Reviewer 2への基本姿勢

「新アーキテクチャがない研究には価値がない」という一般論には同意する必要はない。一方、現原稿で比較と独立検証が不足していることは認めるべきである。

返答は、先行研究との差分を具体化したこと、比較法を追加したこと、品質条件を持つ選択手順へ整理したこと、観測の説明と証明を分けたことの順で述べる。結果が出る前に、優越性や計算削減の達成を約束しない。

## 3. 査読対応と同時に直すべき原稿内の問題

以下は、査読コメントを原稿と照合する過程で見つかった問題である。形式修正に先行して処理する。

### 3.1 テストデータを選択過程で利用している

**該当箇所：** p.5、209–211行、p.12–13、438–448行、p.19–20、§5.4。

AU、TwoNN、MLEをtest subsetで計算すると明記されている。これらがStep 1の探索範囲やStep 3の採否・再探索を決める場合、testを手順の選択に利用することになる。下流test accuracyの最良点を見て探索窓の有効性を説明している点も、未知データでの選択性能とは区別が必要である。

**対応：** 参照AE・候補モデルの学習はtrain、IDに基づく設計・checkpoint選択・AU診断・再探索はtrain内の推定用データまたはvalidation、最終評価はtestとする。旧実験を探索的結果として残すことはできるが、主たる性能・費用比較は分離した設計で再実行する。過去に確認済みのデータを、あとから完全に未見だったと説明しない。

### 3.2 VAEの「82%削減」と必須のMSE検証が両立していない

**該当箇所：** p.8、307–316行、p.19、655–669行、p.20、Table 10。

VAEの最少費用は、参照AEの3回と、m=20のVAEを3 seedsで学習する3回の計6回としている。しかし、必須のMSE確認では「mをさらに増やしてもMSEが改善しないか」を見るため、少なくとも別のmのVAEが必要になる。参照AEのMSEは別モデルなので、VAEのMSE曲線を代用できない。全探索の結果を診断に流用すれば、その費用を無視した実運用上の削減率にはできない。

さらに、比較分母は11点×3 seedsの33回だが、Table 11のVAEは7点である。11点でVAEを実際に測った比較か、仮想的な会計かが不明瞭である。

**対応：** 全探索の候補集合を統一し、ID推定、参照学習、追加検証点、β調整、失敗からの再探索、最終学習を計上する。モデル学習回数と総実時間を併記する。現在の64%、73%、82%は同じ条件で再計算されるまでAbstract・Conclusionsの主要成果から外す。

### 3.3 「MSEも探索窓を支持する」は現数値では強すぎる

**該当箇所：** p.16–17、590–599行、p.20、Table 11、p.24、793–798行。

Table 11では、MNIST VAEのMSEはm=20で0.0126、m=64で0.0089である。m=20からm=64へ約29%改善しており、m=20の誤差はm=64より約42%大きい。AEでもm=20の0.0114からm=64の0.0062へ約46%改善する。

分類精度が比較的平坦でも、再構成が同程度とはいえない。また、VAEで最良の平均分類精度が得られたm=12は、推奨されるVAEの候補m≥20とは異なる。AEの探索窓に最良点があることと、VAEの選択規則が最良点を選ぶことを混同しない。

**対応：** 「最良分類精度の点を含んだ」と「品質を維持した最小次元を選べた」を分ける。MSEの許容劣化量を先に定め、基準に合わなければ範囲を拡張する。誤差尺度によって結論が変わることを示す。

### 3.4 Table 4は表記だけでなく説明も要修正

**該当箇所：** p.13、473–477行、Table 4。

Swiss Rollはm=2でMSE=0.029、m=3で0.0006であり、さらに約48倍、約98%の改善がある。「m=2で急速なMSE改善が止まる」「elbowが正確に最小埋め込み次元に一致する」と断言する根拠にはならない。絶対改善量の減少と、相対的に十分な再構成の達成は別の基準である。

**対応：** elbowの矢印と“exactly”を外し、全数値を同一書式に統一する。例えば0.0006 ± 0.0002のように桁を合わせる。対数MSE曲線と再構成図を示し、「m=2で表現可能である」という位相的議論と、「この学習条件で低いMSEが得られた」という実験結果を分離する。

なお、添付PDFの表示ではReviewer 2が述べる明瞭な括弧崩れは特定できなかった。数値形式の混在は確認できる。括弧の問題は否定的に応酬せず、LaTeXソースと再生成PDFの両方で再点検し、表全体の整形として対応する。

### 3.5 自然画像のStep 1が、独立した参照AEという定義と不整合

**該当箇所：** p.7、238–240行、p.22–23、§5.6.2、Table 13。

手順では候補VAEと独立の参照AEを使うと定義している。一方、自然画像のStep 1の根拠として説明される25.21±0.41は、Table 13の候補Conv-VAE、m=64の潜在表現の値である。本文上は、独立した参照AEでこの値を得たことが示されていない。

**対応：** 自然画像にも独立した参照モデルを用意する。旧Table 13は候補VAEの診断結果として扱い、Step 1の根拠表と分ける。独立参照を用いないなら、自然画像で3段階を適用済みとは記述しない。

### 3.6 V2の上限が判定に効かない場合がある

**該当箇所：** p.7–8、271–279行。

AU≤mは定義から成立する。検証点がm=2d̂の場合、AU≤2d̂≤3d̂なので、V2の上限AU≤3d̂は必ず満たされる。より一般に、検証点がm≤3d̂なら上側条件には識別力がない。

MNISTのm=20、d̂=10では、V1とV2は実質的に10≤平均AU≤18を確認する規則になる。したがって、上限係数3への感度が小さいという観測だけで、係数3の妥当性を主張できない。

**対応：** V1/V2を普遍的な合格条件から補助診断へ変更することを推奨する。残す場合も、各検証点でどの条件が実際に制約として働くかを明示する。平均値だけでなく各seedの判定を掲載する。

### 3.7 「失敗を検出した」と「原因を特定した」を混同しない

**該当箇所：** p.8–9、Table 2、p.18–19、§5.3.6、p.21、§5.6.1。

Fashion-MNISTでAU<d̂となったことは、定めた範囲への不適合を示す。しかし、参照IDの過大推定、許容される情報圧縮、AU閾値の作用、学習条件などを除外していないため、過剰剪定を実証したとはいえない。AU=mも、過大な潜在次元が存在しない可能性を含むため、直ちに失敗とはいえない。

**対応：** 表の“diagnosis”を「可能な説明と追加確認」に変更する。β低減や候補次元増大によって独立した品質指標が回復するかを示す。AUが規則に入ること自体を、診断が正しかった証拠にしない。

### 3.8 定義した検証点と、実際に判定するmが一致していない

**該当箇所：** p.7、271–275行、p.21、692行、p.22、722–727行、p.23、779–783行。

規則は「2d̂以上で最小のグリッド点」を検証点とする。しかしConv-VAEでは、グリッドにm=32があるにもかかわらず、主な判定がm=256で説明される。自然画像でも大きなmでの状態を手順全体の判定としている。

**対応：** 事前指定した検証点の結果を最初に示し、その後の点は拡張探索として順序と費用を記録する。元の点での不適合と、追加探索後の結果を混ぜない。

### 3.9 損失関数の実装定義が不足している

**該当箇所：** §3.1、§5.1、§5.6、Appendix A。

理論上のGaussian likelihoodは説明されるが、実際の学習における再構成項、KL項の総和・平均の取り方、観測分散の扱いが十分に記されていない。画素平均MSEと潜在次元総和KLを組み合わせる場合と、両者を総和にする場合では、同じβでも圧縮圧力が異なる。FCとConvの比較でこの差があると、アーキテクチャの効果と混同する。

**対応：** 実装と一致する損失式を追加し、batch・pixel・latentの各軸でのreduction、KL annealing、likelihood、τ²を明記する。βを比較する前にこの整合性を確認する。これはコード確認が必要な事項であり、現時点で実装ミスと断定するものではない。

### 3.10 人工データの標準化とSigmoid出力の整合性を確認する

**該当箇所：** p.12、439–442行、p.13、Table 4。

共通設定にSigmoid出力とあり、人工データは平均0・分散1に標準化するとされている。負の値を含む標準化データをSigmoid出力で直接復元する設定では、負値を再構成できない。極めて小さいTable 4のMSEとの整合性を確認する必要がある。

**対応：** 人工データだけ線形出力を使ったのか、別の正規化・逆変換があるのかをコードで確かめ、設定表を分ける。本文の省略なら説明修正、実装の不整合なら該当実験を再実行する。

### 3.11 理論を限定する注意書きだけでは、誤った式は修正されない

| 箇所 | 問題 | 修正方針 |
|---|---|---|
| p.11、393–395行、Remark 2 | Var[μj]>0と、δ=0.01を超えるAU判定を同一視している | 正のrateと正の分散の対応は、基底等の仮定を明示した理想設定に限定。有限δのAUとの完全一致を削除 |
| p.11、387–389行 | β=0で全次元が使用されると一般化 | AEの有効次元はデータ・冗長性・最適化にも依存する。「KLによる剪定は課さない」に修正 |
| p.12、402–408行、p.28、968–981行 | 集約事後分布のKLが非負であることから、活性次元数の変化方向を推論 | 目的関数の追加項の非負性だけでは最適解の活性数の順序は決まらない。方向の断言を削除 |
| p.21、708–714行 | 観測AUに合わせて逆算したτ_eff²で、同じ観測を説明 | 独立な検証にはならない。原則削除するか、図解用の事後的解釈に限定 |
| p.12、414–421行、p.28、986–994行 | 高いTrustworthinessから最近傍2点の順位保持やTwoNN誤差の上界を導く | 平均的な順位指標だけではその保証は得られない。未証明のC(ε,δT)による上界を削除し、実測の近傍保存率に置換 |
| p.28、1001–1003行 | decoder Jacobianの有意特異値数=min(m,d_emb)という主張 | **削除を推奨**。大域的な最小埋め込み次元から局所Jacobianのrankは決まらない |

最後の点は、滑らかで完全再構成なら接空間上でDg・Dfが恒等写像になる、という関係までなら説明できる。しかし、データ多様体から外れる潜在方向へのdecoderの振る舞いは自由であり、全Jacobianのrankがd_embになるとは限らない。「significant」の閾値も未定義である。

Appendix B.2の検出誤差上界も、採用した有限差分の検出規則に対して追加仮定と証明が必要である。主結果でないため、改訂の負担を抑えるなら未証明の上界を削除し、観測されたグリッド依存性だけを残す。

### 3.12 その他の整合性確認

- p.7、234行のTwoNNとMLEは、ともに近傍距離に基づくため、「異なる原理の独立な確認」と強調しすぎない。異なる統計を使うDANCo/ESSとの役割を分ける。
- p.16、Table 7のsubsampling区間は、真のIDの95%信頼区間と受け取られないよう「固定表現に条件づけたsubsampling 95% interval」と統一する。
- p.7、236行などの“pre-registered”は、実際の事前登録記録がある場合だけ使用する。なければ、いつ規則を定めたかを明記する。今回からの固定手順を、過去実験にも事前適用したように書かない。
- p.21、Figure 4右の表示にreference AEとβ=4が併記されている。対象がAEかVAEか、元ログと照合する。
- p.19、Table 9、p.20、Table 11のMSEは、multi-seedという表題に対し標準偏差がない。平均・単一seed・別実験のいずれかを特定する。
- p.31、Appendix Dの「β=8で過剰正則化の兆候」と「β∈[2,8]でstable」は、何が安定かを指標別に分ける。50 epochsの予備実験だけで300 epochsの主条件を正当化しない。

## 4. 追加実験の設計

### 4.1 最初に評価目的を固定する

主目的は「所定の再構成品質を満たす小さいボトルネックを、どれだけの費用で選べるか」とする。分類・生成品質は別の評価軸として扱う。

| 評価軸 | 指標 | 解釈上の注意 |
|---|---|---|
| 再構成 | 画素平均MSE、必要ならSSIM | 同じ前処理・出力範囲・評価方式で比較 |
| VAEの確率モデル | 通常のELBO、再構成項、KL項 | βで重みづけた目的関数と通常のELBOを区別 |
| 下流表現 | 固定したlinear probeのtest accuracy | probeの正則化等はvalidationで選択 |
| 圧縮 | 設定次元m、方法固有の有効次元、AU | 有効次元と実際に削除された次元を混同しない |
| 選択の安定性 | 選択mの分布、品質条件達成率 | mean±SDだけでなくseedごとの結果を提示 |
| 計算効率 | 総実時間、GPU時間、学習回数、総epochs | 参照AE・ID推定・再探索を含む |
| 診断の有用性 | 誤受容、誤警報、介入後の品質変化 | V1/V2自体を正解ラベルにしない |

AccuracyはAE/VAE単体の再構成性能の指標ではない。Reviewer 1の提案は、学習曲線ではMSEを採用し、分類Accuracyは下流評価に使用する形で対応する。

### 4.2 共通のデータ分割と再現性

1. 公式trainから学習用とvalidationを作り、公式testを最終評価用として確保する。計算費用を抑えるため部分集合を使う場合、先頭順ではなく、固定した乱数による抽出とインデックス保存を行う。クラスを持つデータでは分割の偏りを点検する。
2. 参照AE、ID推定、候補モデル選択、probeのハイパーパラメータ選択に使用したデータを図表で明記する。標準化・PCAなどの変換もtrainでfitする。
3. 主たる比較では**5つの独立した学習seed**を基本とし、参照モデルから選択まで全手順を反復する。既存checkpointへの5回の再評価を5 seedsと数えない。
4. データ分割への頑健性は、代表条件で**3分割×3学習seeds**を推奨する。すべての条件に広げる必要はなく、主要結論が分割に依存しないかを確認する。
5. 統計単位を区別する。学習seed、データ分割、IDのsubsampling反復を混ぜて標本数を増やさない。同一seed・同一分割で方法間の差を比較する。
6. 既に確認済みのMNIST等を開発条件とし、追加データのtestや未使用条件に閾値を合わせ直さない。条件変更の履歴を残す。

5 seedsは万能な精度保証ではない。差が小さい、あるいは判定がseedで割れる場合に限って反復を追加する。非有意差を性能同等の証明として扱わない。

### 4.3 比較法：最低限必要な構成

| ID | 比較法 | 選択の実装 | 費用の扱い |
|---|---|---|---|
| B1 | validation MSEによる全探索 | 全候補からMSE最良点と、許容劣化内で最小のmを選ぶ | 全候補の学習を計上 |
| B2 | validation ELBOによる全探索 | 同じlikelihoodのVAEで最大ELBOのmを選ぶ | B1と同じ学習結果の再評価が可能 |
| B3 | 下流validationによる全探索 | validation accuracy等でmを選びtestで評価 | ラベル使用法として別枠にし、probe費用も計上 |
| B4 | IDを使わない粗い探索 | 同じ有限グリッドから対数間隔の候補、または固定予算のランダム探索 | 提案法と同程度の総時間で比較 |
| B5 | 固定次元 | 32、64などを結果を見る前に固定 | 安価な実務的対照として使用 |
| B6 | FONDUE | 原著のアルゴリズムを再現し、選択後にモデルを十分学習 | 少数epochsでの探索を尊重し、最終学習を含める |
| B7 | GECO/L0系の次元削減 | 原著に従って再構成制約下で剪定 | 初期モデル・チューニング・追加学習を含める |
| B8 | ARD-VAE | 原著の関連性判定に従って有効次元を得る | 実装可能なら追加。目的関数の違いを明示 |
| B9 | PCA・Bayesian PCA | PCAは分散比の基準と再構成・probe、Bayesian PCAはARD次元も提示 | 補助的対照。PCA次元を真のIDとはしない |

FONDUEには短時間の探索が目的に含まれるため、すべての候補を300 epochsまで学習させる比較は不公平になる。一方、探索段階だけの時間と本手法の最終モデル完成までの時間も比較しない。**どの方法も「最終評価できる選択済みモデルが得られるまで」の費用に揃える。**

目的関数や事前分布が異なる方法間では、各方法の学習損失の値を直接順位づけしない。共通の再構成品質・下流性能・費用で比較し、有効次元は各手法の定義を併記する。AUが少なくなっただけではモデルサイズや推論費用が減ったことにはならず、実際の剪定・再学習を行う場合はその費用も含める。

### 4.4 データセットの優先順位

| データ | 役割 | 今回の推奨対応 |
|---|---|---|
| Swiss Roll / Torus | 既存理論説明・偽のAU合格の検証 | 分割、損失、出力層を修正した条件で再確認 |
| MNIST | 既存主結果の再検証 | 比較法、費用、品質条件、全手順5 seeds |
| Fashion-MNIST | AU下限違反の検証 | β低減・m拡張による品質の変化を調べる |
| CIFAR-10 | 自然画像での実用性 | 独立参照を含む全手順、MSE・KL・probe・総費用を評価 |
| SVHN | CIFAR-10と異なる自然画像条件 | 独立参照と推定器不一致を確認。主要法の再現を優先 |
| dSprites等の制御された画像データ | MNIST/Fashion-MNIST以外の追加データ | FONDUE等との比較がしやすい追加条件として採用候補 |

CIFAR-10とSVHNは既に原稿に登場するため、「新しいデータセットを2つ追加した」とは書かない。追加価値は、従来未完了だった全手順の検証と性能比較である。Reviewer 3の要望により明確に応えるため、新しいデータとしてdSprites等を加えることを推奨する。ただし、離散的な生成因子数をそのまま厳密な多様体次元の真値とみなさない。

自然画像で必ずV1/V2を通過させることを目標にしてはいけない。βを上げてAUを減らしても、画像がぼやけたり品質が悪化したりすれば選択法の改善にはならない。品質条件を満たせない場合は、適用を見送る結果を正しく報告する。

### 4.5 実験パッケージ

#### E1：品質・探索費用・選択次元の直接比較【P0】

**答える問い：** IDによる候補の優先づけに、全探索・粗い探索・既存の次元選択法に対する利点があるか。

- 同一データ・同一前処理・同一基本アーキテクチャで、提案法とB1–B7を比較する。B1–B3は共通の学習グリッドを使える。
- まずβを固定し、m選択の効果を調べる。次に必要な範囲でβもvalidationで選ぶ実験を追加し、その費用を含める。
- MSEの許容劣化と選択次元・費用の関係を示す。例えば相対劣化1%、5%、10%は感度評価の候補であり、推奨値として既に正当化されたものではない。
- 全探索で得たtest最良点は事後的な参考値にとどめ、比較の正式な基準はvalidationで選択したモデルとする。

**掲載物：** 方法別の結果表、品質と総時間の図、品質とmの図、seed別選択mの分布。

**結果に応じた判断：** 品質が保てても総費用が減らなければ「高速な選択法」の主張を縮小する。FONDUEより遅い場合は、その事実を示し、診断が提供する追加情報を別途評価する。

#### E2：学習曲線と過学習の確認【P0/P1】

**答える問い：** mの影響と過学習・未収束の影響を区別できるか。

- 小さいm、初期候補、より大きいmを各データから事前に選ぶ。
- train/validationのMSEをepochに対して示し、VAEではKLと通常のELBO、必要に応じてAUも別パネルに示す。
- 比較するtrain MSEも評価モードで計算し、validationと同じ再構成方式を使う。VAEはg(μ(x))による決定論的再構成か、事後サンプルの平均かを明記する。
- checkpointはvalidationのみで選択する。testは選択済みcheckpointで評価する。

Reviewer 1が要望する3分割の学習曲線への対応としては、train/validation曲線に最終test値を加える方法を第一候補とする。全epochのtest曲線を掲載する場合は、手順とcheckpointを固定後に保存済みモデルを一括評価した事後的図とし、その曲線を見て再調整しない。返答ではtestを選択から隔離した理由を説明する。

#### E3：参照表現への依存性【P1、R3-3に直結】

**答える問い：** 参照AEが変わっても、IDだけでなく最終選択と品質が安定するか。

- 入力空間、PCAで前処理した空間、FC-AE、Conv-AEを主要比較とする。既存のAE/IsometricAE/CAE/DAE比較は補助的に利用する。
- 複数m_ref、学習seed、代表的な学習時点、推定用サンプル数を変える。全組合せを総当たりするより、容量・アーキテクチャ・seedを先に調べる。
- TwoNN/MLEの値に加え、近傍重なり、再構成MSE、選択m、最終品質、費用を示す。
- m_refを増やしても安定しない場合や推定器が大きく食い違う場合に、どの段階で手順を見送るかを固定する。

既存Table 12は有用だが、MNIST/Fashion-MNISTの近いモデル群での結果である。「モデル依存性を検討済み」として済ませず、自然画像とFC/Convの差を確認する。独立したVAE seedで推定値が近いことは、参照AE自体のseed頑健性の代わりにならない。

#### E4：閾値・構成要素の検証【P0/P1】

**答える問い：** 係数2やAU条件に依存せず、品質・費用に基づく結論が維持されるか。

| 要素 | 検証する候補 | 主に見るもの |
|---|---|---|
| 初期範囲の係数c | 1、1.5、2、3、4 | 品質条件達成率、追加探索費用 |
| V1の活性割合閾値 | 0.7、0.8、0.9、0.95 | 品質不良の検出・誤警報 |
| V2の上下係数 | 下側0.5、0.75、1、上側2、3、4 | 条件の冗長性と診断の増分価値 |
| AUのδ | 0.001、0.01、0.1 | seed・モデル間での診断変化 |
| 品質許容誤差 | タスクに応じた複数水準 | 圧縮・品質・費用のトレードオフ |

この表は実験候補であり、全直積で学習をやり直す指示ではない。AU閾値やV1/V2は保存した潜在統計から再評価できる。cの比較は、固定した完全グリッドのログを使って探索を再現してもよいが、その場合は「事後的な探索シミュレーション」と明記し、実時間計測の実行例を別に設ける。

必要なアブレーションは、IDなし、IDのみ、ID＋品質確認、ID＋品質確認＋AU診断である。**AUを追加しても品質不良の検出や判断に役立たないなら、AUを独立した主要貢献から外す。**

現原稿の「観測された判定が変わらない閾値範囲」は、同じデータ内での安定性の説明にとどまる。開発条件で既定値を決め、未使用条件で診断が役立つかを検証する必要がある。

#### E5：自然画像での全手順と失敗の評価【P1】

**答える問い：** 現状未完了のStep 3に対し、品質を伴う選択または妥当な適用見送りができるか。

- CIFAR-10を優先し、独立した参照AEから手順を開始する。
- 損失のスケールを統一した上で、固定βとannealingを分ける。
- 基本decoderを保った比較を先に行い、必要なら薄いdecoderを追加する。mと同時にdecoderまで変更した効果を分離する。
- AUだけでなく、MSE、KL、ELBO、probe、再構成例を示す。
- 最初の候補、拡張した候補、使用時間、適用見送りの理由を記録する。

全条件で成功する必要はない。しかし、自然画像ではいつも見送るだけで、成功はMNISTだけという結果なら、汎用的な選択法としての主張は弱いままである。その場合は適用範囲を狭めるか、診断の体系的比較を主目的にする必要がある。

#### E6：理論の限定と人工条件での切り分け【P1】

**答える問い：** 位相・観測ノイズ・β・学習不足が、それぞれ何に影響するか。

- 既知共分散のGaussianデータと線形Gaussianモデルで、仮定を満たす代理問題の挙動を確認する。これは理論の新規性ではなく、実装・解釈の確認として扱う。
- 同じIDで埋め込み構造の異なる人工データ、ノイズ強度の異なる条件を比較する。
- Torusでは低AUでも再構成が不十分な例を示し、AU合格と品質達成の違いを明確にする。
- AU未満の座標を実際に無効化して再構成が変わるかを補助的に調べる。小さいposterior-mean分散だけでは、座標が完全に無情報とはいえない。

大規模な新しい理論研究や新アーキテクチャの開発は、今回の改訂の必須課題にしない。原稿の不正確な理論的主張を除くことを優先する。

## 5. 手順そのものの修正案

### 5.1 3種類の判定を分離する

推奨する出力は、次の3点である。

1. **選択結果：** 検証した候補のうち、所定の品質条件を満たす選択次元。
2. **ID推定の状態：** 安定、推定器不一致、参照モデル依存、未確定。
3. **AU診断：** 活性割合、IDとの比、seed依存性。これを品質の合否とは別に記録する。

V1に不適合でも再構成品質と費用が良好なら、それだけで「選択失敗」としない。逆にV1/V2に適合しても品質が悪ければ、その候補は採用しない。推定が不安定なときは、通常のvalidation探索への切り替え、または適用見送りを明示する。

### 5.2 再構成の品質条件を定義する

例えば、事前に指定した大きい参照候補のvalidation MSEをD_anchorとし、候補mについて次を用いる。

\[
Q(m):\quad D_{\mathrm{val}}(m)\leq D_{\mathrm{anchor}}+\epsilon_D.
\]

ε_Dはタスクで許容できる誤差、または開発用条件で選んで固定した許容値とする。相対誤差を用いるなら、D_anchorがほぼ0になる場合の扱いも決める。参照候補は同じAE/VAE種別・likelihood・βで学習し、その費用を含める。参照AEをVAEの品質基準に流用しない。

これは「全探索での真の最適値」への保証ではない。参照モデル自体の品質が低ければ弱い基準になるため、実験では全探索のvalidation結果に対する差も報告する。既に用途上の絶対MSE上限があるなら、それを用いて参照候補の学習を省く別条件を設定できる。

品質を満たす候補が複数ある場合は、検証済み候補内で最小のmを選ぶ。探索していない次元も含めて最小であるとは主張しない。推定ID未満の候補を試さないなら、その範囲により小さい有用な候補が存在する可能性も残る。

### 5.3 実装前に固定する設定

| 設定 | 定義する内容 |
|---|---|
| 候補集合M | 正整数の有限集合。最大次元、境界近傍の点、探索順序を含む |
| 参照モデル集合R | m_ref、アーキテクチャ、学習方法、seed数 |
| IDの安定性 | どのm_ref間で、相対変化何%以内なら安定とするか |
| 推定器の一致 | TwoNN/MLEの差・比の許容範囲、不一致時の処理 |
| 初期係数c | 既定の2と、感度評価用の候補。整数への丸め方も指定 |
| 初期範囲W | AE/VAEで別か共通か。推定区間を使うなら構築式を固定 |
| 品質条件Q | MSE尺度、参照候補、ε_D、seed集計規則 |
| 境界確認 | 初期範囲外のどの点を何点検証するか |
| 最大予算B | 学習回数または実時間。超過時の終了状態 |
| AUの設定 | δ、V1/V2係数、評価データ、集計方法 |
| 学習停止 | validation指標、最大epochs、early stopping条件 |

例えばID安定性10%、推定器差20%という数値を採用することもできるが、これらにも普遍的根拠はない。新しい閾値を増やすことで恣意性を解消したつもりにならず、少数の設定を開発条件で固定し、その設定のまま評価する。

### 5.4 掲載する擬似コードの骨子

以下は**改訂提案**であり、MAKE47に既に実装されている手順ではない。VAEの「m≥2d̂」を絶対条件として維持するより、2d̂を初期検証候補とし、その周辺を品質条件に沿って調べる形を推奨する。この変更は返答書でも明示する。

```text
Algorithm: ID-guided candidate search with quality verification

Input:
  D_train, D_val
  model family, fixed training settings including beta
  finite candidate grid M and reference-model settings R
  fixed ID-stability and estimator-agreement criteria
  initial-window factor c, rounding and expansion rules
  quality criterion Q, anchor dimension if Q requires an anchor
  seed set S, maximum budget B
  AU threshold delta and diagnostic coefficients

1. Train independent reference models on D_train according to R.
   Estimate ID on the designated estimation subset.
   Record reference-training and estimation costs.

2. Check ID stability and estimator agreement.
   If the checks fail:
      return ID_UNRELIABLE, or invoke the predeclared conventional search;
      record all fallback costs and identify the result as a fallback.

3. Form the initial candidate window W from the ID estimate(s).
   For VAE, mark the first grid point at or above c * ID as the
   initial diagnostic point, without treating it as an optimum.

4. If needed by Q, train the predeclared anchor of the same model family.
   Train the candidates in W and the predeclared point(s) outside W.
   Select checkpoints using D_val only.
   Cache each trained configuration so it is not charged or trained twice.

5. For each evaluated candidate, compute validation quality and AU.
   Record AU diagnostics separately from the quality decision.

6. If no compact candidate satisfies Q:
      evaluate the next candidate(s) using the fixed expansion rule;
      repeat while budget remains and unevaluated candidates exist.

7. Return the smallest evaluated m satisfying Q, together with
   the search history, ID status, AU diagnostics, and total cost.
   If only the anchor qualifies, report NO_COMPACT_ALTERNATIVE_FOUND.
   If evaluation cannot be completed within B, report BUDGET_EXHAUSTED.

Final evaluation, outside the selection algorithm:
  Freeze the selected configuration and report its performance on D_test.
  Do not revise the selection based on the test results.
```

選択のためのseed平均を使う場合、反復される評価単位は「そのseed集合を用いた選択手順」になる。一方、各seedで独立に選択して分布を比較する場合は、その方式で全比較法を揃える。両者を同じ実験表で混同しない。

### 5.5 この変更によって失われる主張も明示する

品質確認点と独立参照を増やすため、現在の「VAEは1候補の学習だけで十分」「最大82%削減」は成立しなくなる可能性がある。また、MSE確認だけで選択が十分なら、AUは説明用の補助指標となる。

これは改訂の失敗ではなく、検証可能な主張に整理した結果である。ただし、その後も既存法に対する付加価値が示せない場合、現行の方法提案として採録を目指す根拠は弱い。結果に合わせて中心課題を縮小する判断が必要になる。

## 6. Reviewer別の具体的な対応

### 6.1 Reviewer 1：10項目

| 番号 | 指摘と評価 | 実施する修正 | 回答時に示す証拠 |
|---|---|---|---|
| R1-1 | Abstractの記号が多すぎる。妥当 | m、d̂、β、λ、τと不等式を除き、目的・手順・主要結果・限界を平易な文章で説明 | 改訂Abstract。数値成果は再評価後に確定 |
| R1-2 | 32や64を慣習的に選ぶとの説明に引用がない。妥当 | 具体例を削って「latent dimensionality is commonly treated as a hyperparameter」と限定し、次元選択の先行研究を引用。具体数を残すなら実際に用いた論文を挙げる | Introductionの修正文と新しい引用 |
| R1-3 | Introductionの節参照が多すぎる。妥当 | 節参照は最後の論文構成紹介にほぼ集約。導入から表・式への頻繁な参照を削除 | p.1–4の再構成 |
| R1-4 | 導入に結論が混在。おおむね妥当 | データ別結果、閾値範囲、自然画像の詳細をResults/Discussionへ移す。目的と簡潔な貢献は残す | 導入の短縮と対応する移動先 |
| R1-5 | §1.2の記号説明が不足。妥当 | 貢献を文章で記述し、正式定義はMethods冒頭の記号表へ集約 | 新Methodsと整理した記号表 |
| R1-6 | 古典的・グラフ系次元削減の関連研究が不足。一般的趣旨は妥当 | PCA、Isomap、LLEを背景に追加。ただし指定DOIの論文の研究目的を確認し、次元削減法として誤って紹介しない | Related Workの追加段落と文献の位置づけ説明 |
| R1-7 | 証明末尾の□の意味が不明。容易に対応可能 | 最初の証明に「The symbol □ marks the end of a proof.」と一度説明するか、“End of proof.”に変更 | 修正箇所 |
| R1-8 | CUDAのバージョン不明。妥当 | 当時の環境記録からPyTorch、CUDA、cuDNN、driver、GPU、CPU実行の有無を確認して記載 | 環境表とrequirements/環境ファイル |
| R1-9 | 分割、学習曲線、反復数の補強。中心的な指摘 | E1/E2の分割・曲線・主要5 seedsを実施。MSEを主指標、Accuracyは下流評価にする | 分割表、学習曲線、seed別結果、最終test性能 |
| R1-10 | DOI表記が不統一。妥当 | DOIがある文献には付与し、ない文献には安定URL等を記載。著者・年・会議・巻頁も確認 | 統一した参考文献リスト |

**R1-2の細部：** 査読文中の“set to 30”の30は、原稿では行番号に相当する。潜在次元30を使っているという指摘として解釈する必要はない。数値慣習の無出典な一般化に対応する。

**R1-6の注意：** DOI 10.1109/ACCESS.2022.3190505は、Gałka et al.の *Isolation Forest Based on Minimal Spanning Tree* に対応し、確認できた書誌・抄録では異常検知が主題である。これをAE/VAEの潜在次元選択法として引用するのは不適切である。[指定論文](https://doi.org/10.1109/ACCESS.2022.3190505)

返答では、古典的・グラフ系手法の説明を補ったことを先に述べ、指定論文は主題が異なるため背景上の扱いを限定したと丁寧に説明する。関連が薄い引用を機械的に追加したり、査読者の意図を批判したりしない。

**R1-8の注意：** PyTorch 2.0という記載だけからCUDAを推測してはいけない。現在のPCのCUDAバージョンが、既存実験時と同じとも限らない。ログがなければその旨を著者内で整理し、再実験では環境を完全に記録する。

### 6.2 Reviewer 2：4論点

Reviewer 2には番号がないため、本報告書では文章の順にR2-1〜R2-4を付ける。

| 番号 | 指摘と評価 | 対応方針 | 返答の中心となる証拠 |
|---|---|---|---|
| R2-1 | NOLTA2025からの増分が小さい。最重要 | 会議論文本体と逐条照合。既出着想を認め、新しい問い・比較・独立検証を追加。FONDUEとの差分も同時に整理 | 差分表、直接競合比較、改訂後の研究課題 |
| R2-2 | 新アーキテクチャ・目的関数がなく、恣意的heuristic。重大だが「新構造が必須」という含意までは受け入れない | 実証的な選択手順として位置づける。品質条件、停止・拡張、閾値検証、構成要素比較を追加。理論は既知・限定条件の結果として整理 | Algorithm、E1/E4/E6、理論の削除・修正一覧 |
| R2-3 | 全探索以外の既存次元選択・剪定法との比較がない。妥当 | FONDUEと既存剪定法を追加し、validation MSE/ELBO/下流選択、同予算探索とも比較 | E1の方法別性能・費用・選択次元表 |
| R2-4 | Table 4の数値形式・括弧と全体の粗さ。対応可能だが表だけでは不十分 | Table 4を整形し、誤ったelbow説明も修正。全図表をログから再生成して照合 | 修正Table 4、再生成記録、全文の表記統一 |

R2-1への回答で「4ページから33ページへ増えた」「seed数を増やした」だけを強調するのは避ける。また、原稿に既に存在するTable 12や感度分析を、今回新規に追加した結果として説明しない。

R2-2への回答は、理論の新規性を無理に拡張するより、実測で支持できる有用性を示す方がよい。FONDUEやGECO系手法との比較で優位性が見られない場合は、その結果を開示した上で、適用条件や誤診断の分析にどの追加価値があるかを示す。

### 6.3 Reviewer 3：7項目

| 番号 | 指摘と評価 | 実施する修正 | 回答時に示す証拠 |
|---|---|---|---|
| R3-1 | 2×ID、V1、V2がheuristic。妥当 | 係数2を初期候補の既定値へ限定。V1/V2を補助診断へ整理し、冗長な上側条件を説明。独立条件で閾値を検証 | E4、閾値表、品質・費用への影響 |
| R3-2 | MNIST/Fashion-MNIST以外と自然画像での検証が弱い。妥当 | CIFAR-10/SVHNの既存観測を全手順の評価へ拡張し、新しいデータ条件を追加 | E5、追加データの比較結果、適用見送りを含む結果 |
| R3-3 | ID推定の参照AE依存。既存比較はあるが不足 | 入力/PCA/FC-AE/Conv-AE、容量、seed、サンプル数を比較。最終選択への伝播まで評価 | E3、独立参照モデルの設定・結果表 |
| R3-4 | Proposition 2から非線形VAEへの主張をさらに限定すべき。妥当 | 固定β・固定観測分散・Gaussian source等の仮定を明記。AUとの同一視、τ_effの循環説明、KL符号からの方向推論を除く | §4・Appendix Aの具体的な修正 |
| R3-5 | reconstruction、ELBO、下流検証によるbaseline不足。妥当 | B1–B3を共通グリッド・独立testで比較。手法ごとに最適化対象とラベル使用を明記 | E1の選択基準別結果表 |
| R3-6 | “conditional”が繰り返され読みにくい。妥当 | Methodsに適用条件、Discussionに限界を集約。各節では必要な条件だけ記す | 短縮した本文と適用範囲表 |
| R3-7 | Steps 1–3と閾値をまとめた擬似コードが必要。妥当 | §5.4を基に、実際のコードと一致するAlgorithmを掲載。ID不安定時、範囲外、予算上限、同順位の処理も定義 | Algorithm、設定表、再現用コード |

R3-4については、現原稿にも限定の注意書きがあることを主な反論にしない。本文の注意書きと付録の断定が不整合であるため、具体的に何を削除・修正したかを示す。

## 7. 原稿全体の再構成

### 7.1 推奨する構成

| 改訂後の章 | 内容 | 現原稿からの変更 |
|---|---|---|
| 1. Introduction | 実務上の問題、既存選択法の限界、研究上の問い、簡潔な貢献 | 現§1.2の数式・結果列挙、§1.4の詳細な限界を移動 |
| 2. Related Work | 次元削減、ID推定、次元選択、剪定、先行会議論文との関係 | FONDUE・GECO/L0・ARD系を追加。既存研究を測定だけと一括しない |
| 3. Problem Formulation and Protocol | 目的、データ分割、品質条件、Algorithm、閾値、失敗時の処理 | 選択とAU診断を分離。目視依存を削減 |
| 4. Theoretical Background | 位相的必要条件、限定した線形Gaussianの解釈 | 既知結果の背景として簡潔に整理。危うい一般化を削除 |
| 5. Experimental Setup | モデル、損失、データ、分割、seed、比較法、計算費用 | 現§5.1を大幅に明確化 |
| 6. Results | 主要比較、追加データ、参照依存、閾値、診断・失敗例 | 現「成功/境界」の先取りをやめ、独立指標で順に示す |
| 7. Discussion | 何が有効だったか、既存法との差、適用範囲、限界 | “conditional”の反復を集約 |
| 8. Conclusions | 追加実験で支持された内容と限定を短く記述 | 達成していない一般化や未再計算の削減率を除く |
| Supplementary Material | 全グリッド、seed別ログ、補助的感度、細かな導出 | 主結論に不要な探索経緯を移す |

Methods中の現Table 3は実験結果の要約なので、Results末尾またはDiscussionへ移す。研究前提として既知の適用条件と、実験から分かった適用範囲を同じ表に入れない。

### 7.2 Abstractの書き換え方針

Abstractは以下の順に組む。

1. 潜在次元選択の計算負担と、再構成品質とのトレードオフ。
2. IDを用いて候補を優先し、品質と活性を別々に確認する手順。
3. 比較対象と、検証したデータ・モデルの範囲。
4. 独立testで得られた品質と総費用の具体的な結果。
5. 適用を見送った条件と、結論の範囲を一文で述べる。

新しい結果が得られる前に、現在の「60–80%削減」をそのまま入れない。結果が未確定な段階では完成版Abstractを確定せず、目的・方法の文だけ先に整える。

### 7.3 削除または置換すべき表現

| 現在の表現・主張 | 修正方針 |
|---|---|
| “a selection principle grounded in the data itself has not been established” | IDを用いた既存選択法を認め、未解決の実務的問題を具体化 |
| 先行研究は“measure”、本研究は“design” | 次元選択まで扱う先行研究を含む比較に変更 |
| “the elbow appears exactly at m = d_emb” | 実測MSEと位相的必要条件を分け、exactlyを削除 |
| “the protocol return[s] the correct dimension” | 所定の品質条件下で選んだ候補、と目的に沿って記述 |
| “correctly identified” an over-pruning boundary | 独立した品質回復の証拠がない間は「AU条件への不適合を示した」に限定 |
| “β too small”を断定する節見出し | 「指定した学習条件で活性が維持された」等、観測を表す見出しに変更 |
| “Step 1 works”を文献値との一致で判断 | 推定の安定性・一致を観測した、と限定。真値との一致は主張しない |
| “no automatic truncation”からAEは全次元を使うと説明 | 明示的な剪定機構を持たない、と限定 |

### 7.4 追加・再編する図表

図を単に増やすより、査読上の疑問に答える図を優先する。

| 図表 | 内容 | 対応する懸念 |
|---|---|---|
| 新Table A | NOLTA2025、FONDUE等、本研究の比較 | 新規性 |
| 新Algorithm 1＋設定表 | 入力、閾値、探索、品質、停止、出力 | 再現可能性・恣意性 |
| 新Figure A | train/validation曲線と最終test値 | 過学習・収束 |
| 新Table B | 方法別のm、品質、下流性能、総費用 | 既存法比較 |
| 新Figure B | 品質と総時間、品質とmの関係 | 実用的利点 |
| 新Figure C | 参照表現・m_refによるIDと最終選択の変化 | モデル依存性 |
| 新Figure D | 閾値感度とID/AUの構成要素比較 | heuristicの妥当性 |
| 新Figure E | 同一test画像の再構成比較 | AU削減に伴う品質劣化 |
| 新Table C | 成功、不適合、適用見送り、再探索費用 | 適用範囲の明確化 |

mean±SD、CI、subsampling intervalを区別し、seed数、データ分割、評価モードを各captionに記す。図中でd̂を真値のように表示しない。小さいmほど圧縮されることと、AUが小さいことを別の軸で示す。

## 8. 返答書の作り方

### 8.1 各項目を同じ形式で回答する

各Reviewerへの返答は、指摘全文または要点、著者の回答、具体的な修正、改訂稿のページ・行、追加証拠の順とする。英語返答書は、実験と改訂が完了してから過去形で書く。

例えば、新規性の項目なら次の順序がよい。

1. 会議論文との差分が明瞭でなかったことを認める。
2. 既出の着想を明示し、独立した選択・剪定研究の議論を追加したと説明する。
3. 新しく評価した問いと比較法を示す。
4. 実測で支持された結論と、支持されず縮小した主張を示す。
5. 差分表・結果表・改訂箇所へ対応づける。

「既にconditionalと書いている」「補足資料にある」「結果は理論と整合的」という返答だけでは不足する。本文のどこが明確になり、何が新しい証拠になったかを示す。

### 8.2 同意しきらなくてよい指摘

- **新しいアーキテクチャがないこと：** 実証的な選択手順として価値を示す。ただし比較不足は認める。
- **学習曲線はAccuracyにすべきという提案：** 本研究の学習課題に適したMSEを用い、Accuracyは下流評価で報告する。
- **毎epochのtest曲線：** test隔離を保つ評価方法を説明し、必要なら固定後の事後図として示す。
- **指定されたMST論文：** 研究目的が異なることを丁寧に説明し、関連の強い古典的手法を補う。
- **導入の結論をすべて削除：** 詳細な結果は移動するが、簡潔な貢献説明は残す。

反論は、査読者を否定する形ではなく、研究目的に照らした代替対応と証拠をセットにする。

### 8.3 Editorへの説明

カバーレターでは、英文・体裁の改善より先に、先行研究との差分の再整理、独立した検証設計、直接比較法の追加、理論・診断の過大な主張の修正を述べる。各Reviewerへの詳細回答は返答書に委ねる。

追加実験が現行の主張を支持しなかった場合、そのことを隠して「全コメントに完全に対応した」としない。本文の主張をどのように縮小したかを説明する。

## 9. 実施順序と作業の区切り

改訂期限と実験の所要時間が不明なので、確約する日程ではなく、依存関係に沿って進める。

| 段階 | 作業 | 完了条件 |
|---|---|---|
| 1 | NOLTA2025・FONDUE等の照合、損失・データ分割・ログ監査 | 新規性として主張できる範囲と実装上の修正点が決まる |
| 2 | 品質条件、比較法、グリッド、閾値、停止条件を固定 | Algorithmと設定ファイルが一致する |
| 3 | 代表条件の試行と時間計測 | 実行可能性と全体費用を見積もれる |
| 4 | E1/E2の主要比較とE5の自然画像評価 | 独立test、品質、総費用、5 seedsの主結果が揃う |
| 5 | E3/E4と必要なE6 | 参照依存、閾値依存、診断の有用性が説明できる |
| 6 | 結果に合わせて本文・Abstract・Conclusionsを改訂 | 主張と図表・数値が整合する |
| 7 | Reviewer別返答と最終点検 | 各コメントから改訂箇所・証拠へ追跡できる |

試行で得たモデル1回の時間を用いて、データ×候補m×学習seed×必要なβ条件の費用を積算する。比較法の設定探索と参照AEも加える。補助的なAU閾値変更などは既存統計の再集計で済ませる。

計算時間が限られる場合でも、**新規性の整理、test分離、直接競合比較、品質と費用の再計算、理論の誤り修正**は削らない。削減候補は、補助的なdecoder種類、全データでの大規模なβ探索、主結論に関係しない位相例、補助baselineの数とする。

追加実験が期限内に終わらない場合は、必要な実験内容と見積もりを確定してからEditorへの延長依頼を検討する。延長を依頼せず未完了の実験を実施済みと記載したり、すべてをfuture workへ送ったりしない。

## 10. 結果別の改訂方針と提出前チェック

### 10.1 どの結果なら、どの主張を残すか

| 追加検証の結果 | 残せる主張・必要な変更 |
|---|---|
| 複数データで品質を保ち、直接競合を含め総費用が改善 | 実用的なID-guided選択手順として主張。効果のある条件を明示 |
| 品質は保てるが、FONDUE等より高コスト | 最速という主張を削除。診断・適用見送り・頑健性の追加価値を検証して主張 |
| IDなしの粗い探索と同程度 | IDが必須という主張を弱める。推定費用に見合う条件を限定 |
| AUを加えても品質診断が改善しない | AUを補助分析へ移し、主要な方法貢献から外す |
| 自然画像では一貫して適用を見送る | 自然画像への有効性を主張しない。適用範囲の狭さが残ることを認める |
| 既存法に対する利点が示せず、会議論文との差分も小さい | 現行の方法提案としての採録根拠は弱い。診断研究への再構成または追加の研究が必要 |

「全データでAU条件を通過させる」「既存の結論を維持する」を成功条件にしない。独立評価で支持された結論を、適切な強さで提示することを成功条件とする。採録確率を数値で推定する材料はなく、修正による採録の保証もできない。

### 10.2 提出前チェック

- [ ] NOLTA2025本体と比較し、既出と追加の内容を区別した。
- [ ] FONDUEの主手法と付録の関連手順を確認し、差分を説明した。
- [ ] 少なくともFONDUEと既存の次元削減・剪定法を比較した。
- [ ] train/validation/testの役割を分離し、選択にtestを使用していない。
- [ ] 再構成、ELBO、下流評価の選択目的を区別した。
- [ ] AU条件の適合だけを品質保証・原因診断として扱っていない。
- [ ] ID推定・参照学習・追加確認・再探索を総費用に含めた。
- [ ] 全探索の候補集合と比較分母が一致している。
- [ ] 自然画像のStep 1を独立した参照モデルで確認した。
- [ ] 閾値を評価対象のtestに合わせ直していない。
- [ ] 主要結果に学習seedのばらつきを示した。
- [ ] Table 4のelbow断言、Remark 2の有限δとの混同、Appendix Aの不適切なrank・保証を修正した。
- [ ] 損失のreduction、人工データの出力層、CUDA等の環境をコード・ログと照合した。
- [ ] 図表を元ログから再生成し、本文中の数値も一致させた。
- [ ] 参考文献の書誌・DOI・URLを統一した。
- [ ] Reviewer 1の10項目、Reviewer 2の4論点、Reviewer 3の7項目に漏れなく回答した。

## 11. 確認した主な外部資料

以下は2026年9月12日時点で、今回の修正方針のために確認した一次資料である。文献一覧の全面監査や各比較法の実装再現を完了したものではない。

1. **Bonheme, L.; Grzes, M.** *FONDUE: an algorithm to find the optimal dimensionality of the latent representations of variational autoencoders*. arXiv:2209.12806, 2022. [書誌・要旨](https://arxiv.org/abs/2209.12806)、[本文](https://arxiv.org/pdf/2209.12806)。特に§4.3とAppendix Fを比較対象とする。
2. **De Boom, C.; Wauthier, S.; Verbelen, T.; Dhoedt, B.** *Dynamic Narrowing of VAE Bottlenecks Using GECO and L0 Regularization*. [原論文](https://arxiv.org/abs/2003.10901)、[本文](https://arxiv.org/pdf/2003.10901)。再構成制約を伴う次元削減の直接比較候補。
3. **Saha, S.; Joshi, S.; Whitaker, R.** *ARD-VAE: A Statistical Formulation to Find the Relevant Latent Dimensions of Variational Autoencoders*, 2025. [書誌・要旨](https://arxiv.org/abs/2501.10901)、[確認した本文](https://arxiv.org/html/2501.10901v2)。関連性推定による次元適応の比較候補。
4. **Iyer, K.; Bhalodia, R.; Elhabian, S.** *RENs: Relevance Encoding Networks*, 2022. [原論文](https://arxiv.org/abs/2205.13061)。非線形モデルのARD系関連研究として参照。
5. **Gałka, Ł.; Karczmarek, P.; Tokovarov, M.** *Isolation Forest Based on Minimal Spanning Tree*. IEEE Access, 10, 74175–74186, 2022. [DOI](https://doi.org/10.1109/ACCESS.2022.3190505)。Reviewer 1指定文献。書誌・抄録上の主題は異常検知であり、潜在次元選択法として扱わない。
6. **Obata, C.; Jin’no, K.** *Analysis of the Relationship Between Manifold Structure and Latent Space Capacity of Input Data Using VAE*. NOLTA2025, pp.32–35. [著者研究室の業績一覧](https://www.comm.tcu.ac.jp/nel/list_j_2025.html)。書誌確認に使用。本体は未確認なので差分表の確定には会議論文が必要。
7. **Dai, B.; Wipf, D.** *Diagnosing and Enhancing VAE Models*, 2019. [原論文](https://arxiv.org/abs/1903.05789)。VAEのモデル化・目的関数に関する背景の補強候補。

本報告書の具体的な不整合の指摘、数値差の計算、実験設計、優先順位は、添付MAKE47と査読コメントを照合した分析・提案であり、上記論文の結論をそのまま本研究へ適用したものではない。
