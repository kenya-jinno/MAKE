# MAKE53 / NN53 改訂記録

対象：MAKE52_revision_review.md と更新された MAKE52_実験検証結果.md。
既存のMAKE52/NN52原稿・提供された実験コード・raw結果は変更せず，
MAKE53/NN53として別ファイルを生成する。

## 改訂内容

| 査読メモの指摘 | MAKE53での対応・位置 |
|---|---|
| §4.1：実証研究としての貢献と旧主張の扱い | Introduction / Related Work：統制された実証比較を貢献とし，一般的な学習呼出し60–80%削減の主張を撤回。V1/V2を最終選択に使わず，追加予測価値を確立していないことを明記。理論を数値規則の保証としない。 |
| §4.2：既存法の選択後native品質 | §5.5，§6.5，Table 11，Appendix F.1：ARDの4条件（全次元，非選択軸マスク，30 epochs復号器微調整，通常VAEへの転用）を4データセット・3シードで追加。原著全体の再現という位置づけにはしない。 |
| §4.3：ELBO比較の範囲 | §5.4–5.5，Discussion：真のMC ELBO比較は引き続きMNIST・seed 42・proxy選択checkpointの範囲。追加pruning JSONのelbo欄を除外。 |
| §4.4：100条件とID採用25条件 | Abstract / Conclusions：ID採用はMNISTの25条件，残る75条件は同じ昇順fallback。候補数8.84対5.84は25条件の値。10,000件は100設定×100条件と明記。 |
| §4.5：補足ZIPの実物確認 | MAKE53_reproducibility.zipを更新。READMEに再集計・組版手順，完全なファイル対応，未保存記録，使用禁止のELBO欄を記載。検証結果は末尾に記録。 |
| §5：読みやすさ・実装修正履歴 | 過去の損失尺度監査をAppendix C.2へ，歴史的な剪定個数比較をAppendix Fへ移動。ファイル一覧をディレクトリ単位に整理し，個別名をREADMEへ移動。 |
| §5：追加GPU環境 | §5.5で，追加ARM/ARD/nativeマスク実験が§5.3と同じGPU・ソフトウェア環境であることを明記。12件のnativeマスクraw provenanceとも照合。 |
| §5：曖昧なデータセット名・負号 | “three natural-image datasets”を“the three datasets other than dSprites”へ変更。表の負数を数式モードに統一。 |

タイトルはユーザー指定の継続性を優先し，MAKE52から変更していない：
Intrinsic-Dimension-Guided Bottleneck Selection for Variational Autoencoders with Active-Unit Diagnostics.
日本語著者名は「小端 千佳」「代 美月」「神野 健哉」。

## 追加データの監査と解釈

再集計：src/make53/summarize_native_pruning.py。
結果：results/make53/native_pruning_summary.json。
入力と監査コードのSHA-256：results/make53/native_pruning_manifest.json。

- 統合JSONと各データセット別JSONが一致し，4データセットに42・123・777の3シードがある。
- 12件の全次元validation/test MSEと選択個数は，先行する標本中心ARDの値と完全一致する。同じ設定の対応のある拡張であり，独立した基準モデル反復として二重計上しない。
- 転用グリッドが選択個数の最近傍で，validation/test値が通常VAEキャッシュと一致する。
- 全次元・マスク・微調整・転用の4条件について，同じβ=1アンカー由来のQを再計算した。MNIST，Fashion-MNIST，CIFAR-10は各条件3/3，dSpritesは各条件0/3。
- dSpritesでは平均validation MSEが0.0013323→0.0030149→0.0018265（全次元→マスク→微調整）。全次元基準の平均MSE比は+126.284%と+37.087%。転用先も0.0016372でQ未達であり，「選択次元の有効性が検証された」とは記載しない。
- 各除去軸の個別寄与は測定していない。まとめて除去した効果から「除去したすべての軸が有用」とは結論しない。
- マスクは元のネットワーク上で座標をゼロにする操作。実際のパラメータ数・推論時間削減は測定していない。
- 復号器微調整は全18,000学習例を用い，初期学習時の事前更新専用1800例も含む。追加の計算とデータ接触がある。マスクなし30 epochs微調整の対照はない。
- sample-centred variance updateは原著ゼロ中心設定の「訂正」ではなくローカル変種。今回の結果から原著ARDの成功・失敗を一般化しない。

## 提供レポートの文面をそのまま採用しなかった点

1. 新しいnative_pruned_spec.jsonは真のELBOを要求するが，実装のeval_masked/eval_passは事後平均の再構成尤度と標準正規KLを使う。K=32はMSEだけを変える。新しいJSONのelbo欄も真のnative ELBOの証拠から除外した。
2. 新しいnativeマスクJSONには曲線，epochカウンタ，選択軸の識別子，関連度ベクトル，checkpointはない。300/30 epochsはスクリプトと内部仕様の設定として説明し，保存曲線から確認したとはしない。
3. nativeマスク実験の時間は初期学習区間と復号器ループだけ。ヤコビアン・マスク生成・最終評価・アンカー・転用先学習の費用を含む総費用とはしない。
4. 先行するARMのdSpritesでは全ゲートが中間域にあり，全実行で制約未達。レポート旧節の「修正後はゲートが二値化し，制約も満たす」を全データの結論として再導入しない。
5. CIFAR-10の旧ARD 14.2→26程度の差を，自動微分ヤコビアンだけに帰属しない。MAKE51時点の正規化監査で26.0へ修正済みであり，学習設定も違う。

提供レポート・仕様・元コードは追跡用にそのまま収録する。現在の論文・この記録・補足READMEの限定を適用する。

## 継続して残る範囲

原著手法全体との包括的benchmark，複数学習シードの真のELBO比較，独立参照AE群，
完全なend-to-end時間，前処理漏洩を除いた合成データ再評価は今回追加していない。
新しいnativeマスクはMSEの評価を補ったが，未保存のモデルをsummaryだけから再評価できない。
既存30件の参考文献は引き継いだ。書誌情報全件の外部照合を新たに完了したという主張はしない。

## 生成・検証結果

- 英語PDF：45ページ，日本語PDF：52ページ。全ページを画像化して確認した。
- 日本語版は166本文ブロックを対応づけ，44表の数値セル，10件の表示数式，
  引用出現回数，参考文献本文が英語版と一致することを自動確認した。
- 両版の103ラベルの表示番号と30件の参考文献番号を照合した。
- コンパイル後に，未解決の参照・引用，欠落文字，Overfull警告がないことを確認した。
- 追加結果の統合・データセット別記録，転用先キャッシュ，Q，標本SDを再集計した。
- 補足ZIPを実際に展開し，READMEに記載した9個の再集計・組立てコマンドを実行した。
  10,000件の探索感度解析，対応のあるAU解析，MC ELBO集計，開始幅対照，
  初期ARM/ARDおよび新しいnativeマスク集計を含み，47個の表・原稿・主要記録が
  byte単位で一致した。学習実験は再実行していない。
- 数値再集計にはREADME記載のPython 3.11.11環境を統一して用いた。
  別Python版による標本SDの末尾桁差は，掲載表の丸め値には影響しなかった。
- 検証の機械可読記録：
  results/make53/supplement_verification.json，
  results/make53/manuscript_verification.json，
  results/nn53/translation_verification.json。
