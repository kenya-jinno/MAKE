# MAKE49 段階 5 — E4・E3・E5

対象: `MAKE49_査読対応方針.md` §10 段階 5
完了条件: 未使用条件で価値と適用範囲を評価できること
前提: `MAKE49_段階4_主比較結果.md`
実施日: 2026-09-13

---

## E3: 参照表現への依存性（R3-3 直結）— 完了

**問い**: 参照 AE が変わっても、ID だけでなく最終選択と品質が安定するか。

比較した表現: 入力空間 / PCA 前処理空間 / FC-AE（複数 $m_{\mathrm{ref}}$・各 3 seeds）。
推定用サンプル数も変えた。Conv-AE 参照は計算予算の都合で未実施。

### E3-1. 推定用サンプル数への依存（入力空間）

| データ | $n=1{,}000$ | $n=2{,}000$ | $n=5{,}000$ | $n=10{,}000$ | 振れ幅（相対） |
|---|---|---|---|---|---|
| MNIST | 11.93 | 13.32 | 13.63 | 13.62 | 0.13 |
| Fashion-MNIST | 12.81 | 13.11 | 12.89 | 12.67 | 0.03 |
| dSprites | 10.73 | 9.66 | 8.71 | **7.70** | **0.33** |
| CIFAR-10 | 22.60 | 24.35 | 23.53 | **19.50** | **0.21** |

### E3-2.【重要】推定器の一致は標本数に依存する

TwoNN と MLE の相対差（設定表の基準は $\le 0.25$）:

| データ | $n=1{,}000$ | $n=2{,}000$ | $n=5{,}000$ | $n=10{,}000$ |
|---|---|---|---|---|
| MNIST | 0.207 ○ | 0.112 ○ | 0.106 ○ | 0.130 ○ |
| Fashion-MNIST | 0.054 ○ | 0.140 ○ | 0.288 **×** | 0.417 **×** |
| dSprites | 0.068 ○ | 0.345 **×** | 0.621 **×** | 0.885 **×** |
| CIFAR-10 | 0.063 ○ | 0.089 ○ | 0.273 **×** | 0.686 **×** |

**4 データ中 3 データで、標本数を増やすと一致基準を満たさなくなる。**
$n=1{,}000$ では全データが基準を通過し、$n=10{,}000$ では MNIST しか通過しない。

これは重大である。**`ID_UNRELIABLE` を返すかどうかが、推定用標本数という自由パラメータで決まる。**
段階 4 で dSprites が `ID_UNRELIABLE` になったのは $n=10{,}000$ を事前登録していたためで、
$n=1{,}000$ を選んでいれば通過していた。
**閾値を通すために標本数を選ぶ余地を残してはいけない**（設定表 §5.2 項目 6 の趣旨）。

### E3-3. PCA 前処理空間での推定

| データ | PCA 16 | PCA 32 | PCA 64 | PCA 128 | 入力空間 |
|---|---|---|---|---|---|
| MNIST | 7.88 | 8.85 | 9.79 | 10.95 | 13.62 |
| Fashion-MNIST | 7.10 | 7.98 | 8.88 | 9.85 | 12.67 |
| dSprites | 2.68 | 3.31 | 3.89 | 4.32 | 7.70 |
| CIFAR-10 | 11.49 | 14.03 | 15.97 | 17.06 | 19.50 |

**PCA 次元を上げると推定値が単調に上がる。** 全データで入力空間の推定値を下回る。

### E3-4. 参照 AE の容量と seed への依存

| データ | $m_{\mathrm{ref}}$ | TwoNN（3 seeds） | 容量間の相対範囲 | 安定性判定 |
|---|---|---|---|---|
| MNIST | 55 / 111 / 221 | 10.93±0.09 / 11.78±0.12 / 12.32±0.09 | 0.118 | **安定** |
| dSprites | 30 / 61 / 122 | 3.96±0.48 / 3.91±1.12 / **2.10±0.02** | **0.474** | **不安定** |

seed 間の SD は小さい（MNIST で 0.09–0.12）が、**容量を変えると大きく動く**。
dSprites では $m_{\mathrm{ref}}=122$ だけ 2.10 に落ちる。

### E3-5. まとめ — $\hat d_{\mathrm{ID}}$ は「データの性質」ではない

| データ | 入力空間 | PCA 空間 | 参照 AE | 最大/最小 |
|---|---|---|---|---|
| MNIST | 13.62 | 7.88–10.95 | 10.85–12.41 | **1.73 倍** |
| Fashion-MNIST | 12.67 | 7.10–9.85 | — | 1.78 倍 |
| dSprites | 7.70 | 2.68–4.32 | 2.08–4.56 | **3.70 倍** |
| CIFAR-10 | 19.50 | 11.49–17.06 | — | 1.70 倍 |

**同じデータでも、どの表現で推定するかによって $\hat d_{\mathrm{ID}}$ は 1.7〜3.7 倍動く。**

手順は候補窓を $2\hat d_{\mathrm{ID}}$ から決めるので、**窓の位置も同じ倍率で動く。**
「内在次元から探索窓を導く」という主張は、
**どの表現・どの標本数・どの推定器を使うかを固定して初めて意味を持つ**。
原稿はこの依存性を明示し、固定した設定を必ず併記する必要がある。

既存 Table 12 は MNIST/Fashion の近いモデル群での結果であり、
**「モデル依存性を検討済み」では済まない**ことが自然画像と dSprites で確認できた。

---

## E4: 閾値・構成要素の検証（R3-1 直結）— 完了

**問い**: 係数 2 や AU 条件に依存せず、品質・費用に基づく結論が維持されるか。

保存した次元別 μ 分散から**再学習なしで**再評価した（設定表 §5.5 E4 の想定どおり）。
$\beta=1$、5 seeds、300 epochs、候補グリッド全点。

### E4-1.【重要】AU の判定は $\delta$ で決まる — 自然画像では結論が反転する

$m$ に対する AU（5 seeds 平均）:

| データ | $\delta=0.001$ の AU 範囲 | $\delta=0.01$ | $\delta=0.1$ |
|---|---|---|---|
| MNIST | 2.0–6.8 | 2.0–6.4 | 2.0–6.4 |
| dSprites | 2.0–**29.6** | 2.0–15.6 | 2.0–15.0 |
| **CIFAR-10** | 8.0–**256.0** | 8.0–**167.8** | 8.0–**32.2** |

CIFAR-10 の詳細:

| $m$ | $\delta=0.001$ | $\delta=0.01$ | $\delta=0.1$ |
|---|---|---|---|
| 32 | 32.0 | 31.6 | 30.8 |
| 64 | **64.0** | 57.6 | **32.2** |
| 128 | **128.0** | 86.2 | **28.4** |
| 256 | **256.0** | 167.8 | **23.0** |

- $\delta=0.001$ では **AU $=m$ が全点で成立**し、「遮断が全く働かない」という結論になる。
- $\delta=0.1$ では **AU が 23〜32 で飽和**し、「遮断が働いている」という逆の結論になる。

**原稿の「Conv-VAE では標準的な $\beta$ で AU 遮断が働かない」という主張は、$\delta=0.01$ という
固定値に依存しており、$\delta$ を動かすと反転する。** $\delta$ は原稿では定数として扱われている。

**スケール正規化 AU は解決にならない。** 最大分散に対する相対閾値（rel$=0.01$/$0.05$）を使っても
値は $\delta=0.01$/$0.1$ とほぼ同じで、感度が消えるのではなく**再パラメータ化されるだけ**である
（段階 3 の C6 への回答）。

MNIST だけは $\delta$ に対して頑健（6.2–6.8）である。**開発条件での頑健性を一般化してはいけない。**

### E4-2. V1 / V2 の判定規則は現行係数では機能しない

$\mathrm{AU}/\hat d_{\mathrm{ID}}$ の範囲（$\delta=0.01$）:

| データ | $\hat d_{\mathrm{ID}}$ | AU/$\hat d$ の範囲 | 原稿の V2 下側不変域 $(0.70, 1.0]$ |
|---|---|---|---|
| MNIST | 13.84 | **0.14–0.46** | 全域で**下回る** |
| dSprites | 7.61 | 0.26–**2.05** | 大半が**上回る** |
| CIFAR-10 | 20.29 | 0.39–**8.27** | 大半が**上回る** |

**3 データすべてで原稿の不変域から外れる。** MNIST は下側に、dSprites と CIFAR-10 は上側に外れる。

V1（活性割合 $\mathrm{AU}/m \ge$ 閾値）も、$m$ が $\hat d_{\mathrm{ID}}$ を超えると必ず不成立になる:

| データ | V1 $\ge 0.7$ が成立する $m$ の上限 |
|---|---|
| MNIST | $m \le 8$（$\hat d$ の 0.58 倍まで） |
| dSprites | $m \le 20$（$\hat d$ の 2.6 倍まで） |
| CIFAR-10 | $m \le 96$（$\hat d$ の 4.7 倍まで） |

**V1 は「$m$ が小さければ通る」だけの条件になっており、品質不良の検出力を持たない。**
$m$ が小さいときは AU $=m$（全ユニット活性）なので自動的に通過する。

**結論: V1/V2 は現行の係数では判定規則として機能しない。規則自体の再設計が必要である。**

### E4-3. 品質許容誤差 $\epsilon_D$ の水準と選択 $m$

| $\epsilon_D$（相対） | MNIST | dSprites | CIFAR-10 |
|---|---|---|---|
| 0.01 | 6.0 | 57.6 | 41.6 |
| 0.05 | 6.0 | 36.0 | 32.0 |
| **0.10（主水準）** | **6.0** | **20.0** | **24.0** |
| 0.25 | 4.0 | 12.0 | 16.0 |
| 0.50 | 4.0 | 9.6 | 16.0 |

- **MNIST は $\epsilon_D$ にほとんど反応しない**（6 → 4）。$\beta=1$ で再構成が $m$ に対して平坦なため。
- **dSprites は強く反応する**（57.6 → 9.6、6 倍）。圧縮と品質のトレードオフが実在する。
- CIFAR-10 は中間（41.6 → 16）。

**$\epsilon_D$ は選択結果を支配する自由パラメータである。** 事前登録した 0.10 は
「seed 雑音を上回る」という根拠で決めたものであり、タスク側の要請から導いたものではない。
原稿では**この値の選び方が結果を決めること**を明示する必要がある。

### E4-4. 初期範囲の係数 $c$ の感度（事後的な探索シミュレーション）

**以下は固定グリッドのログ上での事後的な再現であり、実時間の計測を伴わない。**
実時間の実行例は段階 4 の主比較を参照すること。

| データ | $c=1$ | $c=1.5$ | $c=2$（既定） | $c=3$ | $c=4$ |
|---|---|---|---|---|---|
| MNIST（$Q$ 達成率 / 選択 $m$） | 1.00 / 6.0 | 1.00 / 6.0 | 1.00 / 6.0 | 1.00 / 6.0 | 1.00 / 6.0 |
| **dSprites** | **0.00 / 64.0** | **0.00 / 64.0** | **0.40 / 44.8** | 1.00 / 20.0 | 1.00 / 20.0 |
| CIFAR-10 | 1.00 / 24.0 | 1.00 / 24.0 | 1.00 / 24.0 | 1.00 / 24.0 | 1.00 / 24.0 |

- MNIST と CIFAR-10 では $c$ を 1 から 4 まで変えても選択 $m$ が変わらない。
- **dSprites では既定値 $c=2$ が失敗する**（$Q$ 達成率 0.40）。$c=3$ 以上で初めて安定して成功する。

**既定値 $c=2$ は 3 データ中 1 データで不十分である。**
$c=2$ の根拠は埋め込み上界に由来する幾何学的動機であって保証ではない、という
方針 §4.6 の位置づけが、実データで裏づけられた形である。
ただし**結果を見てから $c$ を 3 に変えてはならない**。$c=2$ が失敗した事実として報告する。

## E4-5:【最重要】AU の増分価値 — 事前登録の判定と、それを覆す対照

設定表 §1.1 の方式 2（品質確認から独立した予測）を、事前登録した手順どおりに実行した。

- 評価単位: 1 checkpoint。予測対象 $Y$ は「1 つ小さいグリッド点 $m^-$ のモデルが $Q$ を満たすか」
- 開発条件: MNIST。未使用条件: dSprites, Fashion-MNIST, CIFAR-10
- 予測器: ロジスティック回帰(L2)。正則化は開発条件の validation のみで選択（$C=10$）し以後固定
- 判定規則: 未使用条件の平均 $\Delta\mathrm{AUROC}$ の 95% CI の**下限が 0 を上回るときのみ**増分価値あり

### 事前登録どおりの結果

| 未使用条件 | $n$ | AUROC($S_{\text{base}}$) | AUROC($S_{\text{AU}}$) | $\Delta$AUROC | $Y=1$ の割合 |
|---|---|---|---|---|---|
| dSprites | 55 | **0.500** | 0.589 | +0.089 | 0.29 |
| Fashion-MNIST | 55 | 0.984 | 1.000 | +0.016 | 0.91 |
| CIFAR-10 | 45 | 0.583 | 0.792 | +0.208 | 0.73 |

平均 $\Delta\mathrm{AUROC} = +0.108$、95% CI $=[+0.059,\ +0.157]$。

> **事前登録の判定規則に従えば「AU に増分価値あり」となる。**
> これは段階 2 で登録した予測 4（「区間は 0 を含む可能性が高い」）と**逆**の結果である。

### しかし雑音対照がこの判定を覆す

事前登録には含めていなかったが、**特徴量が 3 本増えただけの効果**と区別できるかを確認した。
標準正規乱数 3 本（AU 特徴量と同数）を $S_{\text{base}}$ に足した場合と比較する。

| 未使用条件 | base | +AU | **+雑音 3 本** | $\Delta$AU | $\Delta$雑音 |
|---|---|---|---|---|---|
| dSprites | 0.500 | 0.589 | 0.529 | +0.089 | +0.029 |
| Fashion-MNIST | 0.984 | 1.000 | 0.888 | +0.016 | −0.096 |
| **CIFAR-10** | 0.583 | 0.792 | **0.917** | +0.208 | **+0.333** |

| | 平均 $\Delta$AUROC | 95% CI |
|---|---|---|
| AU 特徴量 3 本 | +0.108 | [+0.056, +0.158] |
| **雑音 3 本** | **+0.098** | **[−0.025, +0.227]** |

**AU の改善（+0.108）は雑音 3 本の改善（+0.098）と統計的に区別できない。**
CIFAR-10 では雑音のほうが大きく改善している（+0.333 対 +0.208）。

### 原因

基底予測器が弱すぎる。dSprites で AUROC $=0.500$（偶然水準）、CIFAR-10 で 0.583 である。
開発条件（MNIST）の $Y=1$ 率 0.82 に対し dSprites は 0.29 と偏りが大きく、**転移そのものが失敗している**。
転移が壊れた状況では、**どんな追加の自由度も見かけ上の改善を生む。**

### 結論

**事前登録した検定は、AU 固有の寄与を識別する検出力を持っていなかった。**

したがって本結果から「AU に診断としての増分価値がある」と主張することは**できない**。
事前登録の判定規則には**特徴量数を揃えた対照が欠けていた**。これは設計上の不備であり、
結果を見てから規則を変えたものではない。**両方を併記して報告する。**

**AU の扱い**: 段階 3 の H2 不支持、段階 4 の $-$AU 対照（選択に影響なし）に加え、
本判定も AU 固有の価値を示さない。**AU は補助分析へ移す**。

---

## E5: 自然画像での全手順（CIFAR-10、R3-2 直結）— 完了

**問い**: 現状未完了の Step 3 に対し、品質を伴う選択または妥当な適用見送りができるか。

$\beta=1$、Gaussian 尤度、5 seeds、候補グリッド 10 点（8〜256）、独立した参照 AE から開始。

### E5-1. 選択結果・品質・費用

| 方法 | 選択 $m$ | val MSE | 総時間 | VAE | 補助 | 終了状態 |
|---|---|---|---|---|---|---|
| B1 全探索(val MSE) | 140.8 ± 53.5 | **0.01383** | 428 s | 10 | 0 | SUCCESS×5 |
| B2 全探索(val ELBO) | 192.0 ± 45.3 | 0.01388 | 428 s | 10 | 0 | SUCCESS×5 |
| B4 粗い探索 | 131.2 ± 113.9 | 0.01401 | 159 s | 4 | 0 | SUCCESS×5 |
| **提案法 (ID+Q)** | 131.2 ± 113.9 | 0.01401 | 499 s | 4 | 9 | **ID_UNRELIABLE×5** |
| B6b+Q | 48.0 ± 0.0 | 0.01403 | 95 s | 3 | 0 | SUCCESS×5 |
| B3 全探索(下流) | 256.0 ± 0.0 | 0.01406 | 574 s | 10 | 10 | SUCCESS×5 |
| B5 固定 $m=32$ | 32.0 ± 0.0 | 0.01417 | 46 s | 1 | 0 | SUCCESS×5 |
| **B6b FONDUE-VAR (T)** | **34.2 ± 1.3** | 0.01417 | 51 s | 2 | 0 | SUCCESS×5 |
| **昇順走査+Q（ID なし）** | **24.0 ± 0.0** | 0.01512 | **157 s** | 4 | 0 | SUCCESS×5 |
| B6a+Q | 24.0 ± 0.0 | 0.01512 | 134 s | 8.6 | 0 | SUCCESS×5 |
| B6a FONDUE | 14.4 ± 0.5 | 0.01779 | 57 s | 6.6 | 0 | SUCCESS×5 |
| B6b FONDUE-VAR (F) | 3.2 ± 0.8 | 0.03170 | 44 s | 2 | 0 | SUCCESS×5 |

seed ごとの選択 $m$:

| 方法 | seed 別 |
|---|---|
| B1 全探索 | 192, 128, 128, 192, 64 |
| **提案法（fallback）** | **48, 256, 48, 256, 48** |
| B6a FONDUE | 14, 14, 15, 14, 15 |
| **B6b FONDUE-VAR (T)** | **33, 36, 33, 34, 35** |
| **昇順走査+Q** | **24, 24, 24, 24, 24** |

### E5-2.【重要】提案法は自然画像でも適用を見送った

CIFAR-10 でも**全 5 seeds で `ID_UNRELIABLE`** となり、事前宣言した通常探索（B4）へ切り替えた。

| データ | 提案法の終了状態 |
|---|---|
| MNIST | SUCCESS×5 |
| dSprites | **ID_UNRELIABLE×5** |
| **CIFAR-10** | **ID_UNRELIABLE×5** |

**3 データ中 2 データで手順が適用できない。成功しているのは開発条件の MNIST だけである。**

方針 §5.5 E5 は次のように予告していた。

> 全条件で成功する必要はない。ただし**自然画像ではいつも見送るだけで成功は MNIST だけ**という結果なら、
> 汎用的な選択法としての主張は弱いままである。その場合は適用範囲を狭めるか、
> 診断の体系的比較を主目的にする。

**この条件に正確に該当した。**

さらに、切り替え先の B4 は CIFAR-10 で極めて不安定である（48, 256, 48, 256, 48、SD $=113.9$）。
**適用を見送った先の代替手順も信頼できない。**

### E5-3. ID なしの昇順走査は自然画像でも優位

| 方法 | 選択 $m$ | val MSE | 総時間 | 安定性 |
|---|---|---|---|---|
| 提案法（fallback） | 131.2 ± 113.9 | 0.01401 | 499 s + 補助 9 本 | **SD 113.9** |
| **昇順走査+Q（ID なし）** | **24.0 ± 0.0** | 0.01512 | **157 s** | **SD 0.0** |

ID なしの走査は、**3.2 倍安く、選択が完全に安定し、5.5 倍コンパクトなモデル**を返す。
品質は 8% 劣るが、これは $\epsilon_D=0.10$ という許容誤差の範囲内での設計上のトレードオフである。

### E5-4. 3 データを通した昇順走査+Q との対照（段階 4 §4 の完成形）

| データ | 提案法の $m$ | 提案法の時間 | 走査の $m$ | 走査の時間 | 提案法/走査 |
|---|---|---|---|---|---|
| MNIST | 6.0 ± 0.0 | 715 s | 6.0 ± 0.0 | 182 s | **3.93 倍** |
| dSprites | 64.0 ± 0.0（見送り） | 1001 s | 20.0 ± 4.0 | 1798 s | 0.56 倍 |
| CIFAR-10 | 131.2 ± 113.9（見送り） | 499 s | 24.0 ± 0.0 | 157 s | **3.18 倍** |

**ID 誘導の増分価値は 3 データのいずれでも観測されなかった。**
MNIST と CIFAR-10 では同等以上の結果をより安く得られ、
dSprites と CIFAR-10 では ID 判定が手順を止める側に働いた。

### E5-5. 副次的な観測

- **B6b FONDUE-VAR は CIFAR-10 で最も安定した非自明手法**である（34.2 ± 1.3、SD $=1.3$）。
  dSprites での崩壊（SD $=49.5$）と対照的で、**不安定性はデータ依存**である。
  「FONDUE-VAR は一般に不安定」とは書けない。
- B6a FONDUE は 3 データすべてで過小選択する（MNIST 7、dSprites 4.2、CIFAR-10 14.4）。
  本実装の閾値は $0.2\hat d_0$ であり、$\hat d_0$ の推定値に強く依存する（E3-2 参照）。
- 下流基準（B3）は CIFAR-10 で常に最大容量 256 を選ぶ。**選択目的で最適次元が大きく変わる。**

---


---

## 段階 5 の総合判断

### 1. 段階 5 で確定したこと

| # | 事項 | 判定 |
|---|---|---|
| S1 | $\hat d_{\mathrm{ID}}$ は表現・標本数・推定器に依存し、1.7〜3.7 倍動く | **確定**（E3-5） |
| S2 | 推定器の一致判定は標本数で決まる。$n$ を変えると `ID_UNRELIABLE` の可否が反転する | **確定**（E3-2） |
| S3 | AU の判定は $\delta$ で決まる。CIFAR-10 では結論が反転する | **確定**（E4-1） |
| S4 | V1/V2 は現行係数では 3 データすべてで機能しない | **確定**（E4-2） |
| S5 | 既定の係数 $c=2$ は dSprites で失敗する（$Q$ 達成率 0.40） | **確定**（E4-4） |
| S6 | AU の増分価値は、雑音特徴量と区別できない | **確定**（E4-5） |
| S7 | 提案法は 3 データ中 2 データで適用を見送る。成功は開発条件の MNIST のみ | **確定**（E5-2） |
| S8 | ID なしの昇順走査が 3 データすべてで同等以上・より安価 | **確定**（E5-4） |

### 2. 方針 §11 のどの行に該当するか

段階 4 で該当した 2 行に加え、段階 5 で次の行が確定した。

> **「AU を加えても品質診断が改善しない」** → AU を補助分析へ移し、主要な方法貢献から外す。
>
> **「自然画像では一貫して適用を見送る」** → 自然画像への有効性を主張しない。適用範囲の狭さが残ることを認める。

そして最終行に近い状態である。

> 「既存法に対する利点が示せず、会議論文との差分も小さい」
> → 現行の方法提案としての採録根拠は弱い。**診断研究への再構成または追加研究が必要**。

### 3. 現行の枠組みでは維持できない主張（確定）

| 主張 | 反証 |
|---|---|
| ID 誘導が探索を効率化する | 3 データすべてで ID なしの走査が同等以上・より安価（S8） |
| 内在次元から探索窓が導ける | $\hat d_{\mathrm{ID}}$ が表現で 1.7〜3.7 倍動く（S1） |
| AU が余剰次元の遮断を診断する | $\delta$ で結論が反転（S3）、増分価値は雑音と同等（S6） |
| V1/V2 が判定規則として働く | 3 データすべてで不変域から外れる（S4） |
| 係数 2 に幾何学的な裏づけがある | dSprites で $c=2$ が失敗（S5） |
| 自然画像に適用できる | CIFAR-10・dSprites で `ID_UNRELIABLE`（S7） |

### 4. 実証的な価値として残るもの

1. **品質条件 $Q$ の定式化**。既存のどの選択法にも後付けでき、FONDUE 系の品質を大きく改善する
   （dSprites で 0.00550 → 0.00126、CIFAR-10 で 0.01779 → 0.01512）。
2. **共通条件での既存法の体系的評価**。FONDUE の過小選択、FONDUE-VAR の
   **データ依存の**不安定性（dSprites で SD $=49.5$、CIFAR-10 で SD $=1.3$）、
   `keep_mixed=False` の退化を、共通の品質尺度と費用計上のもとで再現可能に示した。
3. **ID 推定の不安定性の定量化**。表現・標本数・推定器・容量への依存を 4 データで測った。
   これは R3-3 への実質的な回答になる。
4. **判定規則が閾値に支配されることの明示**。$\delta$・$\epsilon_D$・$c$・$n$ のいずれもが
   結論を変える自由パラメータであることを示した。

### 5. 推奨する再構成

**「ID 誘導による高速な次元選択法」から「VAE の次元選択における診断と落とし穴の実証研究」へ。**

- 主張の中心を $Q$（品質条件つき選択）と、**閾値依存性の体系的な documentation** に置く。
- ID 推定と AU は「使えると期待されたが、本条件では増分価値が確認できなかった診断」として、
  **否定的結果を含めて報告する**。これは再現可能で検証済みの実証的貢献である。
- 会議論文（NOLTA2025）との差分は、**AU の導入ではなく、共通条件での比較と否定的結果の確立**になる。

### 6. 残る不足（明記すべき事項）

| 事項 | 状態 |
|---|---|
| B7（GECO/L0 系）・B8（ARD-VAE） | **未実施** |
| FONDUE 公表値の再現 | **未達**（dSprites で 4.2 対 公表 12.2） |
| $\beta$ 梯子全体での評価 | 未実施（全実験が $\beta=1$） |
| SVHN | 未実施 |
| Conv-AE 参照（E3） | 未実施 |
| 合成多様体の再実験（A4 の標準化修正後） | 未実施 |
| AU 算出分の計算時間の削減量 | 測定できていない |
| 方式 2 の検定設計 | **特徴量数を揃えた対照が事前登録に無かった**（E4-5） |

**すべての結果は $\beta=1$・4 データセット・候補グリッド 1 種での観測である。**
否定的結論を一般命題に広げず、**本条件で観測されなかった**と書く。
