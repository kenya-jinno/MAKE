# MAKE49 段階 3 — 校正実行の結果

対象: `MAKE49_査読対応方針.md` §10 段階 3 の冒頭（実時間の実測）
完了条件: GPU 実時間上限が確定し、学習時間と統計量の効果、実行費用を見積もれること
前提: `MAKE49_実験設定表.md`（段階 2 の事前登録）、`MAKE49_段階1_監査結果.md`
実施日: 2026-09-12

---

## 総括

**予算の確定は完了した。設定表に未確定の項目はなくなり、段階 4 に進める状態になった。**

しかし校正実行で、**本実験をこのまま開始できない問題が 1 件**見つかった。

| # | 事項 | 判定 |
|---|---|---|
| **C1** | GPU 実時間上限を実測して確定 | **完了** |
| **C2** | 事前登録どおりの設定で dSprites Conv-VAE が**完全に事後崩壊**する | **原因を尤度と特定し解消。dSprites のみ Bernoulli に変更（§9 に記録）** |
| C3 | 事前登録の仕様に矛盾・欠落が 4 件見つかり、結果を見る前に修正した | 完了（§9 変更履歴に記録） |
| C4 | 出所情報の自動記録が機能することを確認（A5 の再発防止） | 完了 |
| C5 | 入力空間の ID 推定は推定器間・標本数間で大きく揺れる | 記録（E3 の材料） |
| C6 | AU が $\beta$ に対して単調でない区間がある（絶対閾値 $\delta$ 由来と見られる） | 記録（E4 で必ず扱う） |

---

## C1 実測値と確定した予算

### 実測（$\beta=1$、損失規約 `sum_recon_sum_kl_v1`、early stopping patience 30、seed 42）

| 条件 | $m$ | 実行 epochs | 最良 epoch | s/epoch | test MSE | AU | plateau |
|---|---|---|---|---|---|---|---|
| MNIST FC-VAE | 16 | 203（早期停止） | 173 | 0.327 | 0.022095 | 6/16 | 未 plateau |
| dSprites Conv-VAE | 16 | 261（早期停止） | 231 | 1.147 | 0.042098 | **0/16** | plateau |
| CIFAR-10 Conv-VAE | 64 | 63（早期停止） | 33 | 0.650 | 0.014339 | 59/64 | 未 plateau |

### 確定した GPU 実時間上限

基準は設定表 §1.6 の**学習ラン数上限 12 ラン**と揃えた。
1 ラン $=$ 1 候補を 300 epochs（早期停止なしの最悪値）学習すること。
余裕係数 1.25 は ID 推定・probe 学習・評価・データ読み込みの固定費に充てる。

| データセット | s/epoch | 1 ラン 300ep | 12 ラン | **上限（確定）** |
|---|---|---|---|---|
| MNIST / Fashion-MNIST | 0.327 | 1.6 分 | 0.33 h | **0.41 h**（1,470 秒） |
| dSprites | 1.147 | 5.7 分 | 1.15 h | **1.43 h**（5,163 秒） |
| CIFAR-10 / SVHN | 0.650 | 3.2 分 | 0.65 h | **0.81 h**（2,924 秒） |

1 方法 $\times$ 1 seed の 3 データ合計は 2.65 h。
5 seeds $\times$ 主要比較法 8 種なら **約 106 h** が全体の粗い上限になる。
epoch あたりの時間は $m$ にほぼ依存しないため、データセットごとに 1 つの $m$ で校正した。

---

## C2【解消済み】dSprites が事前登録の設定で完全に事後崩壊した

### 観測

校正実行（$\beta=1$, 300 epochs 上限）で dSprites Conv-VAE は AU $=0/16$、KL $=0.000$ になった。
$\beta$ の梯子全体で確認したところ、**崩壊は $\beta$ に依存しない**。

`src/make49/stage3_beta_feasibility.py`（60 epochs, seed 42, early stopping なし）:

| $\beta$ | dSprites AU | dSprites KL | dSprites val SSE | CIFAR-10 AU | CIFAR-10 KL |
|---|---|---|---|---|---|
| 0.05 | **0/16** | 0.000 | 173.513 | 64/64 | 119.54 |
| 0.10 | **0/16** | 0.000 | 173.515 | 63/64 | 92.59 |
| 0.25 | **0/16** | 0.000 | 173.519 | 64/64 | 59.36 |
| 0.50 | **0/16** | 0.000 | 173.521 | 60/64 | 37.08 |
| 1.00 | **0/16** | 0.000 | 173.521 | 59/64 | 22.50 |
| 2.00 | **0/16** | 0.000 | 173.523 | 40/64 | 13.03 |
| 4.00 | **0/16** | 0.000 | 173.524 | 17/64 | 6.95 |

**dSprites: 梯子内で崩壊しない $\beta$ は存在しない。**
**CIFAR-10: 梯子全域で学習が成立し、AU は 64 から 17 まで単調に動く。**

### 崩壊の程度

| 参照値 | per-sample SSE |
|---|---|
| 全ゼロ予測 | 173.74 |
| 平均画像予測 | 153.77 |
| **実測（全 $\beta$）** | **≈ 173.52** |

再構成は平均画像（153.77）にすら達しておらず、**実質的に全ゼロを出力している**。
潜在変数は一切使われていない。

### 原因の見立て

dSprites は 64×64 の二値画像で、**白画素は 4.24% しかない**。
設定表 §2 は全データに Gaussian（MSE）likelihood を指定しているが、
dSprites に対する標準的な設定は **Bernoulli（BCE）** であり、
FONDUE が依拠する disentanglement_lib 系の実装もそちらである。

疎な二値データに対する Gaussian 尤度では、デコーダが全ゼロを出す解が強い局所解になり、
そこから抜け出す再構成勾配が KL 項に対して弱い、という見立てが立つ。

**この見立ては診断で切り分けた**（`src/make49/stage3_dsprites_likelihood.py`、下記で確定）。

### 検討した選択肢

dSprites は方針 §5.4 で **P0 の共通ベンチマーク**として追加したデータセットであり、
B6a/B6b（FONDUE 再実装）の主比較を支える。そのため次のいずれかを選ぶ必要がある。

| 選択肢 | 内容 | 影響 |
|---|---|---|
| **(a)** | dSprites のみ Bernoulli 尤度に変える | FONDUE の設定に近づき公表値との比較可能性が上がる。**データごとに尤度が異なる**ことを全方法で揃えて明記する必要がある |
| (b) | 全データを Bernoulli に統一 | CIFAR-10 は連続値なので不適切 |
| (c) | Gaussian のまま dSprites を落とす | 共通ベンチマークを失い、R2 への回答が弱くなる |
| (d) | Gaussian のまま学習設定を変える（lr・warmup 等） | 交絡が増え、$\beta$ 以外の自由度を後から足すことになる |

**診断の結果 (a) を採用した**（下記）。事前登録の変更にあたるため §9 の変更履歴に記録した。
どの尤度を使ったかは全方法・全表で明記し、**尤度の異なるデータ間で損失値を直接順位づけしない**。

### 尤度診断の結果（原因を確定）

`src/make49/stage3_dsprites_likelihood.py`（dSprites, $m=16$, 60 epochs, seed 42）:

| likelihood | $\beta$ | AU | val SSE | val KL | 判定 |
|---|---|---|---|---|---|
| Gaussian | 1.00 | 0/16 | 173.52 | 0.000 | 崩壊 |
| Gaussian | 0.25 | 0/16 | 173.52 | 0.000 | 崩壊 |
| **Bernoulli** | 1.00 | **14/16** | **7.30** | 28.03 | 学習している |
| **Bernoulli** | 0.25 | **16/16** | **5.91** | 51.65 | 学習している |

参照値: 全ゼロ予測 SSE $=173.50$、平均画像予測 $=153.88$。

**Bernoulli に変えると val SSE が 173.52 から 7.30 へ 24 分の 1 になり、AU が 0 から 14 に回復する。**
原因は $\beta$ でも学習長でもアーキテクチャでもなく、**尤度**である。

### 決定（選択肢 (a) を採用、設定表 §2.6 と §9 に記録）

**dSprites のみ Bernoulli（BCE）、他のデータは Gaussian（MSE）とする。**

守る事項:

- 使用した尤度を全方法・全表・全図で明記する。
- **尤度の異なるデータ間で損失値を直接順位づけしない。**
- $\beta$ の意味は尤度によって変わる。**データをまたいで同じ $\beta$ を「同じ強さ」と書かない。**
- 評価の**画素平均 MSE は尤度によらず共通**に計算し、品質条件 $Q$ はこの共通尺度で判定する。
- 同じデータセット内では全比較法が同じ尤度を使う。

これは事前登録の変更だが、**結果を見て有利な方を選んだものではない**。
Gaussian では全 $\beta$ で学習自体が成立せず、原因を尤度と特定できたための修正である。

### Bernoulli 下での $\beta$ 梯子の確認

`src/make49/stage3_dsprites_bce_ladder.py`（dSprites + BCE, $m=16$, 60 epochs）:

| $\beta$ | AU | val SSE | val KL | 遮断 | 梯子内 |
|---|---|---|---|---|---|
| 0.25 | 16/16 | 5.906 | 51.65 | なし | ○ |
| 0.50 | 16/16 | 6.320 | 36.84 | なし | ○ |
| 1.00 | 14/16 | 7.301 | 28.03 | **あり** | ○ |
| 2.00 | 12/16 | 9.045 | 20.98 | **あり** | ○ |
| 4.00 | **16/16** | 12.699 | 15.28 | なし | ○ |
| 8.00 | 7/16 | 22.868 | 10.01 | あり | 追加点 |
| 16.00 | 8/16 | 29.303 | 6.37 | あり | 追加点 |

**梯子内で遮断が起きる $\beta$ は 1.0 と 2.0。梯子は dSprites でも使用可能である。**

### 記録すべき異常: AU が $\beta$ に対して単調でない

$\beta=4.0$ で AU が 12 から **16 に戻る**。
一方で KL は単調に減り（20.98 → 15.28）、SSE は単調に増える（9.045 → 12.699）。
すなわち**正則化は強まっているのに AU だけが逆行している**。

これは AU が $\mathrm{Var}_x(\mu_j) > \delta$ という**絶対閾値**で定義されているためと考えられる。
$\beta$ が上がると $\mu$ 全体のスケールが縮み、
「情報を持つか」ではなく「分散が $\delta=0.01$ を超えるか」で判定が揺れる。
段階 1 の A4（$\delta$ がデータスケールに依存する）と同じ問題が、
今度は**同一データ内で $\beta$ を変えただけで現れた**。

**E4（$\delta$ 感度）で必ず扱う。** AU を主要指標として使う限り、
$\delta$ の絶対閾値性は診断の信頼性に直結する。
$\mu$ のスケールで正規化した AU の定義を対照として用意することを検討する。

### 副次的な確認: CIFAR-10 でも AU は $\beta$ で決まる

CIFAR-10 の AU は $\beta$ の単調減少関数（64 → 17）で、$m$ は 64 に固定されている。
これは段階 1 の A2（AU は $m$ ではなく $\beta$ で決まる）を、
**修正規約のもとで別のデータセット・別のアーキテクチャで再確認したもの**である。

---

## C4 出所情報の自動記録（A5 の再発防止の確認）

段階 1 の A5 で「CUDA/cuDNN/driver が一切記録されていない」ことが問題だった。
新しい記録機構が実際に動くことを確認した。

```
python 3.12.3 / PyTorch 2.7.1+cu118 / CUDA 11.8 / cuDNN 90100
driver 570.211.01 / NVIDIA GeForce RTX 3070
platform Linux-6.8.0-124-generic-x86_64-with-glibc2.39
loss_convention_id sum_recon_sum_kl_v1
```

各ランに `index_hash`・`update_steps`・`plateau`・`elapsed_seconds` も記録されている。

**注記**: 校正実行の後に `src/make49/common.py` へ `early_stopping` 引数を追加したため、
記録された `code_hash`（`7ef85ae8a07dc254`）は編集前のものである。
追加した引数の既定値は従来動作と同一なので校正結果には影響しない。
この点は結果 JSON にも記載した。

---

## C5 入力空間の ID 推定（E3 の材料）

設定表 §5.3 に従い、train から固定乱数で 10,000 点を取って推定した。

| データ | TwoNN | MLE | 相対差 | 一致基準（0.25） | AE 窓 $[\hat d, 2\hat d]$ | VAE 初期点 |
|---|---|---|---|---|---|---|
| MNIST | 13.841 | 15.468 | 0.118 | **通過** | 16, 20, 24 | 32 |
| dSprites | 7.610 | 14.595 | 0.918 | **不通過** | 8, 10, 12 | 16 |
| CIFAR-10 | 20.288 | 32.790 | 0.616 | **不通過** | 24, 32 | 48 |

さらに、標本数を 2,000 から 10,000 に増やすと推定値が大きく動く。

| データ | TwoNN ($n=2{,}000$) | TwoNN ($n=10{,}000$) | 変化 |
|---|---|---|---|
| MNIST | 13.411 | 13.841 | +3% |
| dSprites | 11.379 | 7.610 | **−33%** |
| CIFAR-10 | 26.315 | 20.288 | **−23%** |

**読み取り**:

- 推定器の食い違いと標本数依存性は、**自然画像と dSprites で顕著**である。MNIST は安定している。
- CIFAR-10 の $n=2{,}000$ での TwoNN $=26.3$ は Pope らの報告値 $\approx 26$ と一致するが、
  $n=10{,}000$ では 20.3 に下がる。**「文献値と一致した」という記述は標本数に依存する。**
- dSprites の $n=2{,}000$ での TwoNN $=11.38$ は FONDUE の設定既定値 `data_ide: 11` に近いが、
  $n=10{,}000$ では 7.61 になる。原著の推定標本数（`ide_batch_size: 10000`）との条件差を要確認。

**ただしこれは `ID_UNRELIABLE` の判定ではない。** 設定表 §5.3 を明確化し、
判定は**参照 AE の潜在表現**に対して適用することにした（入力空間の $\hat d_0$ は $m_{\mathrm{ref}}$ の決定にのみ使う）。
入力空間の不一致そのものは E3（参照表現への依存性）の観測として記録する。

---

## C3 事前登録の修正（すべて結果を見る前に実施、§9 に記録）

| # | 項目 | 修正内容 | 発見の経緯 |
|---|---|---|---|
| 1 | §4 部分集合サイズ | 未指定 → $N=20{,}000$（合成は 5,000） | 校正実行の実装時に欠落が判明 |
| 2 | §1.6 early stopping の適用範囲 | 全実験 → **E7a の軌跡ランは適用しない** | 校正で 203 epochs で早期停止し、§1.2 の 300 epochs 時点が得られない矛盾が判明 |
| 3 | §5.3 ID 推定標本の出所 | validation → **train** | validation は 2,000 点しかなく $n=10{,}000$ が実行不能だった |
| 4 | §5.3 ID 判定を適用する表現 | 未指定 → **参照 AE の潜在表現** | 入力空間で TwoNN と MLE が大きく食い違い、判定の対象が定まらないことが判明 |

いずれも「結果を見て都合よく変えた」ものではなく、**仕様の矛盾・欠落を実行可能にするための修正**である。
修正前後の値と理由は設定表 §9 に記録した。

---

## 生成物

| ファイル | 内容 |
|---|---|
| `src/make49/common.py` | 損失規約・分割・学習・出所記録の共通実装 |
| `src/make49/datasets.py` | MNIST / dSprites / CIFAR-10 の分割規則つき読み込み |
| `src/make49/models.py` | FC-VAE と 32/64 両対応 Conv-VAE |
| `src/make49/stage3_calibration.py` | 校正実行 |
| `src/make49/stage3_budget.py` | 予算導出 |
| `src/make49/stage3_input_id.py` | 入力空間の ID 推定 |
| `results/make49/stage3_calibration.json` | 実測値と全学習曲線 |
| `results/make49/stage3_budget.json` | 確定した上限 |
| `results/make49/input_space_id.json` | ID 推定と初期窓 |
| `src/make49/stage3_beta_feasibility.py` | $\beta$ 梯子の実行可能性確認 |
| `src/make49/stage3_dsprites_likelihood.py` | dSprites の崩壊原因の切り分け |
| `src/make49/stage3_dsprites_bce_ladder.py` | Bernoulli 下での $\beta$ 梯子確認 |
| `results/make49/stage3_beta_feasibility.json` | 全 $\beta$ での崩壊判定 |
| `results/make49/stage3_dsprites_likelihood.json` | 尤度診断 |
| `results/make49/stage3_dsprites_bce_ladder.json` | BCE 下の $\beta$ 対 AU |

---

## 段階 3 の完了判定と次の作業

| 項目 | 状態 |
|---|---|
| GPU 実時間上限の確定 | **完了**（§8.2） |
| 設定表の未確定項目 | **ゼロ** |
| 事前登録の矛盾・欠落の解消 | **完了**（5 件、すべて §9 に記録） |
| dSprites の実行可能性 | **完了**（Bernoulli で解消） |
| 出所情報の自動記録 | **完了**（A5 の再発防止を実地で確認） |

**段階 3 の残り**: dSprites と MNIST での E7a、E2 の代表試行。
E7a は early stopping を適用せず 300 epochs まで回し、1/2/5/50/300 の checkpoint を保存する。
読み取り A（変数型、active+mixed を主比較）・B（AU）・C（$\mathrm{IDE}_z - \mathrm{IDE}_\mu$）を
同一 checkpoint から計算する。

**段階 4 に進む前に確認すること**: 本報告の C6（AU の非単調性）は
AU を主要指標として扱えるかに直結するため、E4 の $\delta$ 感度を前倒しで検討する価値がある。
