# MAKE49 — MDPI MAKE 査読対応方針（統合版）

最終更新: 2026-09-12（v4: `MAKE49_revision_policy_v3_review.md` のレビューを反映）
対象原稿: `submission_MAKE47/main_paper_MAKE47.pdf`（投稿版）
現行最新: `main_paper_MAKE48.tex`（編集部の言語・書誌チェック対応のみ。本文内容は MAKE47 と同一）
改訂版: **`main_paper_NN49.tex` → `main_paper_MAKE49.tex`**

統合元:
- `MAKE47_revision_strategy.md`（665 行、原稿内在の問題を含む詳細版）
- `MAKE49_査読対応方針_v1_backup.md`（初版、査読コメント逐条対応中心）
- `MAKE49_査読対応方針_v2_backup.md`（v2、NOLTA2025・FONDUE 照合後）
- `MAKE49_revision_policy_review.md`（v2 に対するレビュー。v3 で反映）
- `MAKE49_査読対応方針_v3_backup.md`（v3）
- `MAKE49_revision_policy_v3_review.md`（v3 に対するレビュー。v4 で反映）

---

## 総括（先に要点）

2026-09-12 に **NOLTA2025 原稿と FONDUE 原論文（本文・付録・公開コード）を照合**した。
同日、この方針に対する第三者レビュー（`MAKE49_revision_policy_review.md`）を受け、
その指摘を反映した。以下は追加実験の結果を先取りしない形で書いている。

**照合で分かったこと**:
ID を用いて潜在容量を考える基本着想は既出である。とくに FONDUE 付録 G の Algorithm 3（FONDUE-VAR）は、
推定 ID の 2 倍から開始し、潜在変数の活性状態に基づいて次元を選ぶ点で本研究と近い。
現原稿は FONDUE に一切言及していない。改訂稿ではこの関連を Related Work で自ら明示する。

**本改訂で優先する研究課題**:
FONDUE は、早期学習（1〜5 エポック）における変数型判定の不安定さと品質上の問題を報告し、
Algorithm 3 を採用しなかった。本改訂では、**同一モデル・同一 checkpoint で変数型分類と標準 AU を比較し、
学習時間と読み取り統計量が安定性に与える影響を検証する**（E7a）。
安定した読み取りが、そのまま十分な再構成品質や正しい幾何学的次元を意味するとは仮定しない。
現原稿で観測されている小さいシード間 SD（$\le 0.5$）は「既存条件での観測」であり、共通条件で再検証する。

**比較実験の構成**:
FONDUE 本体、FONDUE-VAR、既存剪定法、従来の validation による選択に加え、
**FONDUE 系へ本手法と同じ品質条件 $Q$ を追加した対照**を含める（§5.3）。
これにより、一般的な品質確認の効果と、ID による候補優先および AU 診断の**追加価値**を区別する。

**NOLTA2025 との差分**は、会議論文からの追加事項、MAKE47 に既存の事項、MAKE49 で新たに検証する事項の
3 列に分けて説明する（§3.4）。

**採録に向けた中心的な打ち手**:

1. **FONDUE 付録 G との近接を自ら開示する。** 共通性を先に一文で述べ、その後に差を説明する（§3.2）。
2. **E7a / E7b で学習時間と統計量の効果を、探索経路の違いから切り離して検証する**（§5.5）。
3. **dSprites を共通ベンチマークとして追加する。** FONDUE の公表結果があるデータセットであり、
   共通環境で再実行した値を主比較に使う。公表値は参考欄にとどめる。
4. **品質条件 $Q$ と AU 診断を分離する。** +Q 対照で品質確認の効果を分離し、AU の診断上の増分価値は同一 checkpoint 群に対する単純診断との比較で別途測る（§4.4, §5.3, §5.5）。
5. **原稿内在の欠陥を先に直す。** test データの選択過程への混入、費用会計の破綻、Table 4 の誤った主張、
   V2 上限の判定力欠如、付録の誤った数学的主張（§2）。

**費用について**: FONDUE は早期探索に強みがあると予想されるが、共通条件での実測前に
「本手法は速さで劣る」とは確定させない。原著の A100 上の公表時間と本プロジェクトの GPU 上の
300 エポック学習を直接比較しない。

---

## 0. 2 つの方針文書の判定

**結論: `MAKE47_revision_strategy.md` を骨格として採用する。** 初版（v1）は査読コメントへの逐条対応としては妥当だが、原稿自体に内在する問題を検出できておらず、**そのまま実行すると査読者に指摘されていない致命的欠陥を残したまま再投稿することになる。**

### 0.1 strategy 版が優れており、必ず採用すべき点

| # | strategy 版の指摘 | 判定 | 重大度 |
|---|---|---|---|
| S1 | **FONDUE**（Bonheme & Grzes 2022）が直接競合として存在する | **正当。原論文を全文確認済み。** ただし内容は strategy 版の想定と一部異なり，**付録 G は追加検証課題を具体化する先行結果**として扱う（→ §3） | **致命的だが対処可能** |
| S2 | AU・TwoNN・MLE を test subset で計算しており、test が選択過程に混入している | **正当**（§5.1 に「AU, TwoNN, and MLE are computed on the test subset」と明記されている） | **致命的** |
| S3 | 「82% 削減」が必須の MSE 検証と両立しない。比較分母 11 点と VAE 実測 7 点が不一致 | **正当** | **重大** |
| S4 | 「MSE も探索窓を支持する」は数値的に強すぎる（VAE の MSE は m=20→64 で 29% 改善、AE は m=20→64 で 46% 改善。VAE 最良 probe は m=12 で推奨 m≥20 の外） | **正当** | **重大** |
| S5 | Table 4 の "elbow appears exactly at m = d_emb" は数値に反する（Swiss Roll は m=2→3 で MSE が約 48 倍改善） | **正当** | **重大** |
| S6 | 自然画像の Step 1（25.21±0.41）は独立参照 AE ではなく候補 Conv-VAE の潜在で得た値であり、手順の定義と矛盾 | **正当** | **重大** |
| S7 | **V2 上限は判定力を持たない**（AU ≤ m は定義から成立。検証点 m=2d̂ なら AU ≤ 2d̂ ≤ 3d̂ が自動的に成立） | **正当かつ鋭い** | **重大** |
| S8 | 事前指定した検証点（2d̂ 以上で最小のグリッド点）と、実際に判定している m（Conv-VAE の m=256）が一致していない | **正当** | 中 |
| S9 | 損失の reduction 規約（画素平均 vs 総和、KL の総和 vs 平均）が未記載で、FC と Conv の β 比較が成立しない可能性 | **正当**（要コード確認） | 中 |
| S10 | 人工データを平均 0・分散 1 に標準化しながら Sigmoid 出力としており、負値を再構成できない | **正当**（要コード確認） | 中 |
| S11 | 理論的主張に誤りが複数（特に付録の「decoder Jacobian の有意特異値数 = min(m, d_emb)」は成立しない） | **正当** | **重大** |
| S12 | R1-2 の "set to 30" の 30 は行番号であり、潜在次元 30 の指摘ではない | **正当**（査読文の引用に原稿の行番号が混入している） | 低 |

### 0.2 v1 版が補える点（統合して残す）

| # | v1 版の内容 | 判定 |
|---|---|---|
| V1 | Whitney 埋め込み定理により $d \le d_{\mathrm{emb}} \le 2d$ が言える | **背景引用としてのみ採用**。係数 2 の動機づけに使うが、正当化・新規命題にはしない（下記 0.3） |
| V2 | Costa & Hero (2004) の MST/測地エントロピーグラフによる ID 推定器を R1-6 の応答に使う | **採用**（strategy 版は PCA/Isomap/LLE を挙げるが、Costa–Hero のほうが「graph-based」という査読者の指摘に正確に対応する。両方入れる） |
| V3 | DOI 付与状況の実測（37 件中 12 件のみ）、Abstract 語数（243 語で MDPI 推奨 200 語超）、Introduction の前方参照数（19 か所）、"conditional" の出現数（24 回） | **採用**（作業量の見積もりに必要な実測値） |
| V4 | ファイル番号運用と投稿前チェックリスト | **採用**（ユーザ指示により NN 番号を MAKE に合わせる形へ改訂） |

### 0.3 Whitney 定理の位置づけ（背景としての利用に限定）

滑らかコンパクト $d$ 次元多様体について $d \le d_{\mathrm{emb}} \le 2d$ が成り立つこと（強 Whitney 埋め込み定理。
Klein bottle が $d_{\mathrm{emb}}=4=2d$ でタイトな例）は既知の結果であり、
「真の次元の 2 倍を初期容量の参考にする」という**幾何学的な動機づけ**として引用できる。

**ただし次は導けない。**

- 区間 $[d, 2d]$ が「唯一の自然な区間」であること。より広い区間も上界を含み、追加情報があれば狭い区間も使える。
- 推定値 $\hat d_{\mathrm{ID}}$ の誤差を考慮した探索窓の被覆保証。
- 有限幅のネットワークが学習でその埋め込みを実現する保証。
- ノイズ付きデータの再構成品質・ELBO・下流精度の保証。
- Gaussian prior と KL 正則化を持つ VAE の最適容量。
- AU と最小埋め込み次元の一致。
- V2 上限の判定力欠如（S7）や比較実験の不足（R2-3）の解消。

したがって現原稿 §3.2.2 の「係数 2 は実験で有効だった既定値」という記述は、
**「既知の埋め込み上界に基づく動機を持つが、本研究の推定 ID と学習された VAE に同じ保証を与えるものではないため、
検証対象の既定値として扱い、感度分析で評価する」に改める。** 経緯を事後的に「定理から導出して設定した」と書き換えない。
「新規命題」として貢献に数えず、引用を伴う背景説明にとどめる（§4.6）。

### 0.4 v1 版の誤り・過剰（破棄する）

- 「Whitney 命題を追加すれば『場当たり的ヒューリスティック』という批判の中核が崩せる」→ **過大評価**。埋め込み上界は係数 2 にも実用的な品質保証を与えず、幾何学的動機を補うにとどまる。
- 「V1 閾値 0.9 と V2 係数 3 の感度区間を本文に上げれば R3-1 に応えられる」→ **不十分**。同一データ内での判定不変性は根拠にならない（S7 により V2 上限はそもそも自明に成立しているので、感度が小さいのは当然）。開発条件で固定し未使用条件で検証する設計が必要。
- 「ベースラインとして PCA/Minka/Bayesian PCA/ARD-VAE/事後枝刈り/MSE/ELBO の 7 種」→ **線形手法に偏っている**。Reviewer 2 の言う established methods には FONDUE と GECO/L0 系が必要。線形手法だけでは応答として弱い。
- 「dSprites と KMNIST を『新規データセット 2 つ追加』と書く」→ 区別が必要。**dSprites / KMNIST は MAKE47 に対する新規追加**であり、**CIFAR-10 / SVHN は既存条件での全手順の再評価**である。両者を同じ「追加」に括らない。
- 「Whitney 命題で係数 2 の恣意性が解消する」「$[d,2d]$ は唯一の自然な区間」→ **導けない**（§0.3）。背景引用にとどめる。
- 「FONDUE 付録 G は本論文への反証材料」「シード間分散が消える」→ **E7 未実施の段階で書いてよい結論ではない**。仮説・既存観測・追加検証予定を分けて書く（§3.3）。

---

## 1. 総合判断と中心課題の再設定

### 1.1 判定の読み

| 評価項目 | R1 | R2 | R3 |
|---|---|---|---|
| English | Fine | Can | Fine |
| Introduction | **Must** | **Must** | Can |
| Research design | Can | **Must** | Can |
| Methods | Can | **Must** | **Must** |
| Results | Can | **Must** | Can |
| Conclusions | Yes | **Must** | **Must** |
| Figures/Tables | Yes | **Must** | Can |

Reviewer 2 は論文の貢献自体を否定しており、多数決的に「2 名は肯定的だから軽微修正でよい」とは判断できない。**文章修正だけでは不十分で、実質的な追加検証が必要。**

### 1.2 中心課題の再設定

現状の「内在次元から適切な次元を決め、AU で確認する」という枠組みを、次の検証可能な問いに絞り直す。

> 内在次元と潜在変数の活性に基づく読み取りは、**どの学習条件で安定し、その安定性は再構成品質とどう関係するか**。
> また、同じ品質確認を備えた既存の次元選択法に対して、**ID による候補の優先づけと AU 診断は何を追加できるか**。

この問いなら、新アーキテクチャ・新目的関数がないこと自体は欠陥にならない。ただし**「既存要素を組み合わせた」という説明だけでは独立した貢献の証明にならない**ため、既存法に対する実測の優位性（または明確な付加価値）が必須になる。

FONDUE の確認を踏まえ、**前段（安定性と品質の関係）を主、後段（既存法に対する追加価値）を従とする**。
費用削減は FONDUE が早期探索で既に扱っており、共通条件での実測前に優劣を予断しない。
前段は FONDUE 著者が早期学習の条件で否定的結論を出したまま残している課題であり、**本改訂で優先する研究課題**とする。
学習過程と VAE 表現の幾何に関する研究は他にもある（Camboulin ら 2024 など）ため、「学習時間を調べること」だけを
未開拓とせず、**同一モデルでの統計量比較・品質との乖離・診断の増分価値の組合せ**について差分を確認する。

改訂後も「真の内在次元」「最小埋め込み次元」「最適な潜在次元」「AU」は厳密に区別する。圧縮・再構成・生成・分類で最適化対象が異なるため、すべてに共通する唯一の正しい次元を求める研究としては説明しない。

### 1.3 優先度

| 優先度 | 課題 | 到達点 |
|---|---|---|
| **P0** | 新規性の再整理 | NOLTA2025 との差分に加え、**FONDUE との差分を明示** |
| **P0** | test 分離 | train/validation/test を分離し、選択に test を使わない |
| **P0** | 比較実験 | validation MSE/ELBO/下流、**FONDUE**、既存剪定法と比較 |
| **P0** | AU 判定と性能保証の分離 | AU 条件適合と、品質を保った次元選択の成功を分ける |
| **P0** | 原稿内在の不整合修正 | Table 4、探索費用、自然画像 Step 1、付録の数学的誤り |
| **P1** | 追加データ・参照依存性 | 自然画像で全手順を評価。独立参照モデルで検証 |
| **P1** | 閾値・再現性 | 開発条件で閾値を固定し未使用条件で検証。主要結果は 5 seeds |
| **P2** | 可読性・体裁 | Abstract、Introduction、Algorithm、図表、引用、実行環境 |

---

## 2. 原稿内在の問題（査読者に指摘される前に直す）

これらは査読コメントと原稿を照合する過程で発見されたもので、**形式修正より先に処理する**。

### 2.1 test データが選択過程に混入している【P0】

**該当**: p.5 209–211 行、p.12–13 438–448 行、p.19–20 §5.4。§5.1 に「AU, TwoNN, and MLE are computed on the test subset」と明記。

下流 test accuracy の最良点を見て探索窓の有効性を説明している点も、未知データでの選択性能とは区別が必要。

**対応**:
- 参照 AE・候補モデルの学習は train。
- ID 推定・設計・checkpoint 選択・AU 診断・再探索は train 内の推定用データまたは validation。
- 最終評価のみ test。
- 旧実験は探索的結果として残してよいが、**主たる性能・費用比較は分離した設計で再実行する**。
- 既に確認済みのデータを、あとから完全に未見だったと説明しない。

### 2.2 費用会計の破綻【P0】

**該当**: p.8 307–316 行、p.19 655–669 行、p.20 Table 10。

- VAE の最少費用を「参照 AE 3 回 + m=20 の VAE 3 seeds = 6 回」としているが、必須の MSE 確認は「m をさらに増やしても MSE が改善しないか」を見るため、別の m の VAE が最低 1 点必要。参照 AE の MSE は別モデルなので代用できない。
- 比較分母は 11 点 × 3 seeds = 33 回だが、Table 11 の VAE は 7 点しかない。

**対応**:
- 全探索の候補集合を統一する。
- ID 推定・参照学習・追加検証点・β 調整・失敗からの再探索・最終学習をすべて計上する。
- **学習回数と総実時間を併記する。**
- 現在の 64% / 73% / 82% は、同条件で再計算されるまで Abstract・Conclusions の主要成果から外す。

### 2.3 「MSE も探索窓を支持する」は現数値では成立しない【P0】

**該当**: p.16–17 590–599 行、p.20 Table 11、p.24 793–798 行。

| | m=20 | m=64 | 改善 |
|---|---|---|---|
| VAE MSE | 0.0126 | 0.0089 | 約 29% |
| AE MSE | 0.0114 | 0.0062 | 約 46% |

さらに VAE の最良平均 probe 精度は **m=12**（89.1%）で、VAE の推奨候補 $m \ge 2\hat d_{\mathrm{ID}} = 20$ の外にある。

**対応**:
- 「最良分類精度の点を含んだ」と「品質を維持した最小次元を選べた」を明確に分ける。
- **MSE の許容劣化量を先に定義する**（→ §4.2 の品質条件 $Q$）。
- 誤差尺度によって結論が変わることを示す。
- AE の探索窓に最良点があることと、VAE の選択規則が最良点を選ぶことを混同しない。

### 2.4 Table 4 は書式だけでなく主張も誤り【P0】

**該当**: p.13 473–477 行、Table 4（`tab:e1`）。

Swiss Roll は m=2 で MSE=0.029、m=3 で 0.0006。**約 48 倍・98% の改善がまだある。**「m=2 で急速な MSE 改善が止まる」「elbow が最小埋め込み次元に exactly 一致する」と断言する根拠にならない。

**対応**:
- `\leftarrow elbow` の矢印と "exactly" を削除する。
- 全数値を同一書式に統一する（例: `0.0006 ± 0.0002`、または列見出しに $\times 10^{-3}$ を括り出し、セル内は素の小数のみ・有効数字 3 桁）。
- 対数 MSE 曲線と再構成画像を示す。
- **「m=2 で表現可能である」という位相的議論と、「この学習条件で低い MSE が得られた」という実験結果を分離する。**
- Reviewer 2 が述べる「括弧の破綻」は PDF 表示上は特定できなかったが、数値形式の混在は事実。応酬せず、LaTeX ソースと再生成 PDF の両方で再点検し、**全 14 表の整形として対応する**。

### 2.5 自然画像の Step 1 が手順定義と矛盾【P0】

**該当**: p.7 238–240 行、p.22–23 §5.6.2、Table 13。

手順は「候補 VAE と独立の参照 AE を使う」と定義しているが、自然画像の Step 1 の根拠 25.21±0.41 は **Table 13 の候補 Conv-VAE（m=64）の潜在表現**の値である。

**対応**:
- 自然画像にも独立した参照モデルを用意する。
- 旧 Table 13 は候補 VAE の診断結果として扱い、Step 1 の根拠表と分ける。
- 独立参照を用いないなら、**自然画像で 3 段階を適用済みとは記述しない。**

### 2.6 V2 上限が判定力を持たない【P0】

**該当**: p.7–8 271–279 行。

$\mathrm{AU} \le m$ は定義から成立する。検証点が $m = 2\hat d$ なら $\mathrm{AU} \le 2\hat d \le 3\hat d$ となり、**V2 の上限は必ず満たされる**。より一般に $m \le 3\hat d$ の検証点では上側条件に識別力がない。

MNIST（$m=20$, $\hat d = 10$）では、V1 と V2 は実質的に $10 \le \overline{\mathrm{AU}} \le 18$ を確認する規則にすぎない。したがって「上限係数 3 への感度が小さい」という観測は、係数 3 の妥当性の根拠にならない。

**対応（重要な設計変更）**:
- **V1/V2 を「普遍的な合格条件」から「補助診断」へ格下げする。**
- 残す場合も、各検証点でどの条件が実際に制約として働くかを明示する。
- 平均値だけでなく各 seed の判定を掲載する。
- 選択の主判定は、§4.2 の再構成品質条件 $Q$ に移す。

### 2.7 「失敗を検出した」と「原因を特定した」の混同【P0】

**該当**: p.8–9 Table 2、p.18–19 §5.3.6、p.21 §5.6.1。

Fashion-MNIST で AU < $\hat d$ となったことは、定めた範囲への不適合を示すだけ。参照 ID の過大推定、許容される情報圧縮、AU 閾値の作用、学習条件などを除外していないため、**過剰剪定を実証したとはいえない**。AU = m も、過大な潜在次元が存在しない可能性を含むため直ちに失敗とはいえない。

**対応**:
- 失敗モード表の "diagnostic interpretation" を「**可能な説明と追加確認**」に変更する。
- β 低減や候補次元増大によって独立した品質指標が回復するかを示す（介入実験）。
- AU が規則に入ること自体を、診断が正しかった証拠にしない。

### 2.8 事前指定した検証点と実際の判定 m が不一致【P1】

**該当**: p.7 271–275 行、p.21 692 行、p.22 722–727 行、p.23 779–783 行。

規則は「$2\hat d$ 以上で最小のグリッド点」を検証点とするが、Conv-VAE ではグリッドに m=32 があるにもかかわらず主な判定を m=256 で説明している。

**対応**: 事前指定した検証点の結果を最初に示し、その後の点は拡張探索として順序と費用を記録する。元の点での不適合と、追加探索後の結果を混ぜない。

### 2.9 損失関数の実装定義が不足【P1・要コード確認】

**該当**: §3.1、§5.1、§5.6、Appendix A。

画素平均 MSE + 潜在次元総和 KL と、両者総和では、**同じ β でも圧縮圧力が異なる**。FC と Conv でこの差があると、アーキテクチャの効果と混同する。

**対応**: 実装と一致する損失式を追加し、batch・pixel・latent の各軸での reduction、KL annealing、likelihood、$\tau^2$ を明記する。β を比較する前にこの整合性を確認する。現時点で実装ミスと断定するものではない。

### 2.10 人工データの標準化と Sigmoid 出力の整合性【P1・要コード確認】

**該当**: p.12 439–442 行、p.13 Table 4。

共通設定に Sigmoid 出力、人工データは平均 0・分散 1 に標準化とある。**負値を含むデータを Sigmoid 出力で復元できない。** Table 4 の極めて小さい MSE との整合性を確認する必要がある。

**対応**: 人工データだけ線形出力を使ったのか、別の正規化・逆変換があるのかをコードで確かめ、設定表を分ける。本文の省略なら説明修正、実装の不整合なら**該当実験を再実行する**。

### 2.11 理論的主張の誤り（削除・修正リスト）【P0】

注意書きを足すだけでは誤った式は直らない。以下は削除または書き換える。

| 箇所 | 問題 | 修正方針 |
|---|---|---|
| p.11 393–395 行, Remark 2 | $\mathrm{Var}[\mu_j] > 0$ と、$\delta = 0.01$ を超える AU 判定を同一視 | 正の rate と正の分散の対応は基底等の仮定を明示した理想設定に限定。**有限 $\delta$ の AU との完全一致を削除** |
| p.11 387–389 行 | $\beta = 0$ で全次元が使用されると一般化 | AE の有効次元はデータ・冗長性・最適化にも依存する。「**KL による剪定は課さない**」に限定 |
| p.12 402–408 行, p.28 968–981 行 | 集約事後分布の KL が非負であることから活性次元数の変化方向を推論 | 目的関数の追加項の非負性だけでは最適解の活性数の順序は決まらない。**方向の断言を削除** |
| p.21 708–714 行 | 観測 AU に合わせて逆算した $\tau^2_{\mathrm{eff}}$ で同じ観測を説明 | **循環論法。原則削除**、または図解用の事後的解釈に明示的に限定 |
| p.12 414–421 行, p.28 986–994 行 | 高い Trustworthiness から最近傍 2 点の順位保持や TwoNN 誤差の上界を導く | 平均的な順位指標だけではその保証は得られない。**未証明の $C(\epsilon, \delta_T)$ による上界を削除**し、実測の近傍保存率に置換 |
| p.28 1001–1003 行 | decoder Jacobian の有意特異値数 $= \min(m, d_{\mathrm{emb}})$ | **削除を強く推奨。** 大域的な最小埋め込み次元から局所 Jacobian の rank は決まらない。データ多様体から外れる潜在方向への decoder の振る舞いは自由であり、"significant" の閾値も未定義 |
| Appendix B.2 | 検出誤差上界 | 採用した有限差分の検出規則に対して追加仮定と証明が必要。**未証明の上界を削除**し、観測されたグリッド依存性だけを残す |

最後の Jacobian の点について、「滑らかで完全再構成なら接空間上で $Dg \cdot Df$ が恒等写像になる」という関係までなら説明できる。そこまでに限定する。

### 2.12 その他の整合性確認【P1–P2】

- p.7 234 行: TwoNN と MLE はともに近傍距離に基づくため、「**異なる原理の独立な確認**」と強調しすぎない。異なる統計を使う DANCo/ESS との役割を分ける。
- p.16 Table 7: subsampling 区間を「真の ID の 95% 信頼区間」と受け取られないよう「**固定表現に条件づけた subsampling 95% interval**」と統一する。
- p.7 236 行ほか: "pre-registered" は実際の事前登録記録がある場合だけ使用する。なければ「いつ規則を定めたか」を明記する。**今回から固定した手順を、過去実験にも事前適用したように書かない。**
- p.21 Figure 4 右: 表示に reference AE と β=4 が併記されている。対象が AE か VAE か元ログと照合する。
- p.19 Table 9, p.20 Table 11: multi-seed という表題に対し MSE に標準偏差がない。平均・単一 seed・別実験のいずれかを特定する。
- p.31 Appendix D: 「β=8 で過剰正則化の兆候」と「β∈[2,8] で stable」は、何が安定かを指標別に分ける。**50 epochs の予備実験だけで 300 epochs の主条件を正当化しない。**

---

## 3. 新規性の再整理（R2-1 の本体）— 一次資料確認済み

2026-09-12 に NOLTA2025 原稿（`nolta2025_eng (4).pdf`, 5 頁）と FONDUE 原論文
（`1ce549c4-6f05-4259-9fcc-791db9746a28.pdf`, arXiv:2209.12806v1, 17 頁）を全文確認した。
**結論を先に述べる。FONDUE との共通性は strategy 版が懸念したとおり大きい。改訂では共通性と追加検証課題を整理し、
貢献の大きさは比較結果で判断する。付録 G は本論文の主張を支える材料ではなく、検証すべき課題を具体化する先行結果である。**

### 3.1 FONDUE の実際の中身

**Bonheme, L.; Grzes, M.** *FONDUE: an algorithm to find the optimal dimensionality of the latent
representations of variational autoencoders.* arXiv:2209.12806v1, 26 Sep 2022, 17 頁。
（arXiv のみ。査読付き会議・論文誌への採録は確認できていない。）

**FONDUE 本体（Algorithm 1）**: 平均表現の ID（$\mathrm{IDE}_\mu$）とサンプル表現の ID
（$\mathrm{IDE}_z$）が乖離し始める点を探す。$\mathrm{IDE}_z - \mathrm{IDE}_\mu \le \mathrm{threshold}$
を満たす最大の潜在次元数を、指数探索＋二分探索で求める。閾値は データ ID の 20%。
初期値は $p \leftarrow \mathrm{IDE}_{\mathrm{data}}$（**2 倍ではない**）。
少数エポック（1〜9）で学習して判定するため高速（7〜50 分）。
データセットは **Symsol・dSprites・Celeba** の 3 種。MNIST は扱っていない。

**重要**: strategy 版が「付録 F の Algorithm 3」としていたものは、正しくは **付録 G の Algorithm 3**
（付録 F は passive variable と posterior collapse の説明）。内容は strategy 版の推測どおりだった。

### 3.2 付録 G / Algorithm 3（FONDUE-VAR）と本論文の関係

```text
Algorithm 3  FONDUE with variable types            ← FONDUE 論文 付録 G, pp.16–17
procedure FONDUE-VAR(data_ide, epochs, keep_mixed)
    l ← 2 * data_ide                               ← データ ID の 2 倍から開始
    n ← −1
    while n < 0 do
        vae ← train_VAE(dim = l, n_epochs = epochs)
        av, mv, pv ← variable_types(vae)           ← active / mixed / passive の個数
        if pv > 0 and keep_mixed then   n ← av + mv
        else if (mv > 0 or pv > 0) and not keep_mixed then   n ← av
        else                            l ← l * 2  ← 打ち切りが起きなければ 2 倍して再試行
        end if
    end while
    return n
```

**共通性（Related Work で最初に一文で明示する）**:
ID に基づく初期容量の設定と、潜在変数の活性状態に基づく読み取りという 2 つの着想を、本研究と FONDUE-VAR は共有する。

**差（共通性を述べた後に説明する）**: 重要な着想を共有するが、同一手順ではない。

| 比較軸 | FONDUE-VAR / Algorithm 3 | MAKE47 |
|---|---|---|
| 初期容量 | データ ID の 2 倍から開始 | VAE に余裕ある容量を与え、$2\times$ 推定 ID 以上の候補を用いる |
| 読み取り | **分散層の出力**を用いた active / mixed / passive の分類（U12 の定義） | **posterior mean のデータ間分散**を閾値 $\delta$ で判定する AU |
| 停止判定 | `keep_mixed` に応じ、passive または mixed が少なくとも 1 つ出たか | V1（$\mathrm{AU}/m \le 0.9$）と V2、および MSE の確認 |
| 追加操作 | 条件を満たさなければ容量を倍増 | 失敗モード表（β・decoder・容量等の追加確認）。固定した倍増探索ではない |
| 出力 | active 数（または active + mixed 数）を**推奨潜在次元として返す** | 設定容量 $m$ と学習後の AU を**区別して扱う**。AU を次元推定量としない |

「非活性が 1 つある」と「$\mathrm{AU}/m \le 0.9$」は同じ条件ではない。FONDUE-VAR が返した次元で作るモデルと、
大きい $m$ を保持したまま AU を報告するモデルは同じものではない。
過度な独自性の主張も、アルゴリズム全体が既に同一であるとの断定も避ける。

**公開コードで確認した実装上の事実**（`vae_ld/learning_dynamics/fondue.py`, `variable_filter.py`; §13 U12–U16）:
変数型は分散層出力 $\sigma_k^2(x)$ のデータ間分散と平均で分類される。閾値 $t$ はコードでは絶対閾値として設定から受け取り
（本文の 20% 規則は $0.2\times$ データ ID を渡せば実装できる。既定値 1 は論文実験時の値を確定する資料ではない）、
FONDUE-VAR のコード経路には分散の二重指数化がある（合成入力で確認）。再実装では「記述仕様」「固定 commit のコード経路」
「公表値」を分けて扱う（U13, U14）。

### 3.3 FONDUE 付録 G の読み方と、本研究の位置づけ（仮説として）

付録 G の見出しは "Why not use variable type instead of IDE for FONDUE?" である。
FONDUE の著者は FONDUE-VAR を **1・2・5 エポック**で評価し、次を報告して採用しなかった。

- 変数型の識別は学習初期に分散が大きく、同等の計算時間では Algorithm 1 より信頼性が低い。
- Table 7 の予測次元数は Table 1（Algorithm 1）と比べて最適から外れる（Symsol で過大、Celeba で過小）。
- Algorithm 3 は分散表現のみに依存するため、学習初期の安定性が落ちる。
- 学習初期には mixed と passive が正しく識別されない。

**FONDUE-VAR の公表値（Table 7、5 seeds）**:

| データ | 1 ep | 2 ep | 5 ep | Algorithm 1（Table 1） |
|---|---|---|---|---|
| Symsol | 14 ± 1.2 | 17 ± 1.7 | 15 ± 1.1 | 11 ± 0 |
| dSprites | 9.2 ± 1.3 | 8.8 ± 1.2 | 9.6 ± 0.5 | 12.2 ± 0.4 |
| Celeba | 38.6 ± 2.6 | 38.6 ± 0.4 | 41.2 ± 0.4 | 50.2 ± 0.9 |

**この付録は「十分学習した標準 AU は必ず不安定である」「活性に基づく診断はすべて無効である」と主張したものではない。**
逆に、長時間学習した別の統計量が安定しても、それは早期学習での観測を否定したことにはならない。
したがって付録 G は「反証すべき否定的結果」ではなく、**早期学習での知見を、学習時間と読み取り統計量の軸で拡張して
検証するための、追加研究課題を具体化できる先行結果**として扱う。

**検証する仮説（E7 で確認するまで結論として書かない）**:

| # | 仮説 | 既存の観測 | 追加検証 |
|---|---|---|---|
| H1 | 学習時間を延ばすと、活性状態の読み取りのシード間ばらつきは小さくなる | MAKE47 の 300 エポック条件で AU の SD $\le 0.5$（**小さい標本 SD の観測であり、分散ゼロではない**） | E7a: 同一 checkpoint 系列で 1/2/5/50/300 エポックの SD を記録 |
| H2 | 平均表現の分散に基づく AU は、分散表現に基づく変数型分類より安定である | FONDUE 著者が分散表現依存を不安定の一因として名指し | E7a: 同一モデルから 2 系統を計算して比較 |
| H3 | 安定した読み取りでも、再構成品質条件 $Q$ を満たさない場合がある | トーラスで AU $=2$、$m=3,4$ で MSE が高い（**ただし AU と変数型は別量で、位相的下界は有限標本の平均 MSE に直接の下限を与えない**） | E7a のトーラス補助条件、E7b で FONDUE-VAR が返した次元の最終モデルを共通 $Q$ で評価 |
| H4 | 適用限界（Conv-VAE・自然画像）は FONDUE 系にも共通する | MAKE47 の Conv-VAE・自然画像節（投稿版の節番号は原稿で確認して記載） | E5 で FONDUE 系を同条件で実行。**失敗は共通 $Q$ と予算で定義する**。FONDUE に本研究固有の V1/V2 を適用してその不適合を FONDUE の失敗と数えない。「自然画像・Conv 全般に共通の失敗がある」という表現にも広げない |

**「300 エポック」を「収束」と同義にしない。** 停止・plateau 判定を事前に定義し、最大予算で未収束ならそう記す。
常に AU $=0$ または AU $=m$ となる場合にも SD は小さくなるため、**安定性だけを成功基準にしない**。

**トーラスの既存結果の扱い**:
AU $=2 < d_{\mathrm{emb}} = 3$ は、AU をそのまま幾何学的次元と読むことへの疑問を示す材料になる。
しかし「FONDUE-VAR が誤った答を返すことを既に示した」とは言えない。理由は
(i) 変数型分類と AU は同じ量ではない、(ii) 有限閾値以下の posterior-mean 分散は座標が無情報であることを意味しない、
(iii) 大きい $m$ のモデルの AU が 2 であることと、$m=2$ に設定したモデルの性能は別である、
(iv) 位相的な完全復元の制約は有限標本・許容誤差付きの平均 MSE に直接の下限を与えない、
(v) 合成実験には §2.10 の正規化・出力層の確認が残る。
**実験目的を「誤答することを示す」から「選択されたモデルが共通 $Q$ を満たすか確認する」へ変更する。**
共通条件で FONDUE、FONDUE-VAR、提案法を実行し、返された次元での最終モデルを同じ評価方式で比較する。
必要なら座標無効化の介入も行う。$Q$ を満たさない結果が得られた場合に初めて、その条件・設定に限定した失敗例として示す。

**FONDUE 論文の品質評価について**: 本文 §4.3・Figure 8・9 は再構成損失と正則化の関係を評価し、
Celeba では選択次元を超えて再構成がなお改善することとのトレードオフも議論している。
正確な差は「**FONDUE-VAR の Algorithm 3 の停止・出力規則に、提案する $Q$ と同型の明示的な品質許容条件が入っていない**」
ことであり、FONDUE 論文全体が品質を検討していないという意味に広げない。

### 3.4 NOLTA2025 との差分（3 列で整理）

**Obata, C.; Jin'no, K.** *Analysis of the Manifold Hypothesis and Latent Space Capacity in
Variational Autoencoders.* NOLTA2025, 5 頁（添付版。**公刊最終版との版の一致は別途確認する**。
MAKE47 の参考文献 9 は異なる題名表記であり、Reviewer 2 は「4 ページ」と書いている。ページ数を反論材料にしない）。

添付版の内容: MNIST を 32×32 にリサイズ（$N=1024$）、**クラス別 VAE**（数字 0–9 ごとに独立学習）、
6 アーキテクチャ（FC 4 + CNN 2）× 10 クラス × 4 seeds = 240 モデル。
入力 ID は TwoNN（クラス別 13.01–16.68）と PCA broken-stick（22–50）。
ボトルネックは $D_0$（予備実験で自己収束した潜在 ID）、$D_1$（入力 ID）、$D_2=(D_1+N)/2$ の 3 水準。
結論として $D \approx M$ を推奨。**AU は扱っていない。**

**差分表は次の 3 列を必ず分ける。** AU・Whitney・線形 Gaussian の解釈は NOLTA2025 になかったからといって
分野全体で新規になるわけではない。今回追加する比較結果を MAKE47 に既に存在した貢献と混ぜない。

| 論点 | NOLTA2025（添付版） | MAKE47 に既存 | MAKE49 で追加・修正予定 |
|---|---|---|---|
| ID と容量の関係 | 中心課題 | 引き継ぐ | 既出として明示 |
| 複数アーキテクチャ・seed | 既に実施（6 種、4 seeds） | FC / Conv、主に 3 seeds | 新規性そのものにせず、再現性向上として説明（主要比較 5 seeds） |
| AU と品質 | AU による評価は扱わない | AU、V1/V2、MSE 照合 | 学習時間・統計量の統制比較（E7a）と診断の増分価値（+Q 対照） |
| 独立した選択法との比較 | 比較体系はない | 主に同じモデルのグリッド評価 | FONDUE、FONDUE-VAR、+Q 対照、剪定法との比較 |
| 理論 | 理論枠組みはない | 位相的下界・線形代理の議論 | 誤りの是正（§2.11）と既知結果の限定的利用（§4.6） |
| 自然画像 | 今後の課題として言及 | 探索的結果が存在 | 独立参照を含む全手順、品質・費用の評価（E5） |
| データ | MNIST のみ | MNIST, Fashion-MNIST, 合成 2 種, CIFAR-10, SVHN | dSprites（新規追加）、CIFAR-10/SVHN は既存条件の再評価 |

**返答書では「会議論文からの追加」と「既存研究に対する今回の実証的貢献」を別段落にする。**
図表の重複は 0 件。共通するのは「入力の ID がボトルネック設計の手がかりになる」という着想と、
「潜在 ID は入力 ID より小さくなる」という観察であり、この 2 点を既出として明示的に認める。

### 3.5 自著間の数値・主張の違い（説明しすぎない）

査読者が NOLTA2025 と本論文を並べて読むと気づく可能性がある。ただし、**条件の異なる観測を無理に整合させない。**

| # | 違い | 扱い |
|---|---|---|
| N1 | NOLTA の MNIST 入力 ID は TwoNN で 13.01–16.68（クラス別・32×32・入力空間直接）、本論文は約 10（全クラス・28×28・参照 AE 潜在経由） | 解像度・クラスのまとめ方・参照表現・推定条件がすべて異なるため**直接比較できない**。「推定経路による差の追加データ点」として使うと交絡が残る。本文では「両研究の推定値は条件が異なるため直接比較できない。今回、同一データ条件で参照表現への依存性を検証する」と書く。因果的に説明したい場合は、同一標本で入力空間と参照潜在を比較し、代表条件で解像度・クラスの扱いを一要因ずつ変える（E3） |
| N2 | NOLTA は $D \approx M$ を推奨、本論文は VAE で $m \ge 2\hat d$ を初期候補とする | 「どの設定容量を試すか」と「学習後に何が使われるか」という異なる問題を含む。MNIST に特定の位相的障害があることを今回の資料だけで実証したわけではない。本文では「先行研究では入力 ID 近傍を容量設定の手がかりとした。本研究では設定容量と学習後の有効な表現を区別し、$2\times$ 推定 ID を初期候補の一つとして評価する。最終選択は品質条件で行い、この係数の有用性は感度分析で検証する」と書く。**「理由は位相的障害である」と断定しない**。位相は動機の一つとして整理する |
| N3 | NOLTA は過大な $D_2$ で MSE が悪化と報告。MAKE47 の MNIST AE は $m=20$ の 0.0114 から $m=256$ の 0.0057 へ**改善**している | **「過大容量は有害という結論が両者で一致」「$m=256$ は追試」とは書かない。** 容量水準・データ・アーキテクチャ・学習設定が違う**条件の異なる観測**として整理する。同条件の再実験なしに「追試」と呼ばない |

さらに NOLTA Table 7 では、FC の model1–3 で $D_1$（入力 ID）より $D_0$（自己収束）の MSE が小さい
（0.0140 vs 0.0149、0.0165 vs 0.0190、0.0157 vs 0.0162）。
差分表には「NOLTA は $D \approx M$ を推奨している」と著者の結論を記述してよいが、
**「入力 ID 近傍が一貫して MSE 最小であることを実証済み」とは扱わない。**

### 3.6 Related Work と比較実験に加える研究

#### 直接競合（比較実験の対象）

| 研究 | 関係 | 対応 |
|---|---|---|
| **FONDUE Algorithm 1** (Bonheme & Grzes 2022) | 平均/サンプル表現の ID 乖離で次元決定。少数エポックで高速 | **最優先の直接比較（B6a）** |
| **FONDUE-VAR / Algorithm 3**（同 付録 G） | 変数の活性状態で次元決定。著者自身が不安定として不採用 | **最重要の比較（B6b）。** 本論文の位置づけの基準点 |
| **GECO + L0**（De Boom et al., arXiv:2003.10901） | 再構成制約下で学習中に潜在次元を削減 | 既存剪定法として優先（B7） |
| **ARD-VAE**（Saha, Joshi & Whitaker, arXiv:2501.10901, 2025） | 階層事前分布で潜在軸の関連性を推定 | 余力があれば追加（B8） |
| **RENs**（Iyer, Bhalodia & Elhabian, arXiv:2205.13061） | ARD 事前分布を用いる VAE 系次元適応 | Related Work に追加 |
| Bayesian PCA / ARD-PCA（Bishop 1999、既引用）、Minka（NIPS 2000） | 線形での次元決定 | 軽量な対照（B9） |

#### 理論・解釈の補強（引用のみ）

| 研究 | なぜ必要か |
|---|---|
| **Rolinek, Zietlow & Martius**, *Variational Autoencoders Pursue PCA Directions (by Accident)*, CVPR 2019 | 線形ガウス代理の**限定した解釈を補う引用**（§4.6 と同じ位置づけ）。引用だけで非線形 VAE への water-filling 条件や有限 AU 閾値の正当性が成立するわけではない。現状未引用 |
| **Dai, Wang & Wipf**, *The Usual Suspects? Reassessing Blame for VAE Posterior Collapse*, ICML 2020 | posterior collapse の原因論。失敗モード表の「β が小さい」という断定を弱める根拠 |
| **Dai & Wipf**, *Diagnosing and Enhancing VAE Models*, ICLR 2019 | 同上。NOLTA2025 では引用済みだが MAKE47 で落ちている |
| **Lucas, Tucker, Grosse & Norouzi**, *Understanding Posterior Collapse in Generative Latent Variable Models*, DGS@ICLR 2019 Workshop（OpenReview r1xaVLUYuE） | 同上。**NOLTA2025 [14] は "K. Xu et al." と誤記**。正しい著者で引用する |
| **Bonheme & Grzes**, *Be More Active! Understanding the Differences between Mean and Sampled Representations of VAEs*, arXiv:2109.12679 (2021); **JMLR 2023 掲載** | FONDUE の変数型分類の定義元。H2 の差分を述べるのに必須。JMLR 版を引用する |
| **Camboulin, Doimo & Glielmo**, *Understanding Variational Autoencoders with Intrinsic Dimension and Information Imbalance*, arXiv:2411.01978, 2024 | ID と VAE を直接結ぶ最近の研究（書誌確認済み）。回収候補として適切。「未引用が査読判断の理由」とは断定しない |
| **Loaiza-Ganem et al.**, *Diagnosing and Fixing Manifold Overfitting in Deep Generative Models*, TMLR 2022 | 多様体過学習。本論文の「過大な $m$ でノイズを学習する」議論の典拠（書誌は原典で再確認） |
| **Tenenbaum, de Silva & Langford**, Isomap, Science 2000 / **Jolliffe**, PCA | R1-6（古典的次元削減手法）への背景補強 |
| **Costa & Hero**, IEEE TSP 2004, 52(8), 2210–2221 | R1-6 への応答として graph-based の ID 推定器の例に引用する。推定器の実装追加は必須化せず、E3 の結果に具体的な不確実性が残る場合の任意追加とする（§6.1 R1-6 と同じ） |
| Gałka, Karczmarek & Tokovarov, IEEE Access 2022 | R1 指定文献。**主題は MST に基づく異常検知**。背景として限定的に引用し、次元削減法として誤って紹介しない |

**NOLTA2025 で引用していた文献のうち MAKE47 で落ちているものが複数ある**（Dai & Wipf 2019、Lucas et al. 2019 workshop、
Loaiza-Ganem et al. 2022、Camboulin et al. 2024、Tenenbaum 2000、Burgess et al. 2018、Chen et al. 2018）。
回収候補として検討するが、**NOLTA の引用を無条件に復活させない**。NOLTA2025 の参考文献リスト自体に
少なくとも 3 件の書誌誤りを確認した。

| NOLTA の文献 | 記載 | 確認結果 |
|---|---|---|
| [14] | K. Xu et al., Understanding posterior collapse..., ICLR 2019 | 正しい著者は **Lucas, Tucker, Grosse, Norouzi**、DGS@ICLR 2019 Workshop |
| [17] | A. Lucas et al., Analysing phase transitions in VAEs, arXiv:2106.05464 | この番号は **作用素環論の別論文**（Okayasu）。意図した原典は検索でも特定できず。**追加しない** |
| [18] | C. Zhao et al., On learning intrinsic dimension of data representations, arXiv:2203.05796 | この番号は **CLIP のベンチマーク論文**。意図した原典は検索でも特定できず。**追加しない** |

DOI 書式を整える前に、題名・著者・年・掲載先・DOI/URL の対応を全件照合する。「一次確認済み」の表示は
実際に確認した資料に限定する。特定できなかった文献は「今回の検索で書誌を確認できなかった」と扱い、存在しないと断定しない。

### 3.7 Reviewer 2 への基本姿勢

「新アーキテクチャがない研究には価値がない」という一般論には同意する必要はない。
一方、**現原稿で比較と独立検証が不足していること、および FONDUE への言及が皆無であることは認めるべきである。**

返答の順序:

1. FONDUE（とくに付録 G の Algorithm 3）と本手法の VAE 経路の**共通性を自ら明示**し、そのうえで差を述べる。
2. FONDUE の著者が Algorithm 3 を**早期学習条件で退けた**という事実を引用し、本改訂がそれを学習時間と統計量の軸で拡張検証したことを述べる。
3. E7a / E7b の結果を、仮説 H1–H4 のどれが支持され、どれが支持されなかったかの形で示す。
4. NOLTA2025 との差分を 3 列（会議版・MAKE47 既存・MAKE49 追加）で示す。
5. 比較実験の結果を示す。**費用・品質のどちらで劣る場合もそれを明記する。**

**結果が出る前に優越性や計算削減の達成を約束しない。**
「4 ページから 33 ページへ増えた」「seed 数を増やした」だけを強調しない。
原稿に既に存在する Table 12 や感度分析を、今回新規に追加した結果として説明しない。

## 4. 手順そのものの再設計

### 4.1 3 種類の判定を分離する

プロトコルの出力を次の 3 点に分ける。

1. **選択結果**: 検証した候補のうち、所定の品質条件 $Q$ を満たす最小の次元。
2. **ID 推定の状態**: 安定 / 推定器不一致 / 参照モデル依存 / 未確定。
3. **AU 診断**: 活性割合、$\hat d_{\mathrm{ID}}$ との比、seed 依存性。**品質の合否とは別に記録する。**

V1 に不適合でも再構成品質と費用が良好なら、それだけで「選択失敗」としない。逆に V1/V2 に適合しても品質が悪ければ、その候補は採用しない。推定が不安定なときは、通常の validation 探索への切り替え、または適用見送りを明示する。

### 4.2 再構成の品質条件 $Q$ を定義する

事前指定した大きい参照候補の validation MSE を $D_{\mathrm{anchor}}$ とし、候補 $m$ について

$$Q(m):\quad D_{\mathrm{val}}(m) \le D_{\mathrm{anchor}} + \epsilon_D.$$

- $\epsilon_D$ はタスクで許容できる誤差、または開発用条件で選んで固定した許容値。
- 相対誤差を用いるなら $D_{\mathrm{anchor}}$ がほぼ 0 になる場合の扱いも決める（合成多様体で該当）。
- 参照候補は**同じ AE/VAE 種別・likelihood・β で学習**し、その費用を含める。**参照 AE を VAE の品質基準に流用しない。**
- これは「全探索での真の最適値」への保証ではない。参照モデル自体の品質が低ければ弱い基準になるため、全探索の validation 結果に対する差も報告する。
- 品質を満たす候補が複数ある場合は、**検証済み候補内で**最小の $m$ を選ぶ。探索していない次元も含めて最小とは主張しない。

**品質目標の固定**: 同一の比較条件では品質目標 $T_D = D_{\mathrm{anchor}} + \epsilon_D$ を共通にし、$Q(m): D_{\mathrm{val}}(m) \le T_D$ を判定する。
**β 調整や容量拡張による「品質回復」は、変更前に定めた $T_D$ に達したかで判断し、介入後に anchor を作り直して目標を緩めない。**
方法ごとに目的関数や事前分布が異なっていても、主比較で「同じ品質を達成した」と述べるなら共通の品質尺度と目標が必要である。
方法ごとの相対的な anchor 基準を使う分析を追加する場合は、共通品質目標での比較と分ける。
前処理、MSE の reduction、決定論的再構成（$g(\mu(x))$）か事後サンプルに基づく再構成かも固定し、設定ファイルに落とす。

### 4.3 実装前に固定する設定

| 設定 | 定義する内容 |
|---|---|
| 候補集合 $\mathcal{M}$ | 正整数の有限集合。最大次元、境界近傍の点、探索順序 |
| 参照モデル集合 $R$ | $m_{\mathrm{ref}}$、アーキテクチャ、学習方法、seed 数 |
| ID の安定性 | どの $m_{\mathrm{ref}}$ 間で相対変化何 % 以内なら安定とするか |
| 推定器の一致 | TwoNN/MLE の差・比の許容範囲、不一致時の処理 |
| 初期係数 $c$ | 既定 2（既知の埋め込み上界を動機とする検証対象の既定値）と感度評価用候補。整数への丸め方 |
| 初期範囲 $W$ | AE/VAE で別か共通か。推定区間を使うなら構築式を固定 |
| 品質条件 $Q$ | MSE 尺度、参照候補、$\epsilon_D$、seed 集計規則 |
| 境界確認 | 初期範囲外のどの点を何点検証するか |
| 最大予算 $B$ | 学習回数または実時間。超過時の終了状態 |
| AU の設定 | $\delta$、V1/V2 係数、評価データ、集計方法 |
| 学習停止 | validation 指標、最大 epochs、early stopping 条件 |

**新しい閾値を増やすことで恣意性を解消したつもりにならない。** 少数の設定を開発条件で固定し、その設定のまま評価する。

### 4.4 掲載する Algorithm の骨子

以下は**改訂提案**であり、MAKE47 に既に実装されている手順ではない。VAE の「$m \ge 2\hat d$」を絶対条件として維持するより、**$2\hat d$ を初期検証候補とし、その周辺を品質条件に沿って調べる形**を推奨する。この変更は返答書でも明示する。

**AU の位置づけ（仕様として固定する）**:
基本の選択手順は ID による候補優先と品質条件 $Q$ で構成し、**AU は選択とは別の診断情報として記録する**。
この Algorithm では AU は探索・停止・選択の分岐に使われないため、同じ候補・乱数・学習結果を使えば、
AU の記録を外しても選択 $m$ と品質は変わらない（変わり得るのは AU 算出分の計算時間のみ）。
したがって「本手法 −AU」対照は再学習不要で、同じ checkpoint 群から評価する。
AU に基づいて追加学習（β 調整・容量拡張）の選択を変える方策を評価する場合は、その規則と費用を**別途定義**し、
記録だけの基本手順と区別して記載する。**AU の記録だけの手順に対して、AU による探索改善が実装済みであるように説明しない。**

```text
Algorithm 1: ID-guided candidate search with quality verification

Input:
  D_train, D_val
  model family, fixed training settings including beta
  finite candidate grid M and reference-model settings R
  fixed ID-stability and estimator-agreement criteria
  initial-window factor c, rounding and expansion rules
  quality criterion Q, anchor dimension if Q requires an anchor
  seed set S, maximum budget B
  AU threshold delta and diagnostic coefficients

1. Train independent reference models on D_train according to R.
   Estimate ID on the designated estimation subset.
   Record reference-training and estimation costs.

2. Check ID stability and estimator agreement.
   If the checks fail:
      return ID_UNRELIABLE, or invoke the predeclared conventional search;
      record all fallback costs and identify the result as a fallback.

3. Form the initial candidate window W from the ID estimate(s).
   For VAE, mark the first grid point at or above c * ID as the
   initial diagnostic point, without treating it as an optimum.

4. If needed by Q, train the predeclared anchor of the same model family.
   Train the candidates in W and the predeclared point(s) outside W.
   Select checkpoints using D_val only.
   Cache each trained configuration so it is not charged or trained twice.

5. For each evaluated candidate, compute validation quality and AU.
   Record AU diagnostics separately from the quality decision.

6. If no compact candidate satisfies Q:
      evaluate the next candidate(s) using the fixed expansion rule;
      repeat while budget remains and unevaluated candidates exist.

7. Return the smallest evaluated m satisfying Q, together with
   the search history, ID status, AU diagnostics, and total cost.
   If only the anchor qualifies, report NO_COMPACT_ALTERNATIVE_FOUND.
   If evaluation cannot be completed within B, report BUDGET_EXHAUSTED.

Final evaluation, outside the selection algorithm:
  Freeze the selected configuration and report its performance on D_test.
  Do not revise the selection based on the test results.
```

選択のために seed 平均を使う場合、反復される評価単位は「その seed 集合を用いた選択手順」になる。各 seed で独立に選択して分布を比較する場合は、その方式で全比較法を揃える。**両者を同じ実験表で混同しない。**

### 4.5 この変更で失われる主張

品質確認点と独立参照を増やすため、現在の「VAE は 1 候補の学習だけで十分」「最大 82% 削減」は**成立しなくなる可能性が高い**。また、MSE 確認だけで選択が十分なら、AU は説明用の補助指標となる。

これは改訂の失敗ではなく、検証可能な主張に整理した結果である。ただしその後も既存法に対する付加価値が示せない場合、**現行の方法提案として採録を目指す根拠は弱い**。結果に合わせて中心課題を縮小する判断が必要になる（→ §9）。

### 4.6 埋め込み上界の引用（背景としての利用）

§3.2.2 の「係数 2 は第一原理から導いたものではない」を、次のように**書き換える**（新規命題としては追加しない）。

> 滑らかな多様体の Euclidean 埋め込みに関する既知の上界（$d \le d_{\mathrm{emb}} \le 2d$、強 Whitney 埋め込み定理）は、
> 真の次元の 2 倍を初期容量の参考にする幾何学的動機を与える。ただし、本研究の推定 ID と学習された VAE について
> 同じ保証を与えるものではない。そのため係数 2 は検証対象の既定値とし、品質・費用に対する感度を実験で評価する（E4）。

**併記する事項**:
- トーラス（$d=2$, $d_{\mathrm{emb}}=3 \le 4$）は上界と矛盾しない。合成実験で $m \ge 5$ が必要だったのは AU 打ち切りの挙動であり、埋め込み可能性の問題ではない。
- 上界 $2d$ は Klein bottle でタイトになる。
- **この引用は V2 の判定力欠如（§2.6）も比較実験の不足（R2-3）も解決しない。**
- 原稿の「実験で有効だった既定値」という経緯を、事後的に「定理から導出して設定した」ことへ書き換えない。

Rolinek ら（CVPR 2019）の「VAE は PCA 方向を追う」という結果も、線形 Gaussian 代理の**限定した解釈を補う引用**として利用する。
引用しただけで、非線形 VAE に対する water-filling 条件や有限 AU 閾値の正当性が成立するわけではない。

典拠は Hatcher [27] で足りるか確認し、不足なら Lee, *Introduction to Smooth Manifolds* を追加する。

---

## 5. 追加実験計画

### 5.1 評価目的を先に固定する

主目的は §1.2 の問いに合わせ、(i) **活性読み取りの安定性と再構成品質の関係**（E7a）、
(ii) **同じ品質条件を備えた既存法に対する ID 優先づけと AU 診断の追加価値**（E7b, E1, +Q 対照）とする。
費用は (ii) の一部として共通条件で測る。分類・生成品質は別の評価軸として扱う。

| 評価軸 | 指標 | 解釈上の注意 |
|---|---|---|
| 再構成 | 画素平均 MSE、必要なら SSIM | 同じ前処理・出力範囲・評価方式で比較 |
| VAE の確率モデル | 通常の ELBO、再構成項、KL 項 | **β で重みづけた目的関数と通常の ELBO を区別** |
| 下流表現 | 固定した linear probe の test accuracy | probe の正則化等は validation で選択 |
| 圧縮 | 設定次元 $m$、手法固有の有効次元、AU | 有効次元と実際に削除された次元を混同しない |
| 選択の安定性 | 選択 $m$ の分布、品質条件達成率 | mean±SD だけでなく seed ごとの結果を提示 |
| 計算効率 | 総実時間、GPU 時間、学習回数、総 epochs | 参照 AE・ID 推定・再探索を含む |
| 診断の有用性 | 誤受容、誤警報、介入後の品質変化 | **V1/V2 自体を正解ラベルにしない** |

Accuracy は AE/VAE 単体の再構成性能の指標ではない。**Reviewer 1 の提案には、学習曲線では MSE を採用し、分類 Accuracy は下流評価で報告する形で対応する。**

### 5.2 共通のデータ分割と再現性

1. 公式 train から学習用と validation を作り、公式 test を最終評価用として確保する。部分集合を使う場合、**先頭順ではなく固定乱数による抽出とインデックス保存**を行う。クラスを持つデータでは分割の偏りを点検する。
2. 参照 AE、ID 推定、候補モデル選択、probe のハイパーパラメータ選択に使用したデータを図表で明記する。標準化・PCA などの変換も train で fit する。
3. 主たる比較では **5 つの独立した学習 seed** を基本とし、参照モデルから選択まで全手順を反復する。**既存 checkpoint への 5 回の再評価を 5 seeds と数えない。**
4. データ分割への頑健性は、代表条件で **3 分割 × 3 学習 seeds** を推奨する。全条件に広げる必要はない。
5. 統計単位を区別する。学習 seed、データ分割、ID の subsampling 反復を混ぜて標本数を増やさない。
6. 既に確認済みの MNIST 等を開発条件とし、**追加データの test や未使用条件に閾値を合わせ直さない。** 条件変更の履歴を残す。

5 seeds は万能な精度保証ではない。差が小さい、または判定が seed で割れる場合に限って反復を追加する。**非有意差を性能同等の証明として扱わない。**

### 5.3 比較法の構成

| ID | 比較法 | 選択の実装 | 費用の扱い |
|---|---|---|---|
| B1 | validation MSE による全探索 | 全候補から MSE 最良点と、許容劣化内で最小の $m$ | 全候補の学習を計上 |
| B2 | validation ELBO による全探索 | 同じ likelihood の VAE で最大 ELBO の $m$ | B1 の学習結果を再評価可能 |
| B3 | 下流 validation による全探索 | validation accuracy で $m$ を選び test で評価 | ラベル使用法として別枠。probe 費用も計上 |
| B4 | ID を使わない粗い探索 | 同じ有限グリッドから対数間隔候補、または固定予算のランダム探索 | 提案法と同程度の総時間で比較 |
| B5 | 固定次元 | 32、64 などを結果を見る前に固定 | 安価な実務的対照 |
| **B6a** | **FONDUE (Algorithm 1)** | 原著を再現。$\mathrm{IDE}_z - \mathrm{IDE}_\mu \le$ データ ID の 20% を満たす最大次元を指数＋二分探索。少数 epochs | 少数 epochs の探索を尊重し、**選択後の最終学習を含める** |
| **B6b** | **FONDUE-VAR (Algorithm 3, 付録 G)** | 原著を再現。$2\times$ データ ID から開始し、passive/mixed が出るまで 2 倍。活性変数数を返す | 同上。**1/2/5 epochs と 300 epochs の両方**で実行し、学習長の効果を分離（→ E7） |
| **B7** | **GECO/L0 系** | 原著に従い再構成制約下で剪定 | 初期モデル・チューニング・追加学習を含める |
| B8 | ARD-VAE | 原著の関連性判定で有効次元を得る | 実装可能なら追加。目的関数の違いを明示 |
| B9 | PCA / Bayesian PCA / Minka | 分散比基準・ARD 次元 | 補助的対照。**PCA 次元を真の ID としない** |

**+Q 対照（$Q$ の追加価値と ID / AU の追加価値を分離するために必須）**:

| 対照 | 検証する点 |
|---|---|
| **B6a+Q**: FONDUE + 提案法と同じ $Q$・同じ追加探索規則 | 品質確認は既存選択法への簡単な追加で得られるか |
| **B6b+Q**: FONDUE-VAR + 同じ $Q$ | 提案法の利点は $Q$ によるものか、AU の定義等によるものか |
| **B4+Q**: ID なしの探索 + $Q$ | ID 推定・参照学習の費用に見合う候補優先効果があるか |
| **本手法 −AU**: ID による探索 + $Q$、AU を記録しない版 | AU が選択判断を変えるのか、補助的な説明なのか |
| 単純な診断対照 | MSE、KL、学習曲線等だけで同じ追加確認を選べるか |

既存法を変更した版には「+Q」と明記し、原著法そのものの結果も残す。$Q$、anchor、追加探索、予算、品質評価方式を揃え、その全費用を計上する。

**費用比較の公平性**: FONDUE には短時間探索が目的に含まれるため、全候補を 300 epochs 学習させる比較は不公平。逆に探索段階だけの時間と本手法の最終モデル完成までの時間も比較しない。**どの方法も「最終評価できる選択済みモデルが得られるまで」の費用に揃える。**

目的関数や事前分布が異なる方法間では、各方法の学習損失の値を直接順位づけしない。共通の再構成品質・下流性能・費用で比較し、有効次元は各手法の定義を併記する。**AU が少なくなっただけではモデルサイズや推論費用が減ったことにならず**、実際の剪定・再学習を行う場合はその費用も含める。

### 5.4 データセットの優先順位

| データ | 役割 | 推奨対応 |
|---|---|---|
| Swiss Roll / Torus | 理論説明・偽の AU 合格の検証 | **分割・損失・出力層を修正した条件で再確認**（§2.10 の確認後） |
| MNIST | 既存主結果の再検証 | 比較法、費用、品質条件、全手順 5 seeds |
| Fashion-MNIST | AU 下限違反の検証 | β 低減・$m$ 拡張による品質の変化を調べる（介入実験） |
| CIFAR-10 | 自然画像での実用性 | **独立参照を含む全手順**、MSE・KL・probe・総費用 |
| SVHN | CIFAR-10 と異なる自然画像条件 | 独立参照と推定器不一致を確認 |
| **dSprites** | **共通ベンチマーク（P0、MAKE47 に対する新規追加）** | FONDUE の原著に公表結果があり（Conv $12.2\pm0.4$、FC $6.6\pm0.5$、FONDUE-VAR $8.8$–$9.6$）、今回共通ベンチマークとして追加する。**公表値は再現性の参考として示し、主比較には共通環境で再実行した値を使う。** Conv と FC の値は異なるアーキテクチャの選択結果であり dSprites の「正解次元」ではない。**離散的な生成因子数（5）をそのまま厳密な多様体次元の真値とみなさない** |
| KMNIST 等 | MNIST 固有でないことの確認 | 余力があれば。P2 |

**CIFAR-10 と SVHN は既に原稿に登場するため、「新しいデータセットを 2 つ追加した」とは書かない。** 追加価値は、従来未完了だった全手順の検証と性能比較である。

**自然画像で必ず V1/V2 を通過させることを目標にしてはいけない。** β を上げて AU を減らしても、画像がぼやけたり品質が悪化すれば選択法の改善ではない。品質条件を満たせない場合は、適用を見送る結果を正しく報告する。

### 5.5 実験パッケージ

#### E1: 品質・探索費用・選択次元の直接比較【P0・最優先】

**問い**: ID による候補の優先づけに、全探索・粗い探索・既存の次元選択法に対する利点があるか。

- 同一データ・同一前処理・同一基本アーキテクチャで、提案法と B1–B7 を比較する。
- まず β を固定し $m$ 選択の効果を調べる。次に必要な範囲で β も validation で選ぶ実験を追加し、その費用を含める。
- MSE の許容劣化と選択次元・費用の関係を示す。相対劣化 1% / 5% / 10% は**感度評価の候補であり、推奨値として正当化済みのものではない**。
- 全探索で得た test 最良点は事後的な参考値にとどめ、比較の正式な基準は validation で選択したモデルとする。

**掲載物**: 方法別結果表、品質と総時間の図、品質と $m$ の図、seed 別選択 $m$ の分布。

**結果に応じた判断**: 品質が保てても総費用が減らなければ「高速な選択法」の主張を縮小する。FONDUE より遅い場合はその事実を示し、診断が提供する追加情報を別途評価する。

#### E2: 学習曲線と過学習の確認【P0/P1・R1-9 直結】

**問い**: $m$ の影響と過学習・未収束の影響を区別できるか。

- 小さい $m$、初期候補、より大きい $m$ を各データから事前に選ぶ。
- train/validation の MSE を epoch に対して示し、VAE では KL と通常の ELBO、必要に応じて AU も別パネルに示す。
- **比較する train MSE も評価モードで計算**し、validation と同じ再構成方式を使う。VAE は $g(\mu(x))$ による決定論的再構成か事後サンプル平均かを明記する。
- checkpoint は validation のみで選択する。test は選択済み checkpoint で評価する。

Reviewer 1 の 3 分割学習曲線への対応は、**train/validation 曲線に最終 test 値を加える方法を第一候補**とする。全 epoch の test 曲線を載せる場合は、手順と checkpoint を固定後に保存済みモデルを一括評価した**事後的図**とし、その曲線を見て再調整しない。返答では test を選択から隔離した理由を説明する。

#### E3: 参照表現への依存性【P1・R3-3 直結】

**問い**: 参照 AE が変わっても、ID だけでなく最終選択と品質が安定するか。

- 主要比較: 入力空間、PCA 前処理空間、FC-AE、Conv-AE。既存の AE/IsometricAE/CAE/DAE 比較は補助的に利用する。
- 複数 $m_{\mathrm{ref}}$、学習 seed、代表的な学習時点、推定用サンプル数を変える。総当たりより、容量・アーキテクチャ・seed を先に調べる。
- TwoNN/MLE を主とする。Costa–Hero の MST 由来 ID 推定器の追加は、E7・直接比較・参照依存の評価を先に完了し、追加の graph 推定器が具体的な不確実性を解消する場合に実施する（R1-6 への回答のために実装を必須化しない）。
- 近傍重なり、再構成 MSE、選択 $m$、最終品質、費用を示す。
- $m_{\mathrm{ref}}$ を増やしても安定しない場合や推定器が大きく食い違う場合に、**どの段階で手順を見送るかを固定する**。

既存 Table 12 は有用だが MNIST/Fashion の近いモデル群での結果である。「モデル依存性を検討済み」で済ませず、自然画像と FC/Conv の差を確認する。**独立した VAE seed で推定値が近いことは、参照 AE 自体の seed 頑健性の代わりにならない。**

#### E4: 閾値・構成要素の検証【P0/P1・R3-1 直結】

**問い**: 係数 2 や AU 条件に依存せず、品質・費用に基づく結論が維持されるか。

| 要素 | 検証候補 | 主に見るもの |
|---|---|---|
| 初期範囲の係数 $c$ | 1, 1.5, 2, 3, 4 | 品質条件達成率、追加探索費用 |
| V1 の活性割合閾値 | 0.7, 0.8, 0.9, 0.95 | 品質不良の検出・誤警報 |
| V2 の上下係数 | 下側 0.5/0.75/1、上側 2/3/4 | **条件の冗長性**と診断の増分価値 |
| AU の $\delta$ | 0.001, 0.01, 0.1 | seed・モデル間での診断変化 |
| 品質許容誤差 $\epsilon_D$ | タスクに応じた複数水準 | 圧縮・品質・費用のトレードオフ |

全直積で学習をやり直す必要はない。AU 閾値や V1/V2 は保存した潜在統計から再評価できる。$c$ の比較は固定した完全グリッドのログで探索を再現してもよいが、その場合は「**事後的な探索シミュレーション**」と明記し、実時間計測の実行例を別に設ける。

**必要なアブレーション**: ID なし / ID のみ / ID + 品質確認 / ID + 品質確認 + AU 診断。
**AU を追加しても品質不良の検出や判断に役立たないなら、AU を独立した主要貢献から外す。**

現原稿の「観測された判定が変わらない閾値範囲」は同一データ内の安定性にとどまる。開発条件で既定値を決め、**未使用条件で診断が役立つか**を検証する。

#### E5: 自然画像での全手順と失敗の評価【P1・R3-2 直結】

**問い**: 現状未完了の Step 3 に対し、品質を伴う選択または妥当な適用見送りができるか。

- CIFAR-10 を優先し、**独立した参照 AE から手順を開始する**（§2.5 の是正）。
- FONDUE・FONDUE-VAR も同条件で実行し、適用限界が本手法固有か FONDUE 系にも共通かを確認する（仮説 H4）。
- 損失のスケールを統一した上で、固定 β と annealing を分ける。
- 基本 decoder を保った比較を先に行い、必要なら薄い decoder を追加する。**$m$ と同時に decoder まで変更した効果を分離する。**
- AU だけでなく MSE、KL、ELBO、probe、再構成例を示す。
- 最初の候補、拡張した候補、使用時間、適用見送りの理由を記録する。

全条件で成功する必要はない。ただし**自然画像ではいつも見送るだけで成功は MNIST だけ**という結果なら、汎用的な選択法としての主張は弱いままである。その場合は適用範囲を狭めるか、診断の体系的比較を主目的にする。

#### E6: 理論の限定と人工条件での切り分け【P1】

**問い**: 位相・観測ノイズ・β・学習不足が、それぞれ何に影響するか。

- 既知共分散の Gaussian データと線形 Gaussian モデルで、仮定を満たす代理問題の挙動を確認する。**理論の新規性ではなく実装・解釈の確認**として扱う。
- 同じ ID で埋め込み構造の異なる人工データ、ノイズ強度の異なる条件を比較する。
- Torus では低 AU でも再構成が不十分な例を示し、**AU 合格と品質達成の違い**を明確にする。
- AU 未満の座標を実際に無効化して再構成が変わるかを補助的に調べる。**小さい posterior-mean 分散だけでは座標が完全に無情報とはいえない。**

大規模な新理論や新アーキテクチャの開発は必須課題にしない。**原稿の不正確な理論的主張を除くことを優先する。**

#### E7: 学習時間・統計量・品質の関係【P0・新設・R2-1 の中核】

E7 は**読み取り自体の分析（E7a）**と**選択手順全体の評価（E7b）**に分ける。
FONDUE Table 7 の mean±SD は「探索後に返した潜在次元数」の分布であり、固定 $m$ の checkpoint で計算する AU の SD とは
推定対象が異なる。**両者を同じ欄に置いて SD の大小だけを比較しない。**

##### E7a: 同一モデル・同一 checkpoint における読み取り

**目的**: 学習時間と統計量の影響を、探索経路の違いから切り離す。仮説 H1・H2・H3（§3.3）を検証する。

| 項目 | 設計 |
|---|---|
| データ | dSprites と MNIST を主条件。トーラスは品質との乖離を調べる補助条件 |
| 容量 | 小さい容量・初期候補・余裕ある容量の少数条件を事前指定。全方式で一致 |
| 読み取り A | 原著の定義・閾値（U12: 分散層出力のデータ間分散 $<0.1$、平均で passive/active を分類）による active / mixed / passive 数。**active 数と active+mixed 数を区別し、H2 で主に比較する方を事前指定**、他方は補助表示 |
| 読み取り B | 標準 AU（posterior-mean のデータ間分散 $>\delta$）。$\delta$ を開発条件で固定し、感度を別に示す |
| 読み取り C | FONDUE 本体の $\mathrm{IDE}_z - \mathrm{IDE}_\mu$ の経時変化と seed 依存性。**A/B とは別パネル**。連続量なので、閾値に対する余裕や判定変化としても見る |
| 学習時間 | 1・2・5・50・300 epochs を記録。必要なら中間点を追加。更新回数・経過時間も保存。**H1 の主要比較は早期と後期の 2 時点（例: 5 vs 300）を事前指定**し、全軌跡は併記 |
| 学習条件 | 同じモデル、β、likelihood、loss reduction、optimizer、batch size、データ分割 |
| 評価標本 | 同じ推定用データ。推定器の subsampling と学習 seed を区別 |
| 反復 | 独立学習 5 seeds を基本。各 seed の軌跡を表示 |
| 安定性の 2 区分 | **seed 間**: 同じ epoch・同じ容量での seed 間 SD と各 seed の値。**学習中**: 同じ seed 内で後期 checkpoint 間の値の変動。両者を分けて報告する |
| 順位づけの禁止 | A・B は閾値で離散化した個数、C は連続量。**A/B/C の生の SD を一列に並べて最小を最良と順位づけしない** |
| 併記 | validation MSE、KL、通常の ELBO、学習曲線。AU $=0$・AU $=m$ 等の退化も表示 |

同じ学習系列の途中の checkpoint を使えるため、各時点のために別々のモデルを学習する必要はない。
ただし**異なる epoch を独立した反復として数えない**。
「収束」は固定 epoch 数と同義にしない。停止・plateau 判定を事前に定義し、最大予算で未収束ならそう記す。
安定化時点は品質の達成時点と分けて示す。

##### E7b: 選択アルゴリズムを最後まで実行した比較

**目的**: 選択次元、品質、費用、seed 依存性を方法単位で評価する。E1 の一部として実施してよい。

- FONDUE 本体は原著の早期探索の設定を尊重する。閾値は**主比較を 20% 規則（初期データ ID の 20% を絶対閾値として固定）と事前に決め**、絶対差 1 は感度・再現性確認として別に示す（U13）。epoch 数はコードの既定値でなく論文の各データの設定に従う（U16）。
- FONDUE-VAR は原著相当の早期設定と、学習時間を延ばす実験的変種を区別する。主比較は**記述仕様に基づく再実装（単一指数化、keep_mixed を事前固定）**で行い、固定 commit のコード経路（二重指数化）は代表条件のみの補助評価にとどめる（U14, U15）。
- 提案法も参照モデルから選択・最終学習まで全手順を反復する。
- 各方法について、選択 $m$、AU 等の補助量、validation / test 品質、総時間、終了状態を報告する。
- 長時間学習の変種には、その追加費用を計上する。
- 原著公表値は再現性の参考として示し、主比較には共通環境で再実行した値を使う。

##### E7 の結果の判断

| 結果 | 言えること | まだ言えないこと |
|---|---|---|
| 長時間学習で読み取りが安定する | 該当条件で時間依存性があった | 一般に 300 epochs で収束する／FONDUE を反証した |
| AU が変数分類より安定する | 共通条件下で統計量による差がある | AU のほうが正しい次元を返す |
| 安定した読み取りでも $Q$ を満たさない | 安定性と品質は別である | AU 診断が他の診断より有用である |
| AU を使う追加判断が未使用条件で役立つ | 診断としての増分価値がある | 全モデル・全タスクへの一般性 |
| 長時間学習でも不安定 | 安定した次元読み取りという主張は支持されない | 実証的分析自体に価値がない |

#### AU 診断の有用性の定義【E4 と E7b に接続】

$Q$ が再構成品質の合否を既に直接判定するなら、「AU が $Q$ 不合格を検出した」と重ねて示すだけでは追加的な実用価値が明確にならない。
次のどちらかを**主要な AU 評価として一つ選び、事前指定**する（両方を必須化しない）。

1. **追加実験の選び方**: $Q$ 不合格の条件で、AU を使う方策が、単純な方策より少ない追加費用で**変更前に固定した品質目標 $T_D$** に到達できるか。β 調整・容量拡張等の選択肢と費用を全方式で揃える。この方策は §4.4 の基本手順とは別の規則として定義する。
2. **品質確認から独立した予測**（推奨）: 現時点の MSE・KL 等だけの診断と、それに AU を加えた診断とで、その後の品質や座標削除の影響の予測が未使用条件で改善するか。**同じ checkpoint 群で評価でき、再学習を要しない。**

評価対象の対応:

| 評価対象 | 実施する比較 | 得たい証拠 |
|---|---|---|
| ID による候補優先 | ID+Q と、ID なし+Q、FONDUE 系+Q | 同じ品質目標での探索費用・選択容量の違い |
| AU の情報としての価値 | MSE・KL 等のみの診断と、それに AU を加えた診断（同一 checkpoint 群） | 未使用条件での予測・説明の改善 |
| AU を使う行動選択の価値 | AU を使う追加確認方策と、単純な追加確認方策 | 同じ品質目標に達する追加費用の違い |

単に $\delta$ や V1/V2 を変えて「合格」が増えたことは診断の検証にならない。
AU が誤った次元を安定して返したことは、AU 単独の次元推定への反例になり得ても、AU が有用な診断であることの証明にはならない。

### 5.6 計算量の見積もり方

確約できる日程は出さない。代表条件を 1 回試行して時間を計測し、以下を積算する。

```
総費用 = Σ_data Σ_method [ 参照AE学習 + ID推定 + 候補学習(|候補| × |seeds|)
                         + anchor学習 + 再探索 + probe学習 + 評価 ]
```

- FONDUE / GECO-L0 は設定探索の費用も含める。
- AU 閾値変更などの補助的感度は既存統計の再集計で済ませる。
- 計算時間が限られる場合でも、**新規性の整理・test 分離・直接競合比較・品質と費用の再計算・理論の誤り修正は削らない。**
- 削減候補: 補助的な decoder 種類、全データでの大規模 β 探索、主結論に無関係な位相例、補助 baseline の数。

---

## 6. 査読者別の対応

### 6.1 Reviewer 1（10 項目）

| 番号 | 指摘と評価 | 実施する修正 | 返答時に示す証拠 |
|---|---|---|---|
| R1-1 | Abstract の記号が多い。妥当 | $m$, $\hat d$, $\beta$, $\lambda$, $\tau$ と不等式を除き、目的・手順・主要結果・限界を平易な文章で説明。**現在 243 語 → MDPI 推奨 200 語以内**に圧縮 | 改訂 Abstract。数値成果は再評価後に確定 |
| R1-2 | 「32 や 64」に引用がない。妥当 | 具体例を削り「latent dimensionality is commonly treated as a hyperparameter」と限定し、**次元選択の先行研究（FONDUE 等）を引用**。具体数を残すなら実際に用いた論文（Locatello et al. ICML2019 が潜在 10 固定、Ha & Schmidhuber NeurIPS2018 が 32 など）を確認の上で挙げる | Introduction の修正文と新引用 |
| R1-3 | Introduction の節参照が多すぎる。妥当 | **現在 19 か所 → 5 か所以内**。節参照は最後の構成紹介にほぼ集約 | p.1–4 の再構成 |
| R1-4 | 導入に結論が混在。おおむね妥当 | データ別結果・閾値範囲・自然画像の詳細を Results/Discussion へ移す。§1.4 は 3 文の範囲宣言に圧縮し §6.4 に一本化 | 導入の短縮と移動先 |
| R1-5 | §1.2 の記号説明不足。妥当 | 貢献を文章で記述し、正式定義は Methods 冒頭の記号表へ集約。Introduction に残す記号は $m$ と $\hat d_{\mathrm{ID}}$ のみ | 新 Methods と記号表 |
| R1-6 | グラフ系次元削減の関連研究が不足。趣旨は妥当 | PCA・Isomap・LLE を背景に追加。Costa & Hero (2004) の MST 由来 ID 推定器を graph-based の例として引用する（推定器の実装追加は必須化せず、E3 の結果が具体的な不確実性を残す場合に行う）。指定 DOI の論文は異常検知が主題なので次元削減法として誤って紹介しない | Related Work の追加段落と位置づけ説明 |
| R1-7 | 証明末尾の □ が不明。容易 | 最初の証明に「The symbol □ marks the end of a proof.」を一度説明、または "End of proof." に変更 | 修正箇所 |
| R1-8 | CUDA バージョン不明。妥当 | **環境記録から実測値**で PyTorch・CUDA・cuDNN・driver・GPU・CPU 実行の有無を記載。PyTorch 2.0 という記載から CUDA を推測しない。現在の PC の CUDA が実験時と同じとも限らない | 環境表と requirements/環境ファイル |
| R1-9 | 分割・学習曲線・反復数。中心的指摘 | **E1/E2 の分割・曲線・主要 5 seeds を実施。** MSE を主指標、Accuracy は下流評価 | 分割表、学習曲線、seed 別結果、最終 test 性能 |
| R1-10 | DOI 不統一。妥当 | **現在 37 件中 12 件のみ DOI 付き。** DOI がある文献には付与する。**公刊版を引用するならその版の DOI を優先**し、arXiv 版を引用する場合は書誌全体（著者・年・版）をその版に合わせる。DOI のない文献は安定 URL 等を統一書式で記載。著者・年・会議・巻頁も原典で確認 | 統一した参考文献リスト |

**R1-2 の注意**: 査読文中の "set to 30" の 30 は**原稿の行番号**であり、潜在次元 30 を使っているという指摘ではない。数値慣習の無出典な一般化に対応する。

**R1-6 の注意**: DOI 10.1109/ACCESS.2022.3190505 は Gałka et al. *Isolation Forest Based on Minimal Spanning Tree*（IEEE Access 10, 74175–74186, 2022）で、**主題は異常検知**である。返答では、古典的・グラフ系手法の説明を補ったことを先に述べ、指定論文は主題が異なるため背景上の扱いを限定したと丁寧に説明する。関連が薄い引用を機械的に追加したり、査読者の意図を批判したりしない。

### 6.2 Reviewer 2（4 論点）

| 番号 | 指摘と評価 | 対応方針 | 返答の中心証拠 |
|---|---|---|---|
| R2-1 | NOLTA2025 からの増分が小さい。最重要 | 会議論文本体と逐条照合**済み（§3.4）**。既出の着想（ID がボトルネック設計の手がかり／潜在 ID < 入力 ID）を明示的に認め、差分を 3 列（会議版・MAKE47 既存・MAKE49 追加）で示す。**あわせて FONDUE 付録 G との共通性を自ら開示し、学習時間と統計量の効果を E7a/E7b で検証する**（§3.2–3.3） | §3.4 の 3 列差分表、§3.3 の仮説 H1–H4、E7a/E7b、E1 と +Q 対照の比較表 |
| R2-2 | 新アーキテクチャ・目的関数がなく恣意的 heuristic。重大だが「新構造が必須」という含意までは受け入れない | 実証的な選択手順として位置づける。品質条件・停止・拡張・閾値検証・構成要素比較を追加。理論は既知・限定条件の結果として整理し、埋め込み上界は係数 2 の動機づけとして背景引用にとどめる | Algorithm、E1/E4/E6、+Q 対照、理論の削除・修正一覧 |
| R2-3 | 全探索以外の既存法との比較がない。妥当 | **FONDUE と既存剪定法を追加**し、validation MSE/ELBO/下流選択、同予算探索とも比較 | E1 の方法別性能・費用・選択次元表 |
| R2-4 | Table 4 の数値形式と全体の粗さ | Table 4 を整形し、**誤った elbow 説明も修正**（§2.4）。全図表をログから再生成して照合 | 修正 Table 4、再生成記録、全文の表記統一 |

**避けるべき返答**:
- 「4 ページから 33 ページへ増えた」「seed 数を増やした」だけを強調すること。
- 原稿に既に存在する Table 12 や感度分析を、今回新規に追加した結果として説明すること。

R2-2 への回答は、理論の新規性を無理に拡張するより、**実測で支持できる有用性**を示すほうがよい。FONDUE や GECO 系との比較で優位性が見られない場合は、その結果を開示した上で、適用条件や誤診断の分析にどの追加価値があるかを示す。

### 6.3 Reviewer 3（7 項目）

| 番号 | 指摘と評価 | 実施する修正 | 返答時に示す証拠 |
|---|---|---|---|
| R3-1 | 2×ID、V1、V2 が heuristic。妥当 | 係数 2 を**初期候補の既定値**へ限定（既知の埋め込み上界を動機として引用し、感度分析で検証）。**V1/V2 を補助診断へ整理**し、冗長な上側条件（§2.6）を説明。独立条件で閾値を検証 | E4、閾値表、品質・費用への影響 |
| R3-2 | MNIST/Fashion 以外と自然画像での検証が弱い。妥当 | CIFAR-10/SVHN の既存観測を**全手順の評価へ拡張**し、新しいデータ条件（dSprites 等）を追加 | E5、追加データの比較結果、適用見送りを含む結果 |
| R3-3 | ID 推定の参照 AE 依存。既存比較はあるが不足 | 入力/PCA/FC-AE/Conv-AE、容量、seed、サンプル数を比較。**最終選択への伝播まで評価** | E3、独立参照モデルの設定・結果表 |
| R3-4 | Proposition 2 の主張をさらに限定すべき。妥当 | 固定 β・固定観測分散・Gaussian source 等の仮定を明記。**AU との同一視、$\tau_{\mathrm{eff}}$ の循環説明、KL 符号からの方向推論を除く**（§2.11） | §4・Appendix A の具体的修正 |
| R3-5 | reconstruction / ELBO / 下流検証の baseline 不足。妥当 | B1–B3 を共通グリッド・独立 test で比較。手法ごとに最適化対象とラベル使用を明記 | E1 の選択基準別結果表 |
| R3-6 | "conditional" の繰り返しで読みにくい。妥当 | **現在 24 回 → 8 回程度。** Methods に適用条件、Discussion に限界を集約 | 短縮した本文と適用範囲表 |
| R3-7 | Steps 1–3 と閾値の擬似コードが必要。妥当 | §4.4 を基に、**実際のコードと一致する** Algorithm を掲載。ID 不安定時・範囲外・予算上限・同順位の処理も定義 | Algorithm、設定表、再現用コード |

**R3-4 の注意**: 現原稿にも限定の注意書きがあることを主な反論にしない。**本文の注意書きと付録の断定が不整合**であるため、具体的に何を削除・修正したかを示す。

---

## 7. 原稿全体の再構成

### 7.1 推奨する章構成

| 改訂後の章 | 内容 | 現原稿からの変更 |
|---|---|---|
| 1. Introduction | 実務上の問題、既存選択法の限界、研究上の問い、簡潔な貢献 | 現 §1.2 の数式・結果列挙、§1.4 の詳細な限界を移動 |
| 2. Related Work | 次元削減、ID 推定、**次元選択**、**剪定**、先行会議論文との関係 | **FONDUE・GECO/L0・ARD 系を追加。** 既存研究を「測定だけ」と一括しない |
| 3. Problem Formulation and Protocol | 目的、データ分割、**品質条件 $Q$**、Algorithm、閾値、失敗時の処理 | 選択と AU 診断を分離。目視依存を削減 |
| 4. Theoretical Background | 位相的必要条件、既知の埋め込み上界（背景引用）、限定した線形 Gaussian の解釈 | 既知結果の背景として簡潔に整理。危うい一般化を削除 |
| 5. Experimental Setup | モデル、損失（reduction 明記）、データ、分割、seed、比較法、計算費用 | 現 §5.1 を大幅に明確化 |
| 6. Results | 主要比較、追加データ、参照依存、閾値、診断・失敗例 | 現「成功/境界」の先取りをやめ、独立指標で順に示す |
| 7. Discussion | 何が有効だったか、既存法との差、適用範囲、限界 | "conditional" の反復を集約 |
| 8. Conclusions | 追加実験で支持された内容と限定を短く記述 | 達成していない一般化や未再計算の削減率を除く |
| Supplementary | 全グリッド、seed 別ログ、補助的感度、細かな導出 | 主結論に不要な探索経緯を移す |

Methods 中の現 Table 3（applicability）は実験結果の要約なので、**Results 末尾または Discussion へ移す**。研究前提として既知の適用条件と、実験から分かった適用範囲を同じ表に入れない。

### 7.2 Abstract の書き換え方針

1. 潜在次元選択の実務上の問題（設定容量と学習後に使われる容量の区別、品質との関係）。
2. ID を用いて候補を優先し、品質条件で選択し、活性の読み取りを診断として別に記録する手順。**学習時間と読み取り統計量が安定性に与える影響を検証したこと**を含める。
3. 比較対象（FONDUE 系・剪定法・validation 選択・+Q 対照）と、検証したデータ・モデルの範囲。
4. 独立 test で得られた品質・総費用と、**AU 診断の追加価値についての結果**。
5. 適用を見送った条件と、結論の範囲を一文で。

**新しい結果が得られる前に、現在の「60–80% 削減」をそのまま入れない。** 結果が未確定な段階では完成版 Abstract を確定せず、目的・方法の文だけ先に整える。記号は全廃し 200 語以内。

### 7.3 削除または置換すべき表現

| 現在の表現 | 修正方針 |
|---|---|
| "a selection principle grounded in the data itself has not been established" | **ID を用いた既存選択法（FONDUE）を認め**、未解決の実務的問題を具体化 |
| 先行研究は "measure"、本研究は "design" | 次元選択まで扱う先行研究を含む比較に変更 |
| "the elbow appears exactly at $m = d_{\mathrm{emb}}$" | 実測 MSE と位相的必要条件を分け、**exactly を削除** |
| "the protocol return[s] the correct dimension" | 所定の品質条件下で選んだ候補、と目的に沿って記述 |
| "correctly identified" an over-pruning boundary | 独立した品質回復の証拠がない間は「**AU 条件への不適合を示した**」に限定 |
| "β too small" を断定する節見出し | 「指定した学習条件で活性が維持された」等、**観測を表す見出し**に変更 |
| "Step 1 works" を文献値との一致で判断 | 推定の安定性・一致を観測した、と限定。**真値との一致は主張しない** |
| "no automatic truncation" から AE は全次元を使うと説明 | **明示的な剪定機構を持たない**、と限定 |
| "pre-registered" | 実際の事前登録記録がある場合のみ使用 |
| "conditional"（24 回） | 8 回程度に削減。具体的な条件そのものを書く |

### 7.4 追加・再編する図表

図を単に増やすより、**査読上の疑問に答える図**を優先する。

| 図表 | 内容 | 対応する懸念 |
|---|---|---|
| 新 Table A | NOLTA2025・FONDUE 等と本研究の比較 | 新規性（R2-1） |
| 新 Algorithm 1 + 設定表 | 入力、閾値、探索、品質、停止、出力 | 再現性・恣意性（R3-7, R3-1, R2-2） |
| 新 Figure A | train/validation 曲線と最終 test 値 | 過学習・収束（R1-9） |
| 新 Table B | 方法別の $m$、品質、下流性能、総費用 | 既存法比較（R2-3, R3-5） |
| 新 Figure B | 品質と総時間、品質と $m$ の関係 | 実用的利点 |
| 新 Figure C | 参照表現・$m_{\mathrm{ref}}$ による ID と最終選択の変化 | モデル依存性（R3-3） |
| 新 Figure D | 閾値感度と ID/AU の構成要素比較 | heuristic の妥当性（R3-1） |
| 新 Figure E | 同一 test 画像の再構成比較 | AU 削減に伴う品質劣化 |
| 新 Table C | 成功・不適合・適用見送り・再探索費用 | 適用範囲の明確化 |
| **新 Figure F（E7a）** | 固定容量・固定 seed 系列での読み取り A/B/C の epoch 軌跡（別パネル）、seed 間 SD、品質の併記 | 学習時間・統計量と安定性（R2-1, R3-1） |
| **新 Table D（E7b）** | 方法単位の選択 $m$（mean±SD）、品質、総費用、終了状態。FONDUE 公表値は参考欄 | 方法単位の比較（R2-3） |
| **新 Table E** | 単純診断と AU 追加診断の比較（同一 checkpoint 群） | AU の増分価値 |

mean±SD、CI、subsampling interval を区別し、seed 数・データ分割・評価モードを各 caption に記す。**図中で $\hat d_{\mathrm{ID}}$ を真値のように表示しない。** 小さい $m$ ほど圧縮されることと、AU が小さいことを別の軸で示す。

既存 14 表は全件書式監査する（桁数・指数表記・括弧・±の前後・太字基準・有効数字 3 桁への統一）。

---

## 8. ファイル運用（NN 番号を MAKE 番号に合わせる）

**ユーザ指示により、NN 版の番号を MAKE 版の番号に一致させる。NN に欠番が生じてよい。**

- 現状: NN 最新 = `main_paper_NN42.tex`、MAKE 最新 = `main_paper_MAKE48.tex`。
- **今回の改訂では `main_paper_NN42.tex` を元に `main_paper_NN49.tex` を作成する（NN43〜NN48 は欠番）。**
- 英訳して `main_paper_MAKE49.tex` を作る。以後、日本語版と英語版は常に同一番号で対応させる。
- 既存ファイルは一切上書き・削除しない。
- `CLAUDE.md` の「【必須ルール1】改稿ごとに +1」を「**改稿ごとに NN と MAKE を同一の新番号に揃える。欠番を許容する**」へ更新する。

**注意**: NN42 は MAKE48 の内容を反映していない（MAKE47→48 は英語表現と書誌情報のみの修正で日本語版を起こさなかった）。NN49 を作る際は、**MAKE48 で行った書誌修正を日本語版にも反映**すること。

### 作業手順

1. `main_paper_NN42.tex` → `main_paper_NN49.tex` を作成し、MAKE48 の書誌修正を取り込む。
2. 本方針に基づき日本語版を改訂する。
3. 追加実験 E7a → E7b/E1 → E2/E4/E3/E5 →（必要なら）E6 の順に実行し、`results/tables/`・`results/figures/` に保存する（§10）。
4. 結果を NN49 に反映する。
5. `main_paper_NN49.tex` を英訳して `main_paper_MAKE49.tex` を作成する（翻訳規約は `CLAUDE.md` に従う）。
6. `platex ×2 + dvipdfmx`（NN）、`pdflatex ×3`（MAKE）でコンパイルし、エラー 0・未解決引用 0 を確認する。
7. `MAKE49_Response_to_Reviewers.md`（→ PDF）と変更箇所ハイライト版を用意する。
8. 補足資料を更新する（新実験スクリプト・ログ・環境情報・データ分割インデックス）。

---

## 9. 返答書の作り方

### 9.1 各項目を同じ形式で回答する

指摘全文または要点 → 著者の回答 → 具体的な修正 → 改訂稿のページ・行 → 追加証拠。**英語返答書は、実験と改訂が完了してから過去形で書く。**

新規性の項目なら次の順序。

1. 会議論文との差分が明瞭でなかったことを認める。
2. 既出の着想を明示し、独立した選択・剪定研究（FONDUE 等）の議論を追加したと説明する。
3. 新しく評価した問いと比較法を示す。
4. **実測で支持された結論と、支持されず縮小した主張を示す。**
5. 差分表・結果表・改訂箇所へ対応づける。

**不足する返答の例**: 「既に conditional と書いている」「補足資料にある」「結果は理論と整合的」。本文のどこが明確になり、何が新しい証拠になったかを示す。

### 9.2 同意しきらなくてよい指摘

- **新アーキテクチャがないこと**: 実証的な選択手順として価値を示す。ただし比較不足は認める。
- **学習曲線は Accuracy にすべき**: 本研究の学習課題に適した MSE を用い、Accuracy は下流評価で報告する。
- **毎 epoch の test 曲線**: test 隔離を保つ評価方法を説明し、必要なら固定後の事後図として示す。
- **指定された MST 論文**: 研究目的が異なることを丁寧に説明し、関連の強い古典的手法を補う。
- **導入の結論をすべて削除**: 詳細な結果は移動するが、簡潔な貢献説明は残す。

反論は、査読者を否定する形ではなく、**研究目的に照らした代替対応と証拠をセット**にする。

### 9.3 Editor へのカバーレター

英文・体裁の改善より先に、**先行研究との差分の再整理、独立した検証設計、直接比較法の追加、理論・診断の過大な主張の修正**を述べる。各 Reviewer への詳細回答は返答書に委ねる。

追加実験が現行の主張を支持しなかった場合、そのことを隠して「全コメントに完全に対応した」としない。本文の主張をどのように縮小したかを説明する。

**改訂期限**: 追加実験が期限内に終わらない場合は、必要な実験内容と見積もりを確定してから Editor へ延長依頼を検討する。延長を依頼せず未完了の実験を実施済みと記載したり、すべてを future work へ送ったりしない。

---

## 10. 実施順序

日程は確約しない。依存関係に沿って進める。E7 を主軸にするので、主要な仮説を早く評価できる順にする。

| 段階 | 作業 | 完了条件 |
|---|---|---|
| 0 | ~~NOLTA2025 本体・FONDUE 本文・公開コードの照合~~ | **完了（2026-09-12）。§3 と §13 に反映済み** |
| 1 | ~~先行研究・書誌・実装の監査~~ | **完了（2026-09-12）。`MAKE49_段階1_監査結果.md` に記録。P0 級 3 件（A1 実効 β=β/m、A2 AU の m 依存性は A1 の副作用、A3 Figure 4 のキャプション不整合）を新規に確定。B6a/B6b の再実装条件は確定したが、A1 により提案法側も再実行が必要** |
| 2 | ~~**一つの実験設定表**に確定~~ | **完了（2026-09-12）。`MAKE49_実験設定表.md`（事前登録）＋ `config/experiment_config_MAKE49.json`。整合は `src/audit/check_config_consistency.py` が全 17 項目で検証済み。AU 主方式＝方式 2、E7a＝5 vs 300 epochs、H2 主比較＝active+mixed、keep_mixed=True、$\epsilon_D$＝max(10%·$D_\mathrm{anchor}$, 3SD, $10^{-3}$Var)、予算＝12 ラン＋終了状態 4 種。$\beta$ 主設定＝1 を新規に確定。GPU 実時間上限のみ段階 3 冒頭の校正で確定** |
| 3 | ~~校正実行 → dSprites と MNIST で E7a、E2 の代表試行~~ | **完了（2026-09-12）。`MAKE49_段階3_校正結果.md` と `MAKE49_段階3_E7a_E2結果.md`。GPU 実時間上限を確定（MNIST 0.41h / dSprites 1.43h / CIFAR-10 0.81h）。dSprites の崩壊は尤度が原因と特定し Bernoulli に変更。E7a: H1 は個数読み取り A・B で部分的に支持、連続量 C は逆向きの条件あり、**H2（AU が変数型分類より安定）は支持する条件ゼロ**。E2: 全 30 ラン中 plateau 0 件で 300 epochs を収束と呼べない** |
| 4 | ~~E7b と E1、+Q 対照の主比較~~ | **完了（2026-09-13）。`MAKE49_段階4_主比較結果.md`。MNIST・dSprites の 2 データ × 13 方法 × 5 seeds。**提案法は MNIST で最も高価（715s、FONDUE の 17.3 倍）で品質でも劣り、dSprites では `ID_UNRELIABLE` で適用を見送った。ID を使わない昇順走査+Q が MNIST で同一の選択・品質を 3.93 分の 1 の費用で達成し、dSprites では提案法が諦めた条件で $m=20$ を発見。**ID 誘導の増分価値は両データで観測されず。** 一方 $+Q$ は FONDUE 系の品質を大きく改善（dSprites 0.00550→0.00126）。B7/B8 は未実施、FONDUE 公表値の再現も未達で主比較の不足として明記** |
| 5 | ~~E4 の診断増分、E3 の代表的参照依存、E5 の自然画像~~ | **完了（2026-09-13）。`MAKE49_段階5_結果.md`。E3: $\hat d_\mathrm{ID}$ は表現で 1.7〜3.7 倍動き、推定器一致は標本数で反転。E4: AU の判定は $\delta$ で反転（CIFAR-10）、V1/V2 は 3 データで機能せず、$c=2$ は dSprites で失敗。**AU の方式2 は事前登録では「価値あり」だが雑音 3 本と区別できず、検定に検出力が無かった**。E5: 提案法は CIFAR-10 でも `ID_UNRELIABLE`、成功は MNIST のみ。ID なしの昇順走査が 3 データで同等以上・より安価** |
| 7 | **追加作業（著者判断 2026-09-14）: B7・B8 の追加、β 梯子全体での再評価、枠組み変更** | **完了（2026-09-17）。`MAKE49_段階7_B7B8とβ梯子.md`。1,215 本＋200 件。**GECO の再構成制約が本研究の品質条件 $Q$ と同一であることが判明し、$Q$ を貢献から取り下げ**。ARD-VAE は β 梯子 5 段すべてで同一値を返す唯一の閾値なし手法。AU の δ 依存は β 全域で成立 |
| 6 | ~~必要な E6 と文章・図表・返答書~~ | **部分完了（2026-09-13）。`MAKE49_段階6_結果.md`。E6: 標準化すると AU が $d_\mathrm{emb}=3$ に達し、**原稿のトーラス異常（AU=2）は前処理の人工物**と判明（原稿に有利な訂正）。返答書の日本語作業版と原稿再構成仕様を作成、図表 5 点を生成。**`main_paper_NN49/MAKE49.tex` の執筆は未着手**——枠組みを「方法提案」から「実証研究」へ変えるかが著者判断のため** |

E7 の試行が期待どおりでなかった場合も、正しい先行研究整理、主要比較、test 分離、表・理論の修正は必要である。
一方、主張を支えない結果が出たまま実験の種類だけを増やす必要はない。

**FONDUE の実装監査**（段階 1）:
公開実装は `https://github.com/bonheml/VAE_learning_dynamics`（TensorFlow 2 / Keras、`vae_ld/learning_dynamics/fondue.py`）。
**監査は commit `f6d6e66b536bcbe635392d3438338949d4dc8730` に固定して行う。**
主比較では、論文の記述仕様に基づく閾値（20% 規則）と変数分類（単一指数化）を事前に固定する。
既定設定、公表実験時の設定、再実装で採用する設定を区別して記載する。
現行コードの二重指数化は補助的な再現性確認として扱い、公表実験の結果や結論が誤りであるとの根拠にはしない。
本プロジェクトは PyTorch なので Algorithm 1・3 を再実装する。妥当性は **dSprites で $12.2\pm0.4$ に近い数字が出ることだけで判定しない**。
結果をその値に合わせるチューニングも行わない。確認するもの:

- 元コードの版、アーキテクチャ、前処理、loss、β、optimizer（既定: MLE 推定器、`max_epochs: 1`、`threshold: 1`）。
- ID 推定器、推定標本数（`ide_batch_size: 10000`）、丸め方。
- active / mixed / passive の定義、閾値、`keep_mixed`（U12, U15）。
- 探索開始点、倍増・二分探索、停止・上限、memoisation。
- 閾値 $t$ の単位（U13）。本文・擬似コード・コードの不一致を明記し、使用した値を記す。
- 分散の指数化回数（U14）。
- 同じ入力統計に対して原著実装と再実装が同じ判定・探索経路を返すか。

異なるフレームワークで公表 mean±SD と完全一致しないことだけで再実装を失敗とみなさない。
原著実装、忠実な移植、条件を変更した評価を区別して報告する。
再現性を確認できなかった場合、公表値だけを主比較へ移しても Reviewer 2 の公平な直接比較への回答にはならない。
**公表値は参考欄にとどめ、主比較の不足は残ることを明記する。**

---

## 11. 結果別の改訂方針

| 追加検証の結果 | 残せる主張・必要な変更 |
|---|---|
| 複数データで品質を保ち、FONDUE 等を含め総費用が改善 | 実用的な ID-guided 選択手順として主張。効果のある条件を明示 |
| 品質は保てるが FONDUE 等より高コスト | **最速という主張を削除。** 役割分担として提示する。診断・適用見送り・頑健性の追加価値は、+Q 対照と「AU 診断の有用性の定義」（§5.5）に基づいて示す。トーラスの既存結果を反例として使わない |
| E7a で長時間学習でも活性読み取りが不安定 | 「安定した次元読み取り」という主張は支持されない。**AU を主要貢献から外す**。早期学習の知見が長時間学習でも成り立つことを報告する（実証的分析自体の価値は残る） |
| E7a で安定化するが、+Q 対照で AU の増分価値が出ない | AU を補助分析へ移す。$Q$ の追加が既存法にも同様に効くことを開示する |
| E7b で FONDUE Algorithm 1 と同じ次元を返す | 「別経路で同じ答に到達した」という三角測量として提示。**独立貢献としては弱い**ので、安定性と品質の関係（E7a）に重心を移す |
| ID なしの粗い探索と同程度 | **ID が必須という主張を弱める。** 推定費用に見合う条件を限定 |
| AU を加えても品質診断が改善しない | **AU を補助分析へ移し、主要な方法貢献から外す** |
| 自然画像では一貫して適用を見送る | 自然画像への有効性を主張しない。適用範囲の狭さが残ることを認める |
| 既存法に対する利点が示せず、会議論文との差分も小さい | **現行の方法提案としての採録根拠は弱い。** 診断研究への再構成または追加研究が必要 |

「全データで AU 条件を通過させる」「既存の結論を維持する」を成功条件にしない。**独立評価で支持された結論を、適切な強さで提示すること**を成功条件とする。採録確率を数値で推定する材料はなく、修正による採録の保証もできない。

---

## 12. 提出前チェックリスト

**新規性・比較**
- [x] NOLTA2025 本体と比較し、既出と追加の内容を区別した（§3.4）
- [x] FONDUE の主手法（Algorithm 1）と付録 G の Algorithm 3 を確認し、差分を整理した（§3.2–3.3）
- [ ] **本文 Related Work で FONDUE 付録 G の Algorithm 3 との共通性を先に一文で明示し、その後に差（§3.2 の 5 軸）を述べた**
- [ ] **FONDUE 著者が Algorithm 3 を早期学習条件で退けた理由を引用し、本改訂がそれを学習時間と統計量の軸で拡張検証したと述べた（反証と書かない）**
- [ ] E7a を同一 checkpoint 系列で実施し、読み取り A/B/C の SD を epoch 別に報告した
- [ ] E7b を方法単位で実施し、選択 $m$ の SD を FONDUE Table 7 と同じ推定対象で並べた（固定 $m$ の AU の SD と混同していない）
- [ ] dSprites を共通ベンチマークとして追加し、主比較には共通環境の再実行値を使い、公表値は参考欄にとどめた
- [ ] FONDUE 再実装の監査項目（§10）を記録した。閾値 $t$ の単位と分散の指数化回数を明記した
- [ ] +Q 対照（B6a+Q, B6b+Q, B4+Q, 本手法 −AU）を実施し、$Q$ の効果と ID/AU の増分価値を分離した
- [ ] AU 診断の有用性を、事前指定した**一つの**定義（§5.5）で評価した
- [ ] 「本手法 −AU」対照が選択 $m$ と品質を変えないことを確認し、AU の記録だけの手順を AU による探索改善のように説明していない
- [ ] 品質回復の評価で、介入前に固定した $T_D$ を用い、介入後に anchor を作り直していない
- [ ] E7a で seed 間の安定性と学習中の安定性を分けて報告し、A/B/C の生の SD を一列で順位づけしていない
- [ ] FONDUE 再実装で「記述仕様に基づく再実装」「固定 commit のコード経路」「公表値」を区別して表記した
- [ ] 主比較の FONDUE 閾値（20% 規則）と keep_mixed を結果を見る前に固定した記録がある
- [ ] 予算超過で正常終了しなかった結果を選択成功の平均から除いていない
- [ ] 「反証」「唯一」「収束済み」「完全に新規」「分散が消える」を本文・返答書から除いた
- [ ] NOLTA 公刊版と添付版の一致を確認し、差分表を 3 列（会議版・MAKE47 既存・MAKE49 追加）で示した
- [ ] 回収候補の文献を原典で書誌確認した。NOLTA [14][17][18] の誤りを引き継いでいない
- [ ] Rolinek et al. CVPR 2019 を線形代理の限定した解釈を補う引用として用いた
- [ ] N1–N3 を「条件の異なる観測」として記述し、「追試」「一致」と書いていない（§3.5）
- [ ] Whitney 上界を背景引用にとどめ、「新規命題」「唯一の自然な区間」「定理から導出して設定」と書いていない

**検証設計**
- [ ] train/validation/test の役割を分離し、選択に test を使用していない
- [ ] 再構成・ELBO・下流評価の選択目的を区別した
- [ ] AU 条件の適合だけを品質保証・原因診断として扱っていない
- [ ] ID 推定・参照学習・追加確認・再探索を総費用に含めた
- [ ] 全探索の候補集合と比較分母が一致している
- [ ] 自然画像の Step 1 を独立した参照モデルで確認した
- [ ] 閾値を評価対象の test に合わせ直していない
- [ ] 主要結果に学習 seed のばらつきを示した

**理論・整合性**
- [ ] Table 4 の elbow 断言（"exactly"）を削除した
- [ ] Remark 2 の有限 $\delta$ との混同を修正した
- [ ] Appendix の Jacobian rank 主張と未証明の上界を削除した
- [ ] $\tau^2_{\mathrm{eff}}$ の循環説明を削除または明示的に限定した
- [ ] 損失の reduction、人工データの出力層、CUDA 等の環境をコード・ログと照合した
- [ ] 図表を元ログから再生成し、本文中の数値も一致させた

**体裁**
- [ ] Abstract が 200 語以内かつ記号なし
- [ ] Introduction の前方参照が 5 か所以内
- [ ] "conditional" の出現が 10 回以下
- [ ] 全表の数値書式が統一されている（有効数字 3 桁、指数表記の統一）
- [ ] Algorithm 1 と閾値一覧表が入っている
- [ ] 参考文献の書誌・DOI・URL を統一した（全件）
- [ ] 新規引用がすべて本文中で参照されている

**ファイル運用**
- [ ] `main_paper_NN49.tex` と `main_paper_MAKE49.tex` の番号が一致している
- [ ] MAKE48 の書誌修正が NN49 に反映されている
- [ ] `CLAUDE.md` の番号規約を更新した

**回答**
- [ ] Reviewer 1 の 10 項目、Reviewer 2 の 4 論点、Reviewer 3 の 7 項目に漏れなく回答した

---

## 13. 未確認事項（実験前に必ず一次確認）

| # | 事項 | 確認方法 | 影響 |
|---|---|---|---|
| ~~U1~~ | ~~FONDUE 付録の Algorithm 3~~ | **解決（2026-09-12）。** 正しくは**付録 G の Algorithm 3（FONDUE-VAR）**。`l ← 2 * data_ide` から開始し、passive/mixed が出るまで 2 倍、活性変数数を返す。**著者自身が学習初期の不安定性を理由に不採用としている**（§3.2–3.3） | 対応済 |
| ~~U2~~ | ~~NOLTA2025 の本体内容~~ | **解決（2026-09-12）。** MNIST 32×32・クラス別 VAE・240 モデル・$D\approx M$ 推奨・AU は一切扱わず（§3.4） | 対応済 |
| ~~U10~~ | ~~FONDUE の査読付き採録の有無~~ | **解決（2026-09-12）。arXiv:2209.12806 は v1 のみ、Comments 欄・Journal reference 欄ともになし。DOI は arXiv の DataCite DOI のみ。OpenReview に投稿記録 `x9S5kdaQkkY` はあるが採録を示す記載は確認できず** | **preprint として引用する。ただし査読を経ていないことを論拠に内容を軽んじる書き方はしない** |
| U11 | FONDUE 公開実装（TF2/Keras）の PyTorch 再実装の妥当性 | §10 の監査項目（同じ入力統計に対する判定・探索経路の一致など）。公表値との数値一致だけで判定しない | 再現性を確認できない場合、公表値は参考欄にとどめ、**主比較の不足が残ることを明記する** |
| ~~U12~~ | ~~変数型の定義~~ | **現行コードの定義については解決**（2026-09-12、commit `f6d6e66b` の `vae_ld/learning_dynamics/variable_filter.py` で確認。**Table 7 の実験が同じ版・同じ設定だったことは未確認**）。 分散層の出力 $\sigma^2_k(x)$ をデータ方向に集約し、**passive** = データ間分散 $<0.1$ かつ平均 $\in[0.9,1.1]$、**active** = データ間分散 $<0.1$ かつ平均 $\le 0.1$、**mixed** = それ以外。閾値は `var_threshold=0.1`, `mean_error_range=0.1` | E7a の読み取り A はこの定義で実装する |
| **U13** | **FONDUE の閾値 $t$ の実体** | 現行コード（commit `f6d6e66b`）の `fondue()` は `cfg.threshold` を ID 差に対する**絶対閾値**として設定から受け取る。既定値は `threshold: 1`（`data_ide: 11`）だが、**これは論文実験時の設定値を確定する資料ではない**。本文の「データ ID の 20%」は $0.2\times$ データ ID を絶対閾値として渡せば実装できる。Algorithm 1 の `t×p/100` 表記と本文の単位の整合は要確認 | **主比較は本文記述に沿う 20% 規則**を採用し、閾値は**初期に推定したデータ ID の 20% として固定**、探索中の容量 $m$ に応じて変えない。絶対差 1 は感度・再現性確認として別に示す。**どちらを主比較にするかは結果を見る前に固定し、都合のよい方を選ぶ余地を残さない。** 本文・擬似コードの表記差と、公表実験時の設定ファイルの未確認を分けて記載する |
| **U14** | **FONDUE-VAR コード経路の二重指数化** | commit `f6d6e66b` で確認: `get_encoder_latents_activations` が log variance を指数化して variance にし、`train_model_and_get_var_types` がそれを `filter_variables` に渡し、そこでも入力を指数化する。合成 encoder 出力（座標分散 0.01 / 1.0 / 0.5）で経路を検証したところ、log variance を直接渡せば active/mixed/passive = 1/1/1、公開コード経路では 0/2/1 となり、**このcommit の当該経路では意図した分散と異なる値が分類されることを確認**（学習実験ではなく既知入力に対するデータ経路の確認） | 3 区分を固定する: (a) **記述仕様に基づく再実装**（$\sigma^2$ を一度だけ生成し定義どおり分類）→ 方法間の主比較、(b) **固定 commit の公開コード経路**（二重指数化を含む）→ 再現性・実装監査の補助資料、(c) **Table 7 の公表値**（版・設定未確認）→ 歴史的な参考結果。(a) を「原著コードの完全再現」と呼ばない。**二重指数化版を全データ・全 epoch・全 seed で学習する必要はない**。代表条件で影響を確認し、主要結論に影響する範囲だけ補助評価する。この疑義から Table 7 や原著の早期学習の結論が誤りだとは言わない（keep_mixed=True なら mixed 数を含む非ゼロ出力が可能で、数値だけから結論できない）。この疑義を本研究の新規性の中心に置かない |
| **U15** | **`fondue_var_type` の `keep_mixed` 指定方法** | コードは `cfg.threshold is True` / `is False` で分岐しており、数値の `threshold` を渡すと**どちらの return も走らず正常な終了条件を満たさない**。`var_type` モードは `threshold=true|false` を渡す前提 | 再実装では `keep_mixed` を独立の Boolean 引数にする。**付録 G は両分岐を説明しているので、本文だけから Table 7 がどちらを使ったかは一意に決めない**。実験設定・ログで特定できなければ未確認と記し、主比較の設定を事前に固定して代表条件で他方を感度評価する。全比較法に最大容量・最大時間と終了状態を用意し、**予算に達して正常終了しなかった結果を選択成功の平均から黙って除かない** |
| **U16** | FONDUE Algorithm 1 の既定推定器・エポック | コード確認済み（commit `f6d6e66b`）: 設定ファイルの既定は `fondue_estimator: MLE`、`max_epochs: 1`、`steps_per_epoch: null`、memoisation あり | **コードの既定値と論文中の実験設定を分ける**。FONDUE 論文はデータ・アーキテクチャにより異なる epoch 数（1〜9）を報告しているため、全データの再現設定を無条件に 1 epoch へ統一しない |
| ~~U3~~ | ~~損失関数の reduction 規約~~ | **解決（2026-09-12）＝P0 の問題として確定（A1）。** `src/models/vae.py:118-120` で再構成項は per-sample SSE、KL 項は `torch.mean` によりバッチと**潜在次元の両方**で平均。よって $\mathrm{KL}_\text{実装}=\mathrm{KL}_\text{標準}/m$、**実効 $\beta=\beta/m$**。同一規約が VAE 実験 12 ファイル全てに存在し、正しい形式は 0 ファイル | **原稿の $\beta$ は文献と比較不能。m 掃引に交絡。全 VAE 実験の再実行が必要**（→ 実験設定表 §2, §2.5） |
| ~~U4~~ | ~~人工データの出力層と正規化~~ | **解決（2026-09-12）。Sigmoid との不整合は存在しない**（`ae.py:50`・`vae.py:55` のデコーダ最終層は `nn.Linear`）。**別の問題を確認**: 合成データが未標準化かつ異方的（Swiss Roll の座標別 std [6.57, **0.29**, 6.99]、全分散 31.4 に対し Torus は 3.3） | MSE を多様体間で比較できない。高さ座標は全分散の 0.2% で、$m\ge d_\mathrm{emb}$ の MSE による裏づけが弱い。$\delta=0.01$ は絶対閾値でスケール依存。**標準化して Table 4 を再計算** |
| ~~U5~~ | ~~実験時の CUDA / cuDNN / driver バージョン~~ | **解決（2026-09-12）＝記録なしと確定。** 環境情報は `supplementary_MAKE46/ENVIRONMENT.md` のみで `PyTorch 2.0`／`RTX 3070` だけ。同ファイル 24-31 行の投稿前 TODO が未処理。`requirements_freeze.txt` は存在せず、`results/tables/*.json` 全 47 件に環境キー 0 件 | **原稿 `:1188-1192` の「library and framework versions」は実態を上回る記述。** 既存結果の環境は復元不能。再実行時の自動記録を設定ファイルに規定（→ 実験設定表 §7） |
| ~~U6~~ | ~~Figure 4 右パネルの対象~~ | **解決（2026-09-12）＝P0 の不整合として確定（A3）。実際は VAE**（`EP_twonn_stability.py:134-135` が `E4_extended.json` の `vae_results` を読む。注釈座標 `xy=(64, 9.16)` は VAE の 9.155 と一致、AE の 9.537 とは不一致） | キャプション・本文は横軸を $m_\mathrm{ref}$（参照 AE）と記述し、参照 AE の設計根拠として引用している。**併せて「$m_\mathrm{ref}\ge 48$」はグリッド非存在値、$\kappa\approx10^9$ は `:150` のハードコード定数で Table 12 の $4.1\times10^7$ と桁が不一致** |
| ~~U7~~ | ~~Table 9 / Table 11 の MSE が平均か単一 seed か~~ | **解決（2026-09-12）＝問題なし。** Table 9 は 3 seed（42,123,777）平均、Table 11 は左（AE）単一 seed 42・右（VAE）3 seed 平均の混在だが、キャプションと本文 `:1147-1150` が明記済み。全行が元 JSON と一致 | **追加対応不要。** 任意の改善として Table 9 の MSE 列への std 併記（m=8 で 0.00026） |
| ~~U8~~ | ~~`mdpi.cls` で `algorithm` 環境が使えるか~~ | **解決（2026-09-12、テストコンパイル済み）。** 外部パッケージ `algorithm` / `algorithmic` / `algorithmicx` / `algpseudocode` / `algorithm2e` は**本機の TeX Live に未導入**（`kpsewhich` で全て不在）。一方 **`mdpi.cls` 自身が `Algorithm` 環境を提供**（`Definitions/mdpi.cls:262-264` で `algorithm` カウンタと `\newtheorem{Algorithm}` を定義、`:1467` で付録は A1, A2 に改番）。テストでは `Algorithm 1.` として正しく組版された | **クラス標準の `Algorithm` 環境を使い、行番号は手書きする**のが第一候補。整形された擬似コード体裁が必要な場合のみ `algorithms`／`algorithmicx` の導入を検討する（投稿先の許容パッケージを要確認） |
| U9 | Editor の正式判定と改訂期限 | 投稿システム | 延長申請の要否 |

~~**段階 1 で必ず解消するのは U3–U7、U10、NOLTA 公刊版との照合、および FONDUE 再実装の監査である。**~~ **完了（2026-09-12）。** U3–U7・U10 は上表のとおり解決、U2・U12–U16 は既に解決済み、NOLTA 照合は U2 で完了。公表実験時の設定は特定できなかったため「未確認」と記し、主比較の設定は `MAKE49_実験設定表.md` で事前に固定した。

**残る未確認事項は U8（`mdpi.cls` の `algorithm` 環境）と U9（Editor の正式判定と改訂期限）のみである。** U11 は再実装の妥当性であり、段階 3 の実施時に判定する。

---

## 付録: 参照した外部資料

2026-09-12 時点で一次確認したもの。文献一覧の全面監査や各比較法の実装再現は未完了。

1. **Bonheme, L.; Grzes, M.** *FONDUE: an algorithm to find the optimal dimensionality of the latent representations of variational autoencoders.* arXiv:2209.12806v1, 26 Sep 2022, 17 頁。**全文確認済み**（ローカル: `1ce549c4-6f05-4259-9fcc-791db9746a28.pdf`）。Algorithm 1（§4.3）、Algorithm 3 = FONDUE-VAR（付録 G）、Table 1・Table 7 の数値、付録 D の FC 結果まで確認。コード: `https://github.com/bonheml/VAE_learning_dynamics`。
2. **De Boom, C.; Wauthier, S.; Verbelen, T.; Dhoedt, B.** *Dynamic Narrowing of VAE Bottlenecks Using GECO and L0 Regularization.* arXiv:2003.10901.
3. **Saha, S.; Joshi, S.; Whitaker, R.** *ARD-VAE: A Statistical Formulation to Find the Relevant Latent Dimensions of Variational Autoencoders.* arXiv:2501.10901, 2025.
4. **Iyer, K.; Bhalodia, R.; Elhabian, S.** *RENs: Relevance Encoding Networks.* arXiv:2205.13061, 2022.
5. **Costa, J.A.; Hero, A.O.** *Geodesic Entropic Graphs for Dimension and Entropy Estimation in Manifold Learning.* IEEE Trans. Signal Process. **2004**, 52(8), 2210–2221. DOI 10.1109/TSP.2004.831130.（書誌を Crossref で確認済み）
6. **Gałka, Ł.; Karczmarek, P.; Tokovarov, M.** *Isolation Forest Based on Minimal Spanning Tree.* IEEE Access **2022**, 10, 74175–74186. DOI 10.1109/ACCESS.2022.3190505.（書誌を Crossref で確認済み。主題は異常検知）
7. **Obata, C.; Jin'no, K.** *Analysis of the Manifold Hypothesis and Latent Space Capacity in Variational Autoencoders.* NOLTA2025, 5 頁。**全文確認済み**（ローカル: `nolta2025_eng (4).pdf`）。MNIST 32×32・クラス別 VAE・6 アーキテクチャ・240 モデル・TwoNN/PCA broken-stick・$D_0/D_1/D_2$ の 3 水準・$D \approx M$ 推奨。**AU は扱っていない。**
9. **Rolinek, M.; Zietlow, D.; Martius, G.** *Variational Autoencoders Pursue PCA Directions (by Accident).* CVPR 2019.（線形ガウス代理の限定した解釈を補う引用。要追加）
10. **Bonheme, L.; Grzes, M.** *Be More Active! Understanding the Differences between Mean and Sampled Representations of Variational Autoencoders.* arXiv:2109.12679 (2021); JMLR 2023.（変数型分類の定義元。E7a に必須。JMLR 版を引用）
11. **Camboulin, C.; Doimo, D.; Glielmo, A.** *Understanding Variational Autoencoders with Intrinsic Dimension and Information Imbalance.* arXiv:2411.01978, 2024.（書誌確認済み。NOLTA2025 で引用済み、MAKE47 で欠落）
12. **Loaiza-Ganem, G.** et al. *Diagnosing and Fixing Manifold Overfitting in Deep Generative Models.* TMLR 2022.（書誌は原典で再確認）
13. **Lucas, J.; Tucker, G.; Grosse, R.B.; Norouzi, M.** *Understanding Posterior Collapse in Generative Latent Variable Models.* DGS@ICLR 2019 Workshop, OpenReview r1xaVLUYuE.（NOLTA2025 [14] の "K. Xu et al." は誤記）
14. **FONDUE 公開コード** `https://github.com/bonheml/VAE_learning_dynamics`（master）: `vae_ld/learning_dynamics/fondue.py`、`variable_filter.py`、`utils.py`、`bin/config/nb_latents.yaml` を 2026-09-12 に確認（§13 U12–U16）。
15. **`MAKE49_revision_policy_review.md`**（2026-09-12）: 本方針 v2 に対する第三者レビュー。本版（v3）で反映済み。
16. 書誌不一致の確認: [arXiv:2203.05796 は CLIP ベンチマーク論文](https://arxiv.org/abs/2203.05796)、[arXiv:2106.05464 は作用素環論](https://arxiv.org/abs/2106.05464)、[Be More Active! は arXiv:2109.12679、JMLR 2023 掲載](https://arxiv.org/abs/2109.12679)。
8. **Dai, B.; Wipf, D.** *Diagnosing and Enhancing VAE Models.* arXiv:1903.05789, 2019.（背景補強候補）
