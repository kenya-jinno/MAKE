# MAKE49 査読対応方針 v3 の確認結果

確認日：2026-09-12  
対象：MAKE49_査読対応方針(1).md（本文1223行）  
比較：前回の方針と MAKE49_revision_policy_review.md

## 1. 判定

**今回の修正方針は、改訂作業の基本方針として採用してよい。前回の主要な指摘は概ね適切に反映されており、方針全体を再構成する必要はない。**

特に、FONDUE との関係を「反証」から「学習時間・統計量・品質の関係の検証」へ変更したこと、E7a / E7b の分離、+Q 対照、NOLTA2025 との差分の三段階整理、Whitney 定理の限定は妥当である。

残る作業は主に、合意した設計を実装できる仕様にすることと、古い記述の取り残しを整理することである。重要なのは次の4点。

1. **AUを記録するだけの手順と、AUを使って判断を変える手順を区別する。**
2. **FONDUEの記述仕様、公開コード、論文実験時の設定を分けて扱う。**
3. **E7の安定性を測る量と主要な比較時点を確定する。**
4. **品質回復の比較では、品質目標を途中で動かさない。**

予備実験へ進める段階にある。ただし、全条件での本実験を開始する前に、上記の仕様と主比較に用いるFONDUEの設定を固定する。

## 2. 前回の指摘への対応状況

「修正済み」は方針文書で対応したことを意味し、論文・実験が修正済みという意味ではない。

| 前回の指摘 | 今回の対応 | 判定 |
|---|---|---|
| 実験前に「反証」「分散が消える」と結論づけていた | 総括、§3.3で仮説と既存観測を分離 | 主旨は修正済み。旧表現が一部残る |
| MAKE47とFONDUE-VARを同一手順のように扱った | §3.2で初期容量・読み取り・停止・追加操作・出力を比較 | 修正済み |
| FONDUEを「成功例のみ」「品質を検討しない」と説明した | §3.3で本文の品質評価と停止規則の違いを明記 | 修正済み |
| トーラスの既存結果をFONDUEの反例とした | §3.3で実際の選択結果を共通Qで評価する方針に変更 | 修正済み |
| 固定モデルのAUのSDと選択次元のSDを混同した | E7a / E7bへ分離 | 修正済み。E7aの測定仕様を確定する |
| Qの効果とID・AUの追加価値を分離できない | B6a+Q、B6b+Q、B4+Q、−AU、単純診断対照を追加 | 適切。−AUの評価対象に注意 |
| 会議論文からの追加と分野全体の新規性を混同した | §3.4でNOLTA・MAKE47・MAKE49を区別 | 修正済み。公刊版との照合は残る |
| 自著間の異なる結果を無理に整合させた | §3.5で交絡、NOLTA Table 7、MAKE47のMSE改善を明記 | 修正済み |
| Whitney定理を実用的な容量選択の保証にした | §0.3、§4.6で背景・動機づけに限定 | 主旨は修正済み。§0.4に旧説明が残る |
| 書誌に著者・arXiv番号の誤りがあった | Lucasらへ訂正し、特定できない2文献を除外 | 方針上は修正済み |
| E7が実施順序に入っていなかった | §10をE7a→E7b/E1中心へ変更 | 修正済み |
| 公表値との一致だけで再実装を判定した | §10でコード監査、条件の区別、共通環境の評価を明記 | 修正済み。今回加わったコードの疑義は後述 |

## 3. 本実験前に確定する仕様

### 3.1 AUを記録するだけなら、−AUでも選択次元は変わらない

対象：§4.4、§5.3、E4、§5.5「AU診断の有用性の定義」。

現在のAlgorithmは、品質Qと固定した探索規則で候補を選び、AUは別に記録する。AUが探索・停止・選択の分岐に使われていないため、**同じ候補・乱数・学習結果を使えば、AUの記録を外しても、選択mと品質は変わらない**。計算時間だけはAUの算出分が変わり得る。

これは、前回こちらが提案したAlgorithmにも残っていた仕様上の曖昧さである。今回追加した−AU対照によって、評価の役割を明確にできる。

推奨する整理：

| 評価対象 | 実施する比較 | 得たい証拠 |
|---|---|---|
| IDによる候補優先 | ID+Qと、IDなし+Q、FONDUE系+Q | 同じ品質目標での探索費用・選択容量の違い |
| AUの情報としての価値 | MSE・KL等のみの診断と、それにAUを加えた診断 | 未使用条件での予測・説明の改善 |
| AUを使う行動選択の価値 | AUを使う追加確認方策と、単純な追加確認方策 | 同じ品質目標に達する追加費用の違い |

方針に既にある「追加実験の選び方」と「品質確認から独立した予測」のうち、**どちらを主要なAU評価にするか一つ決める**。両方を必須化する必要はない。

AUを診断情報として評価する場合、同じcheckpoint群を使ってよい。−AUのために同じモデルを再学習する必要はない。

AUを使ってβ調整・容量拡張などを選ぶ場合は、その規則を事前に定義し、現Algorithmとは区別して記載する。AUの記録だけの手順に対して、AUによる探索改善が実装済みであるように説明しない。

**+Q対照の追加だけでAUの増分価値まで自動的に評価できるわけではない。** 総括の4番は、「+Q対照で品質確認の効果を分離し、別途AUの診断上の増分価値を測る」とすると正確である。

### 3.2 E7aの「安定性」を具体化する

E7a / E7bの分離は適切である。実験設定表に次を加えれば実装しやすい。

| 決める項目 | 推奨する扱い |
|---|---|
| 読み取りAの比較対象 | active数とactive+mixed数を区別する。H2で主に比較する方を事前指定し、他方を補助表示 |
| 読み取りB | δを固定したAU。δの感度評価は別にする |
| 読み取りC | IDE_z−IDE_μの経時変化・seed依存性を表示。A/Bとは別パネルにする |
| H1の主要比較 | 例えば5 epochsと300 epochsなど、早期と後期の比較を事前指定。1・2・5・50・300の全軌跡は併記 |
| seed間の安定性 | 同じepoch・同じ容量でのseed間SDと各seedの値 |
| 学習中の安定性 | 同じseed内で、後期checkpoint間の値がどれだけ動くか |
| 退化の扱い | AU=0、AU=mなども品質と併記。SDの小ささだけで成功としない |

active数・AUは閾値で離散化した個数、ID差は連続量である。**A/B/Cの生のSDだけを一列に並べて、最小のものが最良と順位づけしない。** 読み取りCは、それ自身の閾値に対する余裕や判定変化として見ることもできる。

seed間SDが小さいことと、学習後期に読み取りが変化しないことも異なる。既存checkpointから確認できるので、大規模な追加学習を必要とする指摘ではない。

### 3.3 品質回復を測る間は、Qの目標を固定する

対象：§4.2、+Q対照、AUを用いたβ調整・容量拡張の評価。

同じ比較条件の下では、品質目標

$$
T_D=D_{\mathrm{anchor}}+\epsilon_D
$$

を共通にして、

$$
Q(m):D_{\mathrm{val}}(m)\le T_D
$$

を判定することを明記する。

特に、AU診断に基づいてβを変える際、**変更後のβでanchorを作り直し、より緩い目標に変更した結果を「品質回復」と数えない**。品質回復の評価では、変更前に固定した目標に対して達成したかを見る。

方法ごとの目的関数や事前分布が異なっていても、主比較で「同じ品質を達成した」と述べるなら、共通の品質尺度と目標が必要である。方法ごとに相対的なanchor基準を使う分析を追加する場合は、共通品質目標での比較と分ける。

前処理、MSEのreduction、決定論的再構成か事後サンプルに基づく再構成かも固定する。ここは方針に既に書かれている内容を、設定ファイルへ落とし込む段階である。

## 4. 新たに追加されたFONDUE公開コードの確認

今回、公開リポジトリの次のcommitを確認し、4ファイルが取得時のmasterの内容と一致することを確認した。

**commit：f6d6e66b536bcbe635392d3438338949d4dc8730**

確認したのは、fondue.py、variable_filter.py、utils.py、nb_latents.yaml、呼び出し元のbin/fondue。モデルの学習・論文の数値再現は行っていない。二重指数化については、合成したencoder出力を使って関数間のデータ経路を検証した。

### 4.1 U12：変数型分類の説明は現行コードと一致する

分散のデータ間分散が0.1未満で、平均が0.1以下ならactive、平均が[0.9,1.1]内ならpassive、その他をmixedとする説明は、確認したコードの既定設定と一致する。

ただし、**「現行コードの定義を確認済み」と「Table 7の実験でも同じ版・同じ設定だったことを確認済み」は区別する**。U12の解決範囲を前者に限定する。[変数分類の実装](https://github.com/bonheml/VAE_learning_dynamics/blob/f6d6e66b536bcbe635392d3438338949d4dc8730/vae_ld/learning_dynamics/variable_filter.py)

### 4.2 U13：「本文・擬似コード・コードの3者が不一致」は強すぎる

確認できた事実：

- fondue関数はcfg.thresholdをID差に対する絶対閾値として使う。
- 設定ファイルの既定値はthreshold: 1、data_ide: 11である。
- 論文本文はデータIDの20%と説明する。
- 添付論文のAlgorithm 1にはt×p/100という表記があり、本文との単位の整合を確認する必要がある。

一方、**絶対閾値を引数で渡す実装は、事前に0.2×データIDを計算して設定する運用と両立する**。既定値1がそのまま公表実験で使われたとも限らない。したがって、コードが必ず論文の20%規則と矛盾する、とはいえない。[探索関数](https://github.com/bonheml/VAE_learning_dynamics/blob/f6d6e66b536bcbe635392d3438338949d4dc8730/vae_ld/learning_dynamics/fondue.py)、[既定設定](https://github.com/bonheml/VAE_learning_dynamics/blob/f6d6e66b536bcbe635392d3438338949d4dc8730/bin/config/nb_latents.yaml)

差し替え案：

> 現行コードはID差に対する絶対閾値を設定から受け取る。既定値は1だが、これは論文実験時の設定値を確定する資料ではない。本文の20%規則は、0.2×データIDを絶対閾値として渡すことで実装できる。本文・擬似コードの表記差と、公表実験時の設定ファイルの未確認を分けて記載する。

主比較は、本文記述に沿う20%規則を採用する案が妥当である。その場合、閾値は**初期に推定したデータIDの20%として固定**し、探索中の容量mに応じて変更しない。絶対差1は感度・再現性確認として別に示す。

どちらを主比較に使うかは結果を見る前に固定する。「両方を走らせて、都合のよい方を主結果にする」余地を残さない。

### 4.3 U14：二重指数化の疑義にはコード上の根拠がある

確認したデータ経路は、

1. get_encoder_latents_activationsでlog varianceを指数化してvarianceにする。
2. train_model_and_get_var_typesがその値をfilter_variablesへ渡す。
3. filter_variablesでも入力を指数化する。

という構成である。公開の呼び出し元も、var_typeモードでこのfilter_variablesを渡している。[潜在表現の取得](https://github.com/bonheml/VAE_learning_dynamics/blob/f6d6e66b536bcbe635392d3438338949d4dc8730/vae_ld/learning_dynamics/utils.py)、[呼び出し元](https://github.com/bonheml/VAE_learning_dynamics/blob/f6d6e66b536bcbe635392d3438338949d4dc8730/bin/fondue)

さらに、各座標の分散がデータ間で一定の0.01、1.0、0.5となる合成encoder出力を使って、取得した関数の経路を確認した。

| 入力の扱い | active | mixed | passive |
|---|---:|---:|---:|
| log varianceを直接分類関数へ渡す | 1 | 1 | 1 |
| 確認した公開コードの取得・分類経路を通す | 0 | 2 | 1 |

したがって、**このcommitの当該経路では、意図した分散と異なる値が分類されることを確認できた**。これは学習実験ではなく、既知の入力に対するデータ経路の確認である。

ただし、この事実からTable 7が誤っている、原著の早期学習での結論が無効である、とまでは言えない。公表実験時のコード版と設定が未確認だからである。

また、「Table 7の数値はこの解釈と整合しない」と一括するのも避ける。二重指数化ではactive判定が成立しなくなるため、keep_mixed=Falseなら非ゼロの選択値と整合しにくい。一方、keep_mixed=Trueならmixed数を含む非ゼロの出力が可能であり、**どちらを使ったか不明な段階では数値だけから結論できない**。

### 4.4 疑義を比較実験でどう扱うか

次の3区分を固定する。

| 区分 | 内容 | 用途 |
|---|---|---|
| 記述仕様に基づく再実装 | σ²を一度だけ生成し、定義した変数型で分類 | 方法間の主比較 |
| 固定commitの公開コード経路 | 二重指数化を含む確認済みの経路 | 再現性・実装監査の補助資料 |
| 論文Table 7の公表値 | 実験時の版・設定は未確認 | 歴史的な参考結果 |

単一指数化版は「原著の記述仕様に基づく再実装」と明記する。公開コードと異なる処理を入れた版を、説明なしに「原著コードの完全再現」と呼ばない。

**二重指数化版を全データ・全epoch・全seedで学習する必要はない。** まず今回のような最小の入力検証と代表条件で影響を確認し、主要結論に影響する範囲だけ補助評価する。この疑義を本研究の新規性の中心へ移す必要もない。

### 4.5 U15・U16：残すべき未確認事項

U15の、数値thresholdを渡すとis True / is Falseのどちらにも入らず、正常な終了条件を満たさないという指摘はコードと一致する。keep_mixedを独立のBoolean引数とする修正は妥当である。

ただし、**Appendix Gはkeep_mixedの両分岐を説明しているので、本文だけからTable 7がどちらを使ったか一意に決めない**。実験設定・ログで特定できなければ未確認と記し、主比較の設定を事前に固定して、必要な代表条件で他方を感度評価する。

U16のMLE、1 epoch等は確認した設定ファイルの既定値として正しい。ただし、FONDUE論文自身がデータやarchitectureにより異なるepoch数を報告しているため、**全データの再現設定を無条件に1 epochへ統一しない**。コードの既定値と論文中の実験設定を分ける。

全比較法に最大容量・最大時間と終了状態を用意する。正常終了せず予算に達した結果を、選択成功の平均から黙って除かない。

## 5. 残っている旧記述・内部不整合

以下は主に文章の修正で解消できる。行番号は今回の添付MDの行番号であり、論文PDFの行番号ではない。

| 箇所 | 残る記述 | 修正 |
|---|---|---|
| §0.1 S1、66行 | 「本論文に有利な材料」 | 「追加検証課題を具体化する先行結果」 |
| §3冒頭、319–320行 | 「きわめて有利な材料」「新規性…立て直せる」 | 「共通性と追加検証課題を整理する。貢献は比較結果で判断する」 |
| §0.4、111行 | 「崩れるのは係数2の部分のみ」 | 「係数2にも実用的な品質保証は与えず、幾何学的動機を補う」 |
| §3.6 Rolinek、489行／付録、1215行 | water-fillingの「経験的裏付け」、理論への「直接の応答」 | §4.6と同じ「限定した解釈を補う引用」へ統一 |
| §3.6 Costa–Hero、497行 | 「E3で第3の推定器として実際に使う」 | E3・R1-6と揃え、具体的な不確実性が残る場合の任意追加とする |
| §3.3 H4、407行 | MAKE47 §5.9–5.10への参照 | 投稿版の該当節番号へ訂正。H4の失敗は共通Q・予算で定義する |
| §13末尾、1200行 | 解決済みU1・U2を「必ず解消する」 | 公刊版照合、公表実験の設定、再実装監査など実際の未確認項目へ変更 |
| §6.1 R1-10、904行 | 「会議論文はarXiv DOIで統一可」 | 公刊版を引用するならその版のDOIを優先。arXiv版を引用する場合は書誌全体をその版に合わせる |
| §7.2、959行以降 | Abstract構成が依然として費用中心 | 第2・第4項に学習時間・統計量・品質の関係と診断の追加価値を入れる |
| §7.4、984行以降 | E7専用の図表が明示されていない | 固定容量での読み取り軌跡と、方法単位での選択結果を分けた図表を追加 |

H4の「適用限界が共通する」は仮説として扱える。ただし、FONDUEに本研究固有のV1/V2を適用し、その不適合をFONDUEの失敗と数えない。自然画像・Conv全般に共通の失敗があるという表現にも広げない。

表数・語数・参考文献数は、最終的に対象版で数え直す。これらの固定値や表現の回数を調整するためだけに、実験開始を止める必要はない。

## 6. 方針へ追加する短い文章案

### AU評価について

> 基本の選択手順はIDによる候補優先と品質条件Qで構成し、AUは選択とは別の診断情報として記録する。AUの追加価値は、同じcheckpoint群に対する単純診断との比較で評価する。AUに基づいて追加学習の選択を変える場合は、その方策と費用を別途定義し、記録だけの基本手順と区別する。

### FONDUE再実装について

> 主比較では、論文の記述仕様に基づく閾値と変数分類を事前に固定する。公開コードはcommitを固定して監査し、既定設定、公表実験時の設定、再実装で採用する設定を区別する。現行コードの二重指数化は補助的な再現性確認として扱い、公表実験の結果や結論が誤りであるとの根拠にはしない。

### 品質回復について

> 同一の比較条件では品質目標を共通に固定する。β調整や容量拡張による品質回復は、変更前に定めた目標に達したかで判断し、介入後に目標を緩めない。

## 7. 次の作業

1. **AU評価の主方式、E7の主要比較、FONDUE再実装の主設定、共通Qを一つの実験設定表に確定する。**
2. **既存の損失・分割・合成データの出力層を点検し、dSpritesとMNISTでE7a/E2の代表試行を行う。**
3. **試行で実行費用と測定方法を確認してから、E7b/E1の直接比較と自然画像の評価へ進む。**

現段階では、さらに比較法やデータセットを増やすことより、既に計画した比較が何を検証するのかを固定することが重要である。今回の方針は、その作業に進むための基礎として十分に整っている。

## 確認範囲

今回確認したのは、修正方針の差分・全文の整合、およびFONDUE公開コードの限定した経路である。モデルの学習、公表数値の再現、過去のcommit履歴、NOLTA公刊最終版との同一性、MAKE48や研究室側実験コードの監査は実施していない。

元の方針ファイルは変更していない。本書は方針への追加コメントであり、実験完了・査読指摘解消・採録を認定するものではない。
