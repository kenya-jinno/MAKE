# MAKE49 段階 4 — E7b・E1・+Q 対照の主比較

対象: `MAKE49_査読対応方針.md` §10 段階 4
完了条件: 選択結果・品質・費用の比較が揃うこと
前提: `MAKE49_実験設定表.md`、`MAKE49_段階3_校正結果.md`、`MAKE49_段階3_E7a_E2結果.md`
実施日: 2026-09-13

---

## 1. 実験の枠組み

### 1.1 比較法（設定表 §5.3）

| ID | 方法 | 選択規則 |
|---|---|---|
| B1 | 全探索（validation MSE） | 候補グリッド全点を学習し val MSE 最良点 |
| B2 | 全探索（validation ELBO） | 同じ likelihood で val ELBO 最良点 |
| B3 | 全探索（下流 validation） | 固定 linear probe の val accuracy 最良点 |
| B4 | ID を使わない粗い探索 | 同じグリッドから対数間隔で 4 点 |
| B5 | 固定次元 | $m=32$（結果を見る前に固定） |
| **B6a** | **FONDUE Algorithm 1** | $\mathrm{IDE}_z-\mathrm{IDE}_\mu \le 0.2\,\mathrm{IDE}_{\mathrm{data}}$ を満たす最大次元 |
| **B6b** | **FONDUE-VAR Algorithm 3** | $2\,\mathrm{IDE}_{\mathrm{data}}$ から倍増し passive/mixed 出現時に active(+mixed) を返す |
| B9 | PCA（累積寄与率 95%） | 補助的対照。**PCA 次元を真の ID としない** |
| — | **提案法** | ID 誘導の初期窓 $W$ を全点評価し、$Q$ を満たす検証済み最小 $m$ |
| — | 提案法 −AU | 同上、AU を記録しない版 |
| — | B4+Q / B6a+Q / B6b+Q | 既存法に提案法と同じ $Q$ と拡張規則を付加 |

**B7（GECO/L0 系）と B8（ARD-VAE）は未実施。** 別の目的関数とモデル族の実装を要するため、
段階 4 では FONDUE 系との直接比較（R2-1 の中核）を優先した。**主比較の不足として明記する。**

### 1.2 原著仕様の確定（一次資料で確認）

FONDUE 論文 arXiv:2209.12806v1 を全文確認し、擬似コードと設定を確定した（2026-09-13）。

**Algorithm 1**: 閾値は `threshold ← t×p/100` として**探索開始時に一度だけ計算**され、
探索中に $p$ が変わっても更新されない。事前登録した「探索中の容量に応じて変えない」が原著どおりと確認できた。
本文の $t=0.2$（データ IDE の 20%）と擬似コードの $t\times p/100$ は単位が食い違う（U13 の不一致は原著側に実在）。
**本文の読みを採用した。**

**Table 1 の epoch**: Symsol 2、dSprites 1、Celeba 1。MNIST は原著に無いため、
原著の手順（予測が変わらなくなるまで epoch を増やす）を適用し、**1 と 2 で一致したため 1** に確定した。

**Algorithm 3**: `keep_mixed=True` のとき active+mixed を返す。
事前登録した H2 の主推定対象と一致することを確認した。

### 1.3 費用の公平性

どの方法も「最終評価できる選択済みモデルが得られるまで」に揃える。
**選択後に選択次元での最終学習（300 epochs 相当）を全方法へ一律に課し、その費用を計上する。**
FONDUE 系の短時間探索はそのまま尊重するので、探索の安さは費用に反映される。

計上対象: 参照 AE 学習・ID 推定・候補学習・anchor 学習・再探索・probe 学習・評価。
同じ方法の 1 回の実行中に同じ $(m, \text{epochs})$ を再要求した場合は memoisation で無料にする
（FONDUE Algorithm 2 の GET-MEM に相当）。

### 1.4 事前登録の修正（結果を見る前、設定表 §9 に記録）

| 項目 | 修正 | 理由 |
|---|---|---|
| §5.3 $m_{\mathrm{ref}}$ | 候補グリッドに丸めない | MNIST で 3 つの $m_{\mathrm{ref}}$ が全て 64 に潰れ、ID 安定性の判定が空振りした |
| §1.6 ラン上限の数え方 | 候補 VAE の学習のみを数える | 参照 AE 9 本が 12 ラン中 9 を占めてしまう。実時間上限は候補学習 12 本を基準に導出済み |

修正後、参照 AE の TwoNN 推定は $m_{\mathrm{ref}}=55/111/221$ で 10.11/10.92/11.21、
相対範囲 0.10（基準 0.15 以内）となり、**安定性の判定が実際に機能する**ようになった。

---

## 2. 結果 — MNIST（$\beta=1$、5 seeds、候補グリッド 12 点）

### 2.1 選択結果・品質・費用（完了条件の三点）

品質（画素平均 val MSE）の良い順に並べる。**終了状態の内訳を必ず併記する。**

| 方法 | 選択 $m$ | val MSE | test MSE | 総時間 | VAE ラン | 補助ラン | 終了状態 |
|---|---|---|---|---|---|---|---|
| B1 全探索(val MSE) | 12.8 ± 6.4 | **0.02098** | 0.02050 | 628 s | 12 | 0 | SUCCESS×5 |
| B2 全探索(val ELBO) | 10.0 ± 2.0 | 0.02100 | 0.02054 | 628 s | 12 | 0 | SUCCESS×5 |
| **B6a+Q** | 8.0 ± 0.0 | **0.02172** | 0.02133 | **126 s** | 6 | 0 | SUCCESS×5 |
| B4 粗い探索 | 10.0 ± 0.0 | 0.02180 | 0.02136 | 190 s | 4 | 0 | SUCCESS×5 |
| B4+Q | 10.0 ± 0.0 | 0.02180 | 0.02136 | 190 s | 4 | 0 | SUCCESS×5 |
| **B6a FONDUE** | 7.0 ± 0.0 | 0.02193 | 0.02154 | **41 s** | 5 | 0 | SUCCESS×5 |
| **提案法 (ID+Q)** | **6.0 ± 0.0** | 0.02244 | 0.02205 | **715 s** | 10 | 9 | SUCCESS×5 |
| 提案法 −AU | 6.0 ± 0.0 | 0.02244 | 0.02205 | 715 s | 10 | 9 | SUCCESS×5 |
| B6b+Q | 11.6 ± 11.4 | 0.02254 | 0.02213 | 118 s | 3 | 0 | SUCCESS×5 |
| B6b FONDUE-VAR (keep_mixed=T) | 9.8 ± 8.5 | 0.02297 | 0.02253 | 50 s | 2 | 0 | SUCCESS×5 |
| B5 固定 $m=32$ | 32.0 ± 0.0 | 0.02343 | 0.02299 | 56 s | 1 | 0 | SUCCESS×5 |
| B3 全探索(下流) | 51.2 ± 13.4 | 0.02350 | 0.02301 | 696 s | 12 | 12 | SUCCESS×5 |
| B6b FONDUE-VAR (keep_mixed=F) | **0.0 ± 0.0** | — | — | 0.3 s | 1 | 0 | **DEGENERATE_SELECTION×5** |

### 2.2 seed ごとの選択 $m$（mean±SD だけで判断しない）

| 方法 | seed 別の選択 $m$ |
|---|---|
| B1 全探索(val MSE) | 8, 24, 12, 10, 10 |
| B2 全探索(val ELBO) | 8, 8, 12, 12, 10 |
| B3 全探索(下流) | 64, 64, 32, 48, 48 |
| B4 粗い探索 | 10, 10, 10, 10, 10 |
| **B6a FONDUE** | **7, 7, 7, 7, 7** |
| **B6b FONDUE-VAR (T)** | **25, 6, 5, 6, 7** |
| B6b FONDUE-VAR (F) | 0, 0, 0, 0, 0 |
| **提案法** | **6, 6, 6, 6, 6** |
| B6a+Q | 8, 8, 8, 8, 8 |
| B6b+Q | 32, 6, 6, 6, 8 |

- **提案法と B6a はどちらも 5 seeds で完全に一定**（SD $=0$）。安定性で提案法が優位とは言えない。
- **B6b（keep_mixed=T）は 1 seed だけ 25 を返し不安定**（SD $=8.5$）。
  FONDUE 著者が付録 G で述べた変数型分類の早期不安定性を、共通条件で再現した形である。
- B1 の選択 $m$ が 8〜24 とばらつくのは、$\beta=1$ では MSE が $m$ に対してほぼ平坦で
  argmin が雑音に支配されるためである。

### 2.3 【重要】提案法は最も高価で、品質でも劣る

| 比較対象 | 品質差（対象 − 提案法） | 時間比（提案法 / 対象） |
|---|---|---|
| B6a FONDUE | **−0.00051**（対象が良い） | **17.3 倍** |
| B6a+Q | **−0.00072**（対象が良い） | **5.7 倍** |
| B4 粗い探索 | −0.00065（対象が良い） | 3.8 倍 |
| B1 全探索 | −0.00146（対象が良い） | 1.1 倍 |

**B6a+Q は提案法より品質が良く（0.02172 対 0.02244）、かつ 5.7 倍速い（126 s 対 715 s）。**
提案法が勝っているのは選択次元の小ささ（$m=6$）だけである。

提案法の費用の内訳: 参照 AE 9 本（補助ラン）＋ 初期窓 $W$ の全 10 点＋ anchor。
方針 §4.4 の Algorithm 1 手順 4 に忠実に $W$ を全点学習しているため高価になる。
短絡して最初に $Q$ を満たした点で止めれば安くなるが、**それは実質 B4+Q と同じ手順**になる。

### 2.4 +Q 対照 — $Q$ の効果と ID/AU の効果の分離

| 基底法 | 選択 $m$ | val MSE | 総時間 |
|---|---|---|---|
| B4 → B4+Q | 10.0 → 10.0 | 0.02180 → 0.02180 | 190 s → 190 s |
| B6a → B6a+Q | 7.0 → 8.0 | 0.02193 → **0.02172** | 41 s → 126 s |
| B6b → B6b+Q | 9.8 → 11.6 | 0.02297 → **0.02254** | 50 s → 118 s |

- B4 は元の選択が既に $Q$ を満たすため、$+Q$ で何も変わらない。
- **FONDUE 系は $+Q$ で品質が改善する**（費用は 2〜3 倍）。
- 設定表 §5.3 の問い「品質確認は既存選択法への簡単な追加で得られるか」に対する答えは
  **MNIST では「得られる」**であり、しかも ID 誘導を経由するより安い。

### 2.5 提案法 −AU 対照

選択 $m$ は 5 seeds すべてで提案法と**完全に一致**した（いずれも 6）。
**AU は選択の分岐に使われていない**という設定表 §1.1 の仕様どおりに実装されていることが確認できた。

**測定上の限界**: 本実装では AU を候補学習の内部で計算し結果をキャッシュ共有しているため、
「AU 算出分の計算時間」の削減は測定できていない（両者とも 715 s）。
方針 §4.4 が述べる「変わり得るのは AU 算出分の計算時間のみ」の定量化は未達である。

### 2.6 FONDUE-VAR の退化（原著どおりの挙動）

`keep_mixed=False` では Algorithm 3 が `n ← av`（active 数）を返すが、
**MNIST の 1 epoch では active 数が 0 になり、5 seeds すべてで選択次元 0 を返した。**
学習可能な次元ではないため `DEGENERATE_SELECTION` として記録し、平均からは落とさず内訳に残した。

これは実装の誤りではなく**原著 Algorithm 3 の忠実な挙動**である。
付録 G 自身が「学習初期には mixed と passive が正しく識別されない」と述べており、それと整合する。
**この退化を根拠に FONDUE 全体を否定してはならない。** 原著の主手法は Algorithm 1 である。

### 2.7 B9（PCA）の位置づけ

累積寄与率 95% を満たす線形次元は **MNIST で 150、dSprites で 445** であり、
候補グリッドの上限（64）と ID 推定値（13.8 / 7.6）から大きく乖離する。
Minka の MLE は両データとも固有値の退化により数値的に失敗した。
**PCA 次元を真の内在次元とみなさない**という設定表の但し書きが、実データで必要になった例である。

## 3. 結果 — dSprites（$\beta=1$、Bernoulli 尤度、5 seeds、候補グリッド 12 点）

### 3.1 選択結果・品質・費用

| 方法 | 選択 $m$ | val MSE | 総時間 | VAE | 補助 | 終了状態 |
|---|---|---|---|---|---|---|
| B1 全探索(val MSE) | 57.6 ± 14.3 | **0.00116** | 2565 s | 12 | 0 | SUCCESS×5 |
| B2 全探索(val ELBO) | 57.6 ± 14.3 | 0.00116 | 2565 s | 12 | 0 | SUCCESS×5 |
| B3 全探索(下流) | 64.0 ± 0.0 | 0.00117 | 2589 s | 12 | 12 | SUCCESS×5 |
| B4 粗い探索 | 64.0 ± 0.0 | 0.00117 | 704 s | 4 | 0 | SUCCESS×5 |
| **提案法 (ID+Q)** | 64.0 ± 0.0 | 0.00117 | 1001 s | 4 | 9 | **ID_UNRELIABLE×5** |
| B4+Q | 64.0 ± 0.0 | 0.00117 | 704 s | 4 | 0 | NO_COMPACT×5 |
| B5 固定 $m=32$ | 32.0 ± 0.0 | 0.00122 | 258 s | 1 | 0 | SUCCESS×5 |
| **昇順走査+Q（ID なし）** | **20.0 ± 4.0** | 0.00126 | 1797 s | 9 | 0 | **SUCCESS×5** |
| B6b+Q | 28.8 ± 20.1 | 0.00125 | 1261 s | 8.8 | 0 | SUCCESS×3, NO_COMPACT×1, BUDGET×1 |
| B6a+Q | 20.0 ± 4.0 | 0.00126 | 1677 s | 11.8 | 0 | SUCCESS×4, BUDGET×1 |
| **B6a FONDUE** | **4.2 ± 0.4** | **0.00550** | 141 s | 5 | 0 | SUCCESS×5 |
| B6b FONDUE-VAR (T) | 26.0 ± 49.5 | 0.00939 | 175 s | 3.4 | 0 | SUCCESS×4, DEGENERATE×1 |
| B6b FONDUE-VAR (F) | 1.2 ± 1.6 | 0.00766 | 40 s | 1.4 | 0 | SUCCESS×2, DEGENERATE×3 |

seed ごとの選択 $m$:

| 方法 | seed 別 |
|---|---|
| B1 全探索 | 64, 64, 32, 64, 64 |
| B6a FONDUE | 5, 4, 4, 4, 4 |
| **B6b FONDUE-VAR (T)** | **0, 114, 1, 14, 1** |
| B6b FONDUE-VAR (F) | 0, 0, 3, 0, 3 |
| 提案法 | 64, 64, 64, 64, 64（全て fallback） |
| **昇順走査+Q** | **24, 20, 16, 16, 24** |
| B6a+Q | 24, 20, 16, 16, 24 |

### 3.2 提案法は dSprites で適用を見送った（ID_UNRELIABLE）

| 判定 | MNIST | dSprites |
|---|---|---|
| 参照 AE ごとの $\hat d$（$m_{\mathrm{ref}}$ 別） | 10.93 / 11.78 / 12.32 | **3.96 / 3.91 / 2.10** |
| 相対範囲（基準 $\le 0.15$） | 0.118 **通過** | **0.474 不通過** |
| TwoNN 対 MLE の相対差（基準 $\le 0.25$） | 0.150 **通過** | **1.816 不通過** |
| 終了状態 | SUCCESS | **ID_UNRELIABLE**（B4 へ切り替え） |

**手順の診断としては正しく働いた。** 推定が信頼できない条件で誤った答を出さず、
事前宣言した通常探索へ切り替えている。これは方針 §4.1 が求める動作である。
ただし**その結果、ID 誘導は dSprites に何も寄与していない**。

### 3.3 FONDUE 再実装は公表値を再現しなかった

| | 本再実装 | 原著公表値（Table 1） |
|---|---|---|
| dSprites の選択次元 | **4.2 ± 0.4** | **12.2 ± 0.4** |

探索経路（seed 42、閾値 $=0.2\times\mathrm{IDE}_{\mathrm{data}}=1.522$）:
$p=8$ で gap $=3.44>1.52$ → $p=4$ で $0.955\le1.52$ → $p=6$ で $3.196>1.52$ → $p=5$ で $0.174\le1.52$ → 選択 5。

原因の候補: 本研究の入力空間 ID 推定が $\hat d_0=7.61$ であるのに対し、
原著の dSprites 設定値は約 11 である。閾値が $0.2\hat d_0$ で決まるため、
**ID 推定値の違いがそのまま閾値と探索結果の違いになる。** 加えて枠組み・尤度・アーキテクチャも異なる。

**この不一致を FONDUE の誤りとして扱わない**（設定表 §6 の 3 区分）。
公表値は参考欄にとどめ、**主比較の不足が残ることを明記する**。
なお選択された $m\approx4$ の品質は 0.00550 で全探索の 0.00116 の 4.7 倍悪く、
**本条件では FONDUE は大きく過小選択する**。

### 3.4 FONDUE-VAR は dSprites で崩壊的に不安定

探索経路（`(容量, active, mixed, passive)`）:

| seed | 経路 | 返り値 |
|---|---|---|
| 42 | (15, 0, 0, 15) | **0**（退化） |
| 123 | (15,0,15,0) → (30,1,29,0) → (60,0,60,0) → (120,0,114,6) | **114** |
| 777 | (15,3,12,0) → (30,1,29,0) → (60,0,60,0) → (120,0,1,119) | **1** |
| 2024 | (15, 0, 14, 1) | 14 |
| 31337 | (15,3,12,0) → (30,1,29,0) → (60,0,1,59) | **1** |

同一条件で 0, 114, 1, 14, 1。SD $=49.5$。
付録 G が述べた早期学習での変数型分類の不安定性を、**極端な形で再現した**。

---

## 4. 【決定的】ID 誘導の増分価値 — 昇順走査+Q との対照

提案法と**同じ目的関数**（検証済み候補内で $Q$ を満たす最小の $m$）を、
**ID 推定も参照 AE も使わずに**達成する対照を追加した（`ascending_scan_Q`）。
両者の差は「ID 誘導があるか」だけである。

| データ | 方法 | 選択 $m$ | val MSE | 総時間 | VAE | 補助 | 終了状態 |
|---|---|---|---|---|---|---|---|
| MNIST | 提案法 (ID+Q) | 6.0 ± 0.0 | 0.02244 | **715 s** | 10 | 9 | SUCCESS×5 |
| MNIST | **昇順走査+Q** | **6.0 ± 0.0** | **0.02244** | **182 s** | 4 | 0 | SUCCESS×5 |
| dSprites | 提案法 (ID+Q) | 64.0 ± 0.0 | 0.00117 | 1001 s | 4 | 9 | **ID_UNRELIABLE×5** |
| dSprites | **昇順走査+Q** | **20.0 ± 4.0** | 0.00126 | 1797 s | 9 | 0 | **SUCCESS×5** |

- **MNIST: 完全に同一の選択・同一の品質を、3.93 分の 1 の費用で達成する。**
  ID 推定と参照 AE 9 本は、選択にも品質にも寄与していない。
- **dSprites: 提案法が適用を見送る条件で、ID なしの走査は $m=20$ を見つける。**
  ID 誘導は寄与しないどころか、**手順を止める側に働いている**。

**ID 誘導の増分価値は、本実験の条件では観測されなかった。**

---

## 5. 最終判断

### 5.1 方針 §11 のどの行に該当するか

方針 §11 の表に照らすと、**該当するのは次の 2 行**である。

> **「品質は保てるが FONDUE 等より高コスト」** → 最速という主張を削除。役割分担として提示する。
>
> **「ID なしの粗い探索と同程度」** → ID が必須という主張を弱める。推定費用に見合う条件を限定する。

さらに、§4 の対照により**より強い結論**が出た。

> **ID 誘導の増分価値は、本実験の 2 データセットのいずれでも観測されなかった。**

MNIST では同じ答を 3.93 分の 1 の費用で ID なしに得られ、
dSprites では ID 判定が手順を止めるため ID ありの方が悪い結果になった。

### 5.2 残せる主張と、削除すべき主張

**削除すべき主張**

| 主張 | 根拠 |
|---|---|
| 「最大 82% 削減」「高速な選択法」 | 提案法は MNIST で全比較法中**最も高価**（715 s）。FONDUE の 17.3 倍 |
| 「VAE は 1 候補の学習だけで十分」 | 初期窓の全点学習が必要。1 候補では $Q$ の判定ができない |
| ID 誘導が探索を効率化する | 昇順走査+Q が同じ答をより安く出す（§4） |
| AU が変数型分類より安定 | 段階 3 の H2 が不支持（`MAKE49_段階3_E7a_E2結果.md`） |
| AU が容量に応じた有効次元を示す | AU は $m$ に非依存で $\beta$ で決まる（段階 1 A2、段階 3 で再確認） |

**残せる主張**

| 主張 | 根拠 |
|---|---|
| **品質条件 $Q$ の定式化に価値がある** | FONDUE 系に $+Q$ を足すと品質が大きく改善（dSprites で 0.00550 → 0.00126） |
| **ID 推定の信頼性判定が診断として働く** | dSprites で `ID_UNRELIABLE` を正しく返し、誤った答を出さずに切り替えた |
| **既存法の失敗条件を共通条件で示せる** | FONDUE の過小選択（$m\approx4$）、FONDUE-VAR の崩壊的不安定（SD $=49.5$）、`keep_mixed=False` の退化 |
| 選択目的によって最適次元が大きく変わる | MNIST で下流基準は $m\approx51$、再構成基準は $m\approx13$ |

### 5.3 貢献の組み替え案

現行の「ID 誘導による高速な次元選択法」という枠組みは、本結果では支持されない。
**貢献の中心を $Q$ と診断へ移すことを推奨する。**

1. **主貢献を「品質条件つき次元選択の枠組み $Q$」にする。**
   既存のどの選択法にも後付けでき、FONDUE 系では品質を大きく改善する。
   ID 誘導は $Q$ を満たす候補を探す**一手段**にすぎず、昇順走査で代替できることを正直に書く。
2. **ID 推定は「適用可否の診断」として位置づける。**
   選択の高速化ではなく、**推定が信頼できない条件を検出して適用を見送る**役割で提示する。
   dSprites の `ID_UNRELIABLE` はその実例である。
3. **既存法の比較研究としての価値を前面に出す。**
   共通条件・共通品質尺度・共通費用計上のもとで FONDUE 系を評価した例は本研究が初である。
   過小選択・不安定性・退化を再現可能な形で示せている。
4. **AU は補助分析へ移す。** 段階 3 の H2 不支持と段階 4 の −AU 対照（選択に影響なし）により、
   主要な方法貢献としては維持できない。設定表 §1.1 の方式 2（未使用条件での予測改善）を
   段階 5 の E4 で評価し、そこでも増分価値が出なければ完全に補助へ移す。

方針 §11 の最終行「既存法に対する利点が示せず、会議論文との差分も小さい → 現行の方法提案としての
採録根拠は弱い。診断研究への再構成または追加研究が必要」に**近い状態**である。
ただし $Q$ の効果と既存法の失敗条件の記録は実証的な価値があるため、
**診断・比較研究への再構成**であれば十分な内容がある。

### 5.4 主比較の不足（明記すべき事項）

| 事項 | 状態 |
|---|---|
| **B7（GECO/L0 系）・B8（ARD-VAE）** | **未実施。** 別の目的関数とモデル族の実装を要する。主比較の不足として残る |
| **FONDUE 公表値の再現** | **未達**（dSprites で 4.2 対 公表 12.2）。公表値は参考欄にとどめる |
| CIFAR-10 / SVHN | 段階 5（E5）で実施 |
| $\beta$ 梯子全体での評価 | 本段階は $\beta=1$ のみ。梯子は段階 5 以降 |
| AU 算出分の計算時間の削減量 | 測定できていない（§2.5） |
| Fashion-MNIST | 未実施 |

**本結果は $\beta=1$・2 データセット・候補グリッド 1 種での観測である。**
$\beta$ を変えれば品質と $m$ の関係が変わるため、
「ID 誘導に価値がない」を一般的な主張に広げない。**本条件で観測されなかった**と書く。

### 5.5 段階 5 へ引き継ぐ論点

1. E4 の $\delta$ 感度を前倒しで実施する。段階 3 の C6（AU が $\beta$ に対して非単調）が未解決である。
2. 設定表 §1.1 の方式 2 を実行し、AU の増分価値を最終判定する。
3. V1/V2 の係数は現行値では機能しない（MNIST 0.43、dSprites 1.85）。規則自体を再設計する。
4. B7/B8 を追加するか、主比較の不足として明記するかを決める。
