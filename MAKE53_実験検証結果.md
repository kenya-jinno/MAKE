# MAKE53 実験・検証結果

対象: `MAKE53_revision_review.md` §5 が求めた追加実験・検証
実施日: 2026-09-19 〜 2026-09-20
生成物: `results/make53/`、実装は `src/make53/`
計算環境: NVIDIA GeForce RTX 3070 / PyTorch 2.7.1+cu118 / CUDA 11.8 / driver 570.211.01 / Python 3.12.3

---

## 0. 実施したもの

| 査読文書の項目 | 実施 | 主な結果 |
|---|---|---|
| §5.1 真の ELBO による容量選択を複数 seed で比較 | 完了 | 3 基準は 5 seeds 中 3 で一致。**MC ELBO の選択が最も安定（SD 1.7）** |
| §5.2 ゼロ中心 ARD への native 評価 | 完了 | sample-centred と同じ結論。**dSprites だけマスク損害 +129%** |
| §5.3 再評価できる記録の保存 | 完了 | 6 run で checkpoint・選択軸・relevance・履歴を保存。**機能確認 18/18 通過** |
| §5.4 未マスク 30-epoch 追加学習対照 | 完了 | **自然画像 3 データでマスク固有の補償は検出できない。dSprites のみ −29pp** |
| §5.5 任意の補強項目 | 未実施 | 査読文書自身が必須としていない |

合計 GPU 実時間はおよそ 1 時間 30 分（§5.1 が 44 分、§5.2 が約 10 分、§5.4 が 41 分）。

**本文書は実験と検証の記録である。** 原稿・返答書への反映は行っていない。

---

## 1. 真の ELBO による容量選択（§5.1）

### 1.1 何が求められたか

MAKE53 の実 ELBO 比較は MNIST・seed 42 の 1 件に限られていた。Reviewer 3 の「強い ELBO baseline」に対して、学習 seed を増やすことが最優先の追加実験とされた。

### 1.2 設定

事前に固定し、結果を見て変更していない。

| 項目 | 値 |
|---|---|
| データセット | MNIST、β = 1 |
| 候補格子 | 2, 4, 6, 8, 10, 12, 16, 20, 24, 32, 48, 64 |
| 学習 seed | 42, 123, 777, 2024, 31337 |
| 学習 | 最大 300 epochs、patience 30、batch 128、lr 1e-3 |
| checkpoint 選択 | 決定論的 ELBO proxy の最良 |
| MC ELBO | K = 32、共通乱数、評価 seed 20260919 |
| 尤度 | Gaussian、分散 1/2 |

60 条件（12 幅 × 5 seeds）をすべて本 run の GPU 環境で新規学習した。**seed 42 を含め、既存の CPU 環境の結果とは混ぜていない。** 学習合計 43.8 分、総 epoch 8,877。

実装は `src/make53/elbo_multiseed.py`、結果は `results/make53/elbo_multiseed.json`。

### 1.3 実装の事前確認

査読文書が挙げた恒等式を照合した。

```
MC ELBO = −D × (標本ごとの画素平均 MSE) − KL − (D/2) log π
```

| 量 | 値 |
|---|---|
| MC ELBO | −484.40969823788913 |
| 右辺から再構成した値 | −484.4096982378892 |
| 絶対差 | 5.68 × 10⁻¹⁴ |

`results/make53/elbo_selfcheck.json` に記録した。

### 1.4 seed ごとの選択

| seed | MSE | ELBO proxy | MC ELBO | 3 基準一致 |
|---|---|---|---|---|
| 42 | 8 | 8 | 8 | ○ |
| 123 | **24** | 8 | 8 | × |
| 777 | 12 | 12 | 12 | ○ |
| 2024 | 10 | **12** | 10 | × |
| 31337 | 10 | 10 | 10 | ○ |

3 基準が完全に一致するのは 5 seeds 中 3。MC ELBO は proxy と 4/5、MSE と 4/5 で一致する。

### 1.5 選択幅の安定性と最終性能

| 基準 | 選択幅 mean ± SD | test MSE | test MC ELBO |
|---|---|---|---|
| MSE | 12.8 ± 6.4 | 0.02050 ± 0.00026 | −477.63 ± 0.13 |
| ELBO proxy | 10.0 ± 2.0 | 0.02054 ± 0.00025 | −477.62 ± 0.07 |
| MC ELBO | **9.6 ± 1.7** | 0.02051 ± 0.00027 | **−477.60 ± 0.08** |

**真の MC ELBO による選択が最も安定する。** MSE は seed 123 で 24 を選び、SD が 6.4 と大きい。

一方、**選ばれた幅が違っても test 指標はほぼ同じである。** 基準の違いは選択の安定性に現れ、最終性能には現れない。

### 1.6 学習 seed の変動と Monte Carlo 誤差の分離

| 量 | 値 |
|---|---|
| 選択モデルの val MC ELBO 平均 | −477.998 nats |
| 学習 seed 間の SD | 0.059 nats |
| MC 標準誤差（K = 32）の平均 | 0.011 nats |
| 比 | 5.6 |

**学習 seed 間の変動が Monte Carlo 誤差の 5.6 倍である。** seed を増やすほうが K を増やすより有効である。

### 1.7 K = 32 の妥当性は条件つき

候補どうしの識別可能性は seed によって異なる。

| seed | 最良 ELBO | 次点との差 | MC SE | 差 / SE | 全候補の幅 |
|---|---|---|---|---|---|
| 42 | −478.008 | 0.322 | 0.0104 | 30.9 | 6.39 |
| 123 | −478.041 | 0.159 | 0.0088 | 18.0 | 6.22 |
| 777 | −477.906 | 0.102 | 0.0107 | 9.5 | 6.79 |
| 2024 | −477.978 | **0.014** | 0.0118 | **1.2** | 6.61 |
| 31337 | −478.056 | 0.030 | 0.0116 | **2.6** | 6.27 |

候補全体の幅は 6.2〜6.8 nats で、MC SE の 500 倍以上ある。良い幅と悪い幅の区別に K = 32 は十分である。

**ただし最良と次点の区別には十分でない seed がある。** seed 2024 では差が MC SE の 1.2 倍、seed 31337 では 2.6 倍しかない。共通乱数により差の誤差は単独の SE より小さくなるが、これらの seed で最良幅が MC 雑音に左右されうることは否定できない。表 1.4 の「MC ELBO による選択」はこの限定つきで読む必要がある。

### 1.8 この実験が示さないこと

- checkpoint は決定論的 proxy で選んでいる。**同一の checkpoint 群から容量だけを選ぶ設計であり、epoch 選択まで ELBO で行う pipeline との比較ではない。**
- MNIST のみである。4 データセット全体の実 ELBO 比較には拡張していない。

---

## 2. ゼロ中心 ARD への native 評価（§5.2）

### 2.1 何が求められたか

MAKE52 の native 品質評価は、ARD-VAE の中心化を標本平均で行うローカル変種に対するものだった。原著と共通するゼロ中心設定（μ^α = 0）でも同じ評価を行うことが、Reviewer 2 への比較を強める次の候補とされた。

### 2.2 設定

原著の共役更新（Eq. 8/9）、目的関数（Eq. 15）、Jacobian relevance（Eq. 19）、累積 relevance 99% の選択規則はそのままに、中心化のみ μ^α = 0 とした。MNIST と dSprites、各 3 seeds、初期容量 64、300 epochs、追加学習 30 epochs。dSprites は D = 4096 のため VJP 積み上げ版の Jacobian を用いた。

実装は `src/make53/ard_zero_centred.py`、結果は `results/make53/ard_zero_centred.json`。

### 2.3 結果（validation 画素平均 MSE）

| データセット | seed | 選択 | Full | Mask | Adapt | 転用 | 転用幅 |
|---|---|---|---|---|---|---|---|
| MNIST | 42 | 6/64 | 0.02381 | 0.02382 | 0.02358 | 0.02267 | 6 |
| MNIST | 123 | 6/64 | 0.02384 | 0.02385 | 0.02368 | 0.02241 | 6 |
| MNIST | 777 | 6/64 | 0.02279 | 0.02353 | 0.02322 | 0.02244 | 6 |
| dSprites | 42 | 9/64 | 0.00131 | 0.00304 | 0.00189 | 0.00186 | 8 |
| dSprites | 123 | 9/64 | 0.00134 | 0.00297 | 0.00189 | 0.00182 | 8 |
| dSprites | 777 | 9/64 | 0.00135 | 0.00313 | 0.00192 | 0.00198 | 8 |

### 2.4 中心化の選び方で結論は変わらない

| データセット | 変種 | 選択軸数 | マスクによる損害 |
|---|---|---|---|
| MNIST | sample-centred | 6, 6, 6 | +0.05% |
| MNIST | zero-centred | 6, 6, 6 | +1.10% |
| dSprites | sample-centred | 10, 10, 9 | +125.98% |
| dSprites | zero-centred | 9, 9, 9 | +128.64% |

**原著の「非関連軸は復号器出力をほとんど変えない」という主張は、MNIST では中心化によらず成立し、dSprites では中心化によらず破れる。** MAKE52 の結論がローカル変種の産物でないことを確認した。

---

## 3. 再評価できる記録の保存（§5.3）

### 3.1 何が求められたか

native マスク評価の記録に選択軸 ID・relevance・checkpoint・epoch 履歴がなく、「どの軸を除去したモデルでその MSE になったか」を summary から独立に再評価できなかった。

### 3.2 保存したもの

`src/make53/artifacts.py` に集約し、§5.2 の 6 run すべてで有効にした。保存先は `results/make53/checkpoints/<tag>/`。

| ファイル | 内容 |
|---|---|
| `model_full.pt` | 追加学習前の state |
| `model_adapt.pt` | 追加学習後の state |
| `record.json` | relevance（64 軸）、prior 分散、順位、選択軸 ID、binary mask、モデル構成、split index hash、学習 seed、完了 epoch 数、微調整中の validation 記録、Full/Mask/Adapt/転用の評価、コード指紋、保存時刻 |

### 3.3 機能確認

査読文書が挙げた確認をすべて実装し、6 run × 3 種 = 18 件を実行した。

| 確認項目 | 件数 | 結果 |
|---|---|---|
| 全軸を残す mask なら Full と同じ出力になる | 6 | 絶対差 0.0、全件通過 |
| 選択数と mask 中の 1 の個数が一致する | 6 | 全件通過 |
| decoder-only 追加学習で encoder が変化しない | 6 | 最大絶対変化 0.0、全件通過 |

`results/make53/functional_checks.json`。`all_pass: true`。

省メモリ版 Jacobian（VJP 積み上げ）と明示的計算（jacrev）の一致は MAKE52 で確認済み（相関 1.00000000、相対差 4.35 × 10⁻⁷）。

### 3.4 残る限定

**掲載 Table 11 の 12 件は、実行時に checkpoint を保存していなかった。** これらを summary だけから復元したとは記載できない。ただし §4.3 で示すとおり、学習は決定的であり再学習でビット一致の state が得られることを確認した。

---

## 4. 未マスク 30-epoch 追加学習対照（§5.4）

### 4.1 何が求められたか

Adapt は追加計算を使い、初期の勾配学習から除外していた 1,800 例を含む 18,000 例で学習する。未マスク Full に同じ条件で 30 epochs 追加学習した対照がないため、**改善がマスクによる損失の補償に特有なのか、単なる追加学習でも得られるのかを区別できない**とされた。

査読文書が指定した 4 条件を実施した。

| 条件 | マスク | 追加学習 |
|---|---|---|
| Full | なし | なし |
| Mask | あり | なし |
| Full + 30 epochs | なし | decoder のみ、同じ 18,000 例・optimizer・予算 |
| Mask + 30 epochs | あり | 同上 |

実装は `src/make53/full_control.py`、集計は `src/make53/summarize_full_control.py`。

### 4.2 二つの範囲に分けた理由

| 範囲 | 対象 | 対応づけの厳密さ |
|---|---|---|
| A | §5.2 のゼロ中心 run（MNIST・dSprites × 3 seeds） | **完全**。同一 state から、追加学習直前に同一 seed を張って両腕を実行。マスク以外に違いがない |
| B | 掲載 Table 11 の run（4 データセット × 3 seeds） | 記録対応。新しい Full+30ep 腕のみを追加し、既存記録の Mask / Adapt と対応づけた |

範囲 A は checkpoint と選択軸 ID を保存してあるため厳密な対応比較ができる。範囲 B は掲載結果そのものを被覆する。両者が独立に一致すれば結論は堅い。

### 4.3 決定的再学習の照合

範囲 B の 12 件は checkpoint 未保存のため再学習した。学習は `set_seed` と `cudnn.deterministic = True` により決定的である。

| 確認 | 結果 |
|---|---|
| 記録済み Full MSE と再学習後 MSE の一致 | **12/12 件でビット一致（絶対差 0.0）** |

これにより relevance の再計算が不要になった。Full+30ep 腕にマスクは要らず、既存の Mask / Adapt は同一モデルのものとしてそのまま対応づけられる。範囲 B の実時間は 37.9 分（うち再学習 30.3 分、追加学習 2.2 分）で、Jacobian を再計算する場合の見込み約 2.5 時間を回避した。

### 4.4 結果（validation 画素平均 MSE、3 seeds、mean）

| 変種 | データセット | Full | Mask | Full+30ep | Mask+30ep | ΔFull | ΔMask | マスク固有 |
|---|---|---|---|---|---|---|---|---|
| zero-centred | MNIST | 0.02348 | 0.02373 | 0.02329 | 0.02346 | −0.80% | −1.15% | −0.36 ± 0.11pp |
| zero-centred | dSprites | 0.00133 | 0.00305 | 0.00119 | 0.00192 | −10.57% | −37.09% | **−26.52 ± 3.85pp** |
| sample-centred | MNIST | 0.02361 | 0.02362 | 0.02343 | 0.02336 | −0.79% | −1.12% | −0.34 ± 0.26pp |
| sample-centred | FashionMNIST | 0.01943 | 0.01943 | 0.01924 | 0.01919 | −0.94% | −1.22% | −0.28 ± 0.18pp |
| sample-centred | dSprites | 0.00133 | 0.00301 | 0.00120 | 0.00183 | −10.04% | −39.24% | **−29.20 ± 2.37pp** |
| sample-centred | CIFAR10 | 0.01440 | 0.01489 | 0.01429 | 0.01473 | −0.80% | −1.09% | −0.29 ± 0.38pp |

「マスク固有」は二つの相対変化の差（ΔMask − ΔFull）をポイントで表したもの。0 に近いほど、改善がマスクに特有でないことを意味する。

test 側も同じ向きである。

| 変種 | データセット | Full | Mask | Full+30ep | Mask+30ep |
|---|---|---|---|---|---|
| zero-centred | MNIST | 0.02297 | 0.02323 | 0.02281 | 0.02298 |
| zero-centred | dSprites | 0.00132 | 0.00298 | 0.00118 | 0.00187 |
| sample-centred | MNIST | 0.02317 | 0.02318 | 0.02298 | 0.02290 |
| sample-centred | FashionMNIST | 0.01917 | 0.01917 | 0.01897 | 0.01893 |
| sample-centred | dSprites | 0.00132 | 0.00294 | 0.00117 | 0.00178 |
| sample-centred | CIFAR10 | 0.01464 | 0.01514 | 0.01453 | 0.01497 |

### 4.5 雑音下限の測定

マスク固有の差を判定するには、追加学習の RNG 流だけで生じる変動を知る必要がある。二通りで測った。

**測定 1: 微調整 seed を振る（MNIST、学習 seed 3 × 微調整 seed 3）**

| 学習 seed | Full+30ep の変化率（微調整 seed 1/2/3） | SD | 幅 |
|---|---|---|---|
| 42 | −1.055% / −1.250% / −0.895% | 0.178pp | 0.355pp |
| 123 | −0.548% / −0.607% / −0.702% | 0.077pp | 0.153pp |
| 777 | −1.146% / −0.898% / −1.131% | 0.139pp | 0.248pp |

SD は最大 0.178pp、幅は最大 0.355pp。

**測定 2: 範囲 A の Mask+30ep 再実行と §5.2 記録の Adapt の差**

追加学習の RNG 流だけが違う。MNIST で最大 0.21%、dSprites で最大 3.04%。

### 4.6 結論: 補償はマスクの損害に比例する

| 変種 | データセット | マスクの損害 | マスク固有の補償 | 雑音下限との比較 |
|---|---|---|---|---|
| sample-centred | MNIST | +0.05% | −0.34pp | 雑音の幅（0.355pp）の内側 |
| sample-centred | FashionMNIST | +0.02% | −0.28pp | 同上 |
| sample-centred | CIFAR10 | +3.38% | −0.29pp | 同上。seed 777 では符号が反転（+0.03pp） |
| zero-centred | MNIST | +1.10% | −0.36pp | 同上 |
| sample-centred | dSprites | +125.98% | **−29.20pp** | 雑音（3.04%）の一桁上 |
| zero-centred | dSprites | +128.64% | **−26.52pp** | 同上 |

**マスクがほとんど損害を与えない 3 つの自然画像データセットでは、マスク固有の補償を検出できない。** 0.28〜0.36pp は微調整 RNG の変動幅の内側にあり、3 seeds では雑音と区別できない。同じ改善は未マスクモデルでも得られる。

**マスクが誤差を 2.3 倍にする dSprites では、追加学習がその大半を回復する。** 26〜29pp は雑音の一桁上で、明確に区別できる。

**補償の大きさは、補償すべき損害の大きさに追随する。** 範囲 A（厳密な対応）と範囲 B（掲載被覆）が独立に一致し、ARD の中心化の選び方にも依存しない。

追加学習後も dSprites では Mask+30ep が Full+30ep より 52〜61% 悪い。回復は部分的である。一方 MNIST と FashionMNIST では、追加学習後のマスク済みモデルが未マスクモデルをわずかに上回る（−0.29%、−0.26%）。64 軸中 6 軸、64 軸中 4 軸しか使っていない。

### 4.7 原稿への含意

- 現行の「30 epochs 追加したところ MSE が下がった」という記述的結論は、そのままで妥当である。
- **「この微調整が pruning の損失を特に補償する」と書けるのは dSprites だけである。**
- **「pruning と微調整の組合せに一般的な優位性がある」とは書けない。** 自然画像 3 データでは未マスクモデルでも同じ改善が得られる。

### 4.8 この実験が示さないこと

- 範囲 B の Mask+30ep は既存記録の値であり、追加学習の RNG 流が Full+30ep 腕と異なる。§4.5 の雑音下限はこの差を評価するために測ったものである。厳密な同一流の比較は範囲 A のみ。
- 3 seeds である。自然画像側の 0.3pp 程度の差を検定するには足りない。本文書は「雑音と区別できない」とだけ述べ、「効果がない」とは述べない。
- 追加学習は decoder のみ、30 epochs 固定である。予算を変えた場合の挙動は調べていない。

---

## 5. 記録上の訂正

`provenance()` を `src/make52/geco_arm.py` から流用したため、§5.4 の保存 JSON の `estimator` 欄が既定値の `"L0-ARM (原著 Eq.12)"` のままになっていた。`"ARD-VAE 未マスク 30-epoch 対照（MAKE53 §5.4）"` に訂正し、実装側も修正した。数値には影響しない。

---

## 6. 生成したファイル

### 結果

| パス | 内容 |
|---|---|
| `results/make53/elbo_multiseed.json` | §5.1。60 条件の学習曲線末尾・val/test・MC ELBO・選択 |
| `results/make53/elbo_selfcheck.json` | §5.1。MC ELBO 恒等式の照合 |
| `results/make53/ard_zero_centred.json` | §5.2。ゼロ中心 ARD の 6 run |
| `results/make53/functional_checks.json` | §5.3。機能確認 18 件 |
| `results/make53/checkpoints/ard_zero_*/` | §5.3。6 run の model_full.pt・model_adapt.pt・record.json |
| `results/make53/full_control.json` | §5.4。18 run の統合記録 |
| `results/make53/full_control_scopeA.json` | §5.4。厳密対応版 6 run |
| `results/make53/full_control_scopeB.json` | §5.4。掲載被覆版 12 run と RNG 感度 |
| `results/make53/full_control_summary.json` | §5.4。集計 |
| `results/make53/full_control.tex` | §5.4。本文用の対応比較表 |
| `results/make53/full_control_seeds.tex` | §5.4。付録用の seed 別表 |

LaTeX 表は 2 つとも試験組版をエラー 0 で通した。

### 実装

| パス | 内容 |
|---|---|
| `src/make53/elbo_multiseed.py` | §5.1。複数 seed の真 ELBO 比較と自己確認 |
| `src/make53/ard_zero_centred.py` | §5.2。ゼロ中心 ARD と成果物保存 |
| `src/make53/artifacts.py` | §5.3。成果物保存と機能確認 |
| `src/make53/full_control.py` | §5.4。範囲 A・B と微調整 RNG 感度 |
| `src/make53/summarize_full_control.py` | §5.4。集計と LaTeX 生成 |

### ログ

| パス |
|---|
| `results/make53_elbo_multiseed.log` |
| `results/make53_ard_zero.log` |
| `results/make53_full_control_b.log` |

---

## 7. 残っている作業（本文書の範囲外）

| 項目 | 由来 |
|---|---|
| §6.1 略語と概念の初出を整える | MAKE53_revision_review.md §6 |
| 科学的結果を先に置く順序変更 | 同上 |
| Data Availability の範囲 | 同上 |
| 著者側再集計の注記 | 同上 |
| 修正メモと返答書の分離 | 同上 |
| 補足 ZIP の再作成と検証 | §5.3 の成果物追加に伴う |
| 参考文献の外部照合 | 継続 |
| MAKE52 / MAKE53 の知見の原稿・返答書への反映 | 執筆範囲 |
| 逸脱申告 3 件の訂正 | MAKE52_実験検証結果.md §4 |

§5.5 の任意項目（独立参照 AE bank、ARD 累積閾値の解析、新規大規模データセット、end-to-end 時間計測、leakage 除去 synthetic 再実験、AU 課題の難化）は、査読文書自身が必須としていないため実施していない。
