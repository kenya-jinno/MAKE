# MAKE49 段階 7 — B7・B8 の追加と β 梯子全体での再評価

対象: 著者判断（2026-09-14）による追加作業
  (1) B7（GECO+L0）・B8（ARD-VAE）の追加
  (2) β 梯子 5 水準全体での再評価
  (3) 枠組みの変更
実施: 2026-09-14 〜 2026-09-17
生成物: `results/make49/stage7_b7b8.json`, `stage7_analysis.json`

---

## 0. 実施規模

| 項目 | 規模 |
|---|---|
| β 梯子のキャッシュ | 4 データ × 候補グリッド全点 × 5 seeds × **5 つの β** ＝ **1,215 本** |
| B7・B8 | 4 データ × 5 seeds × 2 手法 × **5 段** ＝ **200 件** |
| 計算時間 | β 梯子 約 28.5 時間、B7・B8 約 6 時間 |

**着手前に修正した設計上の不具合**: 候補キャッシュのキーに $\beta$ が入っておらず、
そのまま梯子を回すと**別の $\beta$ の結果を誤って返す**状態だった。
既存 289 件には $\beta=1$ を示すキーを付与して移行した。

---

## 1. B7・B8 の実装と、原著からの逸脱

### B7: GECO + L0（De Boom et al., arXiv:2003.10901）

原著 Eq.(13): $\;L(x,\lambda) = \mathrm{KL}(q(z|x)\Vert p(z)) + \Vert\nu\Vert_0
+ \lambda\,\mathbb{E}_z[C(x, g(z\odot\nu))]$、$\;C = \Vert x - g(z\odot\nu)\Vert^2 - \tau$

実装した要点（原著 Algorithm 1）:
KL をゲートでマスクする／制約は移動平均で近似し勾配は現ステップのみ／
$\lambda$ に二乗 softplus をかけてクランプ／**L0 項は制約充足時のみ加える**／
推論時は $\sigma(\gamma_j)\le 0.5$ のゲートを閉じる。

**逸脱（明記する）**: 原著は L0-ARM 勾配推定器を使うが、本実装は
**hard concrete 緩和**（Louizos et al. 2018）を用いた。目的関数は同一で推定器のみが異なる。
原著本文も hard concrete を同じ L0 目的への代替として挙げている。

**実装中に発見した不具合（自己申告）**: 当初、制約 $C$ を**画素平均**で評価していた。
原著 Eq.(8) は画素についての**和**である。入力 784 次元では制約項が 784 分の 1 に潰れ、
L0 項（最大 $m$）に対して無視できる大きさになる。その結果 $\lambda$ が育たず、
**品質制約を破ったまま 64 次元から 1 次元まで剪定**した。
画素和スケールに修正して解決した。
**これは段階 1 の A1（KL の和と平均の取り違え）と同じ型の誤りである。**

### B8: ARD-VAE（Saha et al., arXiv:2501.10901）

階層事前 $p(z|\alpha)=\prod\mathcal{N}(z_l;0,\alpha_l^{-1})$、$p(\alpha)=\prod\mathrm{Gamma}$。
無情報ハイパー事前のもとで共役更新は $\hat\sigma^2_l = b_L/a_L = \mathbb{E}[z_l^2]$。
関連軸は復号器の軸別感度で重みづけたスコアの**累積 99%** で決める（原著 §3.4）。

**逸脱（明記する）**: 原著は $\alpha$ を周辺化した Student's t を KL の目標にするが、
本実装は $\mathcal{N}(0,\hat\sigma^2)$ の Gaussian 近似を用いた。$\hat\sigma^2$ の推定は原著と同じ。

---

## 2.【最重要】GECO の制約は品質条件 $Q$ と同一である

$\tau$ は各 $\beta$ 段の anchor から導いた品質目標、すなわち**本研究の $Q$ そのもの**である。
B7 は $\beta$ を持たないので、以下で動いているのは $\tau$ だけである。

| データ | 手法 | $\beta$=0.25 | 0.5 | 1 | 2 | 4 | **$\beta$ 幅** |
|---|---|---|---|---|---|---|---|
| MNIST | **B7 (GECO)** | 11.8±1.3 | 9.4±0.5 | 7.2±0.4 | 5.6±0.5 | 3.0±0.0 | **8.8** |
| MNIST | B8 (ARD) | 6.0±0.0 | 6.0±0.0 | 6.0±0.0 | 6.0±0.0 | 6.0±0.0 | **0.0** |
| Fashion | **B7** | 5.8±0.4 | 5.2±0.4 | 4.2±0.4 | 4.0±0.7 | 3.8±0.8 | 2.0 |
| Fashion | B8 | 3.8±0.4 | 3.8±0.4 | 3.8±0.4 | 3.8±0.4 | 3.8±0.4 | **0.0** |
| dSprites | B7 | 64.0±0.0 | 64.0±0.0 | 64.0±0.0 | 64.0±0.0 | 64.0±0.0 | 0.0（剪定せず） |
| dSprites | B8 | 9.2±1.5 | 9.2±1.5 | 9.2±1.5 | 9.2±1.5 | 9.2±1.5 | **0.0** |
| CIFAR-10 | **B7** | 42.6±5.1 | 30.8±0.8 | 20.0±1.0 | 13.4±0.5 | 8.4±0.5 | **34.2** |
| CIFAR-10 | B8 | 14.2±0.4 | 14.2±0.4 | 14.2±0.4 | 14.2±0.4 | 14.2±0.4 | **0.0** |

**B7 の選択次元は $\tau$ の単調減少関数として完全に説明できる**（CIFAR-10 で 42.6 → 8.4、5 倍）。

さらに、$Q$ を直接適用した選択（§3）と B7 の選択が**同じ方向・同じ規模**で動く。

| データ | $Q$ の選択（$\beta$=0.25 → 4） | B7 の選択（同） |
|---|---|---|
| MNIST | 9.6 → 3.6 | 11.8 → 3.0 |
| CIFAR-10 | 60.8 → 8.0 | 42.6 → 8.4 |

### 帰結

**段階 5 で「残せる貢献の第一」に挙げた「品質条件 $Q$ の定式化」は、
GECO（De Boom et al., 2020）に実質的に先取りされている。**

「再構成誤差の上限のもとで最大限に次元を削る」という設計は原著そのものである。
原著の本文も「潜在空間のパラメータを調整するのは抽象的だが、
データ空間の品質指標を調整するのは具体的で実務的である」と、
$\beta$ ではなく品質目標で制御する利点を明示している。

**この事実を返答書と本文で自ら開示する。** $Q$ を新規の貢献として提示しない。

---

## 3. B8（ARD-VAE）は自由閾値を持たない唯一の手法である

段階 5 で「$\delta$・$\epsilon_D$・$c$・$n$ のいずれもが結論を変える自由パラメータである」と結論した。
**B8 はこの結論の唯一の例外である。**

- $\beta$ 文脈 5 段すべてで**完全に同一の値**を返す（4 データすべてで $\beta$ 幅 0.0）。
- $\tau$ も $\beta$ も $\delta$ も使わない。階層事前と累積 99% の規則だけで決まる。
- MNIST で SD $=0.0$、CIFAR-10 で SD $=0.4$ と seed 間も安定。

**これは論文の主要な対比になる。** 閾値に支配される手法群（提案法・FONDUE・GECO・AU 診断）と、
閾値を持たない手法（ARD-VAE）を並べることで、
「どこに自由度があるか」を実証的に示せる。

---

## 4. AU の $\delta$ 依存は $\beta$ 全域で成立する

CIFAR-10 で「AU $=m$（遮断が全く起きない）」となる割合:

| $\delta$ | $\beta$=0.25 | 0.5 | 1 | 2 | 4 |
|---|---|---|---|---|---|
| 0.001 | 100% | 100% | 100% | 98% | 60% |
| 0.01 | 60% | 50% | 49% | 22% | 10% |
| 0.1 | 48% | 38% | 40% | 20% | 10% |

$\delta=0.001$ では $\beta$ をどう選んでも大半の条件で「遮断なし」と判定される。
**段階 5 の結論（$\delta$ で結論が反転する）は $\beta$ 全域で確認された。**
「$\beta=1$ に固有の観測ではないか」という反論は成立しない。

---

## 5. B7 は制約を満たせない（原著手法の限界の記録）

制約充足で終了した試行（5 seeds 中）:

| データ | $\beta$=0.25 | 0.5 | 1 | 2 | 4 |
|---|---|---|---|---|---|
| MNIST | 0/5 | 0/5 | 0/5 | 2/5 | 2/5 |
| Fashion | 4/5 | 2/5 | 1/5 | 2/5 | 2/5 |
| **dSprites** | **0/5** | **0/5** | **0/5** | **0/5** | **0/5** |
| CIFAR-10 | 3/5 | 4/5 | 5/5 | 4/5 | 5/5 |

- **dSprites では全 25 試行で制約を一度も満たせず、64 次元から 1 つも剪定しなかった。**
  品質目標に達しない限り L0 項を加えない設計なので、目標が厳しいと剪定が起きない。
- MNIST では制約未充足のまま剪定している。移動平均が一度負になった時点でゲートが閉じ、
  その後戻らない履歴依存である。**原著自身が指摘する「pruning と性能最適化の drift」**にあたる。
- 安定して機能するのは自然画像（CIFAR-10）のみで、他手法と逆のパターンである。

**これを FONDUE のときと同様、原著の誤りとしては扱わない。**
本再実装は hard concrete を用いた近似であり、原著の L0-ARM とは推定器が異なる。

---

## 6. 貢献の再整理（段階 5 からの修正）

### 取り下げる貢献

| 段階 5 で挙げた貢献 | 本段階での判定 |
|---|---|
| **品質条件 $Q$ の定式化** | **取り下げ。** GECO（2020）に先取りされている（§2） |

### 残る貢献

1. **共通条件での既存法の体系的評価**。FONDUE・FONDUE-VAR・GECO/L0・ARD-VAE を、
   共通の品質尺度・共通の費用計上・5 seeds・4 データ・5 つの $\beta$ で比較した例は他にない。
2. **ID 推定の不安定性の定量化**（表現で 1.7〜3.7 倍、標本数で一致判定が反転）。
3. **判定規則が閾値に支配されることの実証**。$\beta$ 全域で確認した。
4. **閾値を持つ手法群と持たない手法（ARD-VAE）の対比**（§3、本段階で新規）。
5. **再実装で発見した失敗様式の記録**。GECO の制約スケール、FONDUE-VAR の退化、
   AU の $\delta$ 反転。いずれも再現可能な形で示した。
6. **合成条件と実データの対比**（段階 6 の E6）。

---

## 7. 残作業

| 事項 | 状態 |
|---|---|
| 提案法・昇順走査の $\beta$ 全段での比較表 | 未作成（データは揃っている） |
| 図表の $\beta$ 対応版への更新 | 未実施 |
| 返答書への §2・§3 の反映 | 未実施 |
| 原稿再構成仕様の貢献リスト更新 | 未実施 |
| `main_paper_NN49/MAKE49.tex` の執筆 | 未着手 |
