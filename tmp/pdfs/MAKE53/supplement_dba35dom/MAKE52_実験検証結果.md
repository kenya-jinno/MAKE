# MAKE52 実験・検証結果

対象: `MAKE51_完成に向けた残作業.md` のうち**原稿執筆に関係しない作業**
実施日: 2026-09-19
生成物: `results/make52/`、実装は `src/make52/`

---

## 0. 実施したもの

| 残作業の項目 | 実施 | 主な結果 |
|---|---|---|
| §2 項目 1: ID 採用・fallback 別の層別集計 | 完了 | 文書の集計を再現。差の 2 件は fallback 由来と確認 |
| §2 項目 2: 開始幅だけを変える対照 | 完了 | **選択幅が異なる条件は 0/100**。ID 開始幅の寄与なし |
| §3: 既存剪定法の原著対応検証 | **2 手法とも完了** | GECO/L0 は一致、ARD は CIFAR-10 で不一致 |
| §4.2: 真の ELBO | 完了（新規学習に同梱） | 4 データ × 3 seeds で記録 |
| §4.3: 曲線と費用 | 完了（同上） | 5 epoch ごとの train/val 曲線、時間の分離記録 |
| **R2-3: ARD の native 枝刈り品質** | **完了** | **原著の主張は 3 データで成立、dSprites で不成立（+126%）** |

**VAE の全再学習は行っていない。** §2 は既存 CSV とキャッシュの再集計であり、
§3 の新規学習は原著対応の実装を検証するために必要な範囲に限った。

---

## 1. ID 開始幅の寄与は観測されない（§2）

### 1.1 層別集計（再学習なし）

`results/make52/stratified_replay.json`

| 層 | 条件数 | ID local | Midpoint local | Ascending Q |
|---|---|---|---|---|
| ID 採用（MNIST） | 25 | 25/25 | 25/25 | 25/25 |
| ID 不採用（他 3 データ） | 75 | 75/75 | 73/75 | 75/75 |
| 全体 | 100 | 100/100 | **98/100** | 100/100 |

Midpoint local が最小適格幅を取り逃した 2 件:

| 条件 | full-grid 最小 | ID local | Midpoint | ID local の分岐 |
|---|---|---|---|---|
| Fashion-MNIST, seed 31337, β=0.25 | 6 | 6 | 12 | **昇順 fallback** |
| dSprites, seed 31337, β=4 | 8 | 8 | 16 | **昇順 fallback** |

**2 件とも ID local 側が fallback を実行した条件である。** 差は開始幅ではなく、
fallback の有無と局所探索の停止規則から生じている。

### 1.2 開始幅のアブレーション

`results/make52/start_width_ablation.json`、実装 `src/make52/start_width_ablation.py`

信頼性判定と fallback を ID local と**同一に固定**し、ID 採用時の開始幅だけを
格子中点に置き換えた対照（`ID gated midpoint`）。参照 bank の費用は両者に計上した。

| 層 | 条件数 | ID local 一致 | gated midpoint 一致 |
|---|---|---|---|
| ID 採用 | 25 | 25/25 | 25/25 |
| ID 不採用 | 75 | 75/75 | 75/75 |
| 全体 | 100 | 100/100 | **100/100** |

**選択幅が異なった条件は 0/100。** 費用は ID 側が不利である。

| 層 | 方策 | 候補数 | cold 秒 |
|---|---|---|---|
| ID 採用 | ID local | 8.84 | 705.2 |
| ID 採用 | **ID gated midpoint** | **5.84** | **537.7** |
| ID 不採用 | 両者とも（fallback が同一） | 5.24 | 995.8 |

**結論**: 開始幅を ID から決めても格子中点から決めても選択は同一で、
ID 側は同じ答えに 1.5 倍の候補学習を要する。
100/100 対 98/100 を ID の優位性の根拠に用いることはできない。

---

## 2. GECO + L0: 原著推定量でも結論は変わらない（§3）

実装 `src/make52/geco_arm.py`、結果 `results/make52/geco_arm_all.json`

原著 De Boom et al. (arXiv:2003.10901) の **L0-ARM 勾配推定量（Eq. 12）** を実装した。
従来は hard concrete 緩和を用いており、逸脱として明記していた。

### 2.1 原著推定量と hard concrete の対応

| データ | hard concrete | **L0-ARM（原著）** | 差 |
|---|---|---|---|
| MNIST | 7.2 ± 0.4 | 7.7 ± 0.6 | +0.5 |
| Fashion-MNIST | 4.2 ± 0.4 | 4.7 ± 0.6 | +0.5 |
| dSprites | 64.0 ± 0.0 | 54.0 ± 7.2 | −10.0 |
| CIFAR-10 | 20.0 ± 1.0 | 21.0 ± 2.6 | +1.0 |

**4 データすべてで整合する。** 推定器の逸脱は結論を変えない。

### 2.2 ゲートの二値化（原著は「学習後 0 か 1 に近い」と記述）

| データ | <0.1 | 中間 | >0.9 | 期待開数 | 閾値超 | 二値化 |
|---|---|---|---|---|---|---|
| MNIST | 56 | 0 | 8 | 8.33 | 8 | ○ |
| Fashion-MNIST | 59 | 0 | 5 | 5.32 | 5 | ○ |
| **dSprites** | 0 | **64** | 0 | 32.08 | 52 | **×** |
| CIFAR-10 | 236 | 1 | 19 | 25.65 | 19 | ○ |

dSprites のみ二値化せず、制約も全 3 seed で未充足（val MSE 0.042 対 目標 0.00128）。
**推定器を原著どおりにしても dSprites での失敗は解消しない。**

### 2.3 native と次元転用の分離

| データ | native/決定論的 | native/事後標本 K=32 | 通常 VAE へ転用 |
|---|---|---|---|
| MNIST | 0.02167（Q 達成） | 0.02757（未達） | 0.02117（達成） |
| Fashion-MNIST | 0.01915（達成） | 0.02264（未達） | 0.01915（達成） |
| dSprites | 0.04236（未達） | 0.04236（未達） | **0.00119（達成）** |
| CIFAR-10 | 0.01578（未達） | 0.01743（未達） | 0.01709（未達） |

**事後標本評価は 4 データすべてで目標を満たさない。** 再構成の取り方で判定が変わる。
dSprites は native が失敗する一方、返された幅を通常 VAE に転用すると目標を満たす。

---

## 3. ARD-VAE: 原著対応で CIFAR-10 の結果が変わる（§3）

実装 `src/make52/ard_native.py`、結果 `results/make52/ard_native_all.json`

原著 Saha et al. (arXiv:2501.10901) の共役更新（Eq. 8, 9）、目的関数（Eq. 15)、
**Jacobian による関連度重み（Eq. 19）** を実装した。
Jacobian は `torch.func.jacrev` で厳密に計算し、出力走査版との相関 1.000000、
相対差 4.35×10⁻⁷ を確認した。

### 3.1 従来版（有限差分）との対応

| データ | 従来（有限差分） | **原著対応（厳密 Jacobian）** | 差 | 一致 |
|---|---|---|---|---|
| MNIST | 6.0 ± 0.0 | 6.0 ± 0.0 | +0.0 | ○ |
| Fashion-MNIST | 3.8 ± 0.4 | 4.0 ± 0.0 | +0.2 | ○ |
| dSprites | 9.2 ± 1.5 | 9.7 ± 0.6 | +0.5 | ○ |
| **CIFAR-10** | 14.2 ± 0.4 | **26.3 ± 0.6** | **+12.1** | **×** |

**CIFAR-10 で 1.85 倍の差が出た。** 従来の有限差分は摂動幅に $\hat\sigma$ を用いており、
関連度スコア $w \odot \hat\sigma^2$ に $\hat\sigma$ が二重に入っていた。
潜在分散の分布が広い CIFAR-10 でこの重複が効き、上位軸を過大評価して
累積 99% に達する軸数を少なく見積もっていた。

**これは「近似」ではなく別の量を計算していたことになる。従来の申告を修正する。**

### 3.2 中心化（原著 Eq. 9 の $\mu^\alpha$）の影響

原著は軸平均を引いてから $b_l$ を計算する。従来実装は引いていなかった。

12 条件中 **3 条件**で選択が 1 だけ動いた（dSprites 2 件、CIFAR-10 1 件）。
MNIST と Fashion-MNIST では差がない。影響は小さいが、原著に合わせて中心化を採用する。

### 3.3 Jacobian の計算コスト

MNIST 0.01 秒、dSprites 10.9 秒、CIFAR-10 7.2 秒。
学習時間（87〜299 秒）に対して十分小さく、**厳密計算を避ける理由はない。**

---

## 3b. ARD-VAE: 選択軸だけを残した native モデルの品質（Reviewer 2 No.3 への直接回答）

実装 `src/make52/ard_native_pruned.py`、結果 `results/make52/ard_pruned_all.json`
評価規則は**実行前に** `results/make52/native_pruned_spec.json` へ登録した。

### 3b.1 何が欠けていたか

`MAKE52_revision_review.md` §4.2 は、ARD について現在の記録が
「full-width モデルと転用先の品質」にとどまり、
**native な枝刈り後の品質を示していない**と指摘している。
「次元数を他の VAE に渡すと再構成できる」ことと
「当該枝刈りモデルが成功した」ことは別である。

同文書は、手法を増やすより 1 手法について
「未枝刈り・枝刈り後・転用を分けて比較する」ほうが R2-3 への回答として直接的だとしている。

### 3b.2 事前に定義した 4 条件

| 条件 | 定義 |
|---|---|
| 未枝刈り | 学習済み ARD-VAE の全 $L$ 軸、決定論的再構成 $g(\mu)$ |
| **枝刈り（マスク）** | 非選択軸を事前平均 0 に固定。**復号器は再学習しない** |
| 枝刈り（微調整） | 上記から復号器のみ 30 epochs 微調整。符号化器と事前分散は固定。予算は事前固定 |
| 転用 | 選択幅に最も近い格子点で通常 VAE を新規学習（既存キャッシュ） |

マスク条件は、原著の
「非関連軸について復号器は出力をほとんど変化させない」という記述の**直接の検証**にあたる。

### 3b.3 結果（validation 画素平均 MSE、3 seeds、mean±SD）

| データ | 選択/初期 | 未枝刈り | 枝刈り（マスク） | 枝刈り（微調整） | 転用 |
|---|---|---|---|---|---|
| MNIST | 6.0/64 | 0.02361±0.00018 | 0.02362±0.00018 | 0.02336±0.00016 | 0.02251±0.00015 |
| Fashion-MNIST | 4.0/64 | 0.01943±0.00008 | 0.01943±0.00008 | 0.01919±0.00005 | 0.01928±0.00023 |
| **dSprites** | 9.7/64 | 0.00133±0.00006 | **0.00301±0.00027** | 0.00183±0.00009 | 0.00164±0.00030 |
| CIFAR-10 | 26.3/256 | 0.01440±0.00024 | 0.01489±0.00020 | 0.01473±0.00008 | 0.01519±0.00012 |

test 集合（選択は validation のみで決定済み）:

| データ | 未枝刈り | 枝刈り（マスク） | 枝刈り（微調整） | 転用 |
|---|---|---|---|---|
| MNIST | 0.02317 | 0.02318 | 0.02290 | 0.02215 |
| Fashion-MNIST | 0.01917 | 0.01917 | 0.01893 | 0.01894 |
| dSprites | 0.00132 | 0.00294 | 0.00178 | 0.00161 |
| CIFAR-10 | 0.01464 | 0.01514 | 0.01497 | 0.01542 |

### 3b.4 原著の主張は 3 データで成立し、dSprites で破れる

未枝刈りからの劣化率:

| データ | 落とす軸数 | 枝刈り（マスク） | 微調整後 | 転用 | 判定 |
|---|---|---|---|---|---|
| MNIST | 58 | **+0.0%** | −1.1% | −4.7% | 成立 |
| Fashion-MNIST | 60 | **+0.0%** | −1.2% | −0.8% | 成立 |
| **dSprites** | 54 | **+126.3%** | +37.1% | +22.9% | **不成立** |
| CIFAR-10 | 229 | +3.4% | +2.2% | +5.5% | 成立 |

- **MNIST と Fashion-MNIST では 58〜60 軸を落としても誤差が変わらない。**
  原著の記述が定量的に確認できた。
- CIFAR-10 は 229 軸を落として +3.4%。復号器の微調整で +2.2% まで戻る。
- **dSprites では誤差が 2.26 倍になる。** 選択されなかった 54 軸が再構成に寄与しており、
  原著の前提が成り立たない。微調整しても +37.1% までしか戻らない。

### 3b.5 native と転用の区別が決定的になる例

**dSprites は両者の乖離が最も大きい。** 転用は +22.9% で済むのに native 枝刈りは +126.3% である。
すなわち**選択された次元数は妥当だが、その軸だけを残したモデルは機能しない**。
未枝刈りと転用しか見ていなければ、この状態は検出できない。

CIFAR-10 は逆で、枝刈りモデル（0.01489）が転用（0.01519）より良い。
これも native の評価があって初めて言える。

### 3b.6 費用

| データ | ARD 学習 | 微調整 | 合計 |
|---|---|---|---|
| MNIST | 86 s | 6 s | 92 s |
| Fashion-MNIST | 87 s | 6 s | 92 s |
| dSprites | 289 s | 21 s | 310 s |
| CIFAR-10 | 142 s | 11 s | 153 s |

微調整は学習の 7% 程度で済む。

### 3b.7 実装上の障害と対処（記録）

Jacobian は $(B, D, L)$ の配列を確保するため、出力次元の大きいデータで GPU メモリが不足した。

| データ | 出力次元 | 対処 |
|---|---|---|
| CIFAR-10 | 3072 | Jacobian のバッチを 25 → 4 |
| dSprites | 4096 | **VJP を積み上げる方式**に変更（$(B, L)$ しか確保しない） |

VJP 版と `jacrev` 版の一致を確認した（相関 1.00000000、相対差 4.35×10⁻⁷）。

### 3b.8 この実験が示さないこと

- 原著の学習系・初期化・ハイパーパラメータ全体の再現。
- native 枝刈りが転用より優れるという一般的主張。本実験は 4 データ・3 seeds の観測である。

---

## 4. 逸脱申告の訂正（原稿・返答書に反映が必要）

| 従来の申告 | 検証結果 | 対応 |
|---|---|---|
| ARD は「Student's t ではなく Gaussian 近似」を用いた逸脱 | **誤り。** 原著 Eq.(15) は KL(N(μ,σ²)‖N(0,σ̂²)) そのもの。Student's t は動機づけで、最適化する目的関数は Gaussian 近似である | **逸脱の記述を削除する** |
| GECO は「L0-ARM ではなく hard concrete」を用いた逸脱 | 事実。ただし 4 データで選択が一致し、結論を変えない | 逸脱は残し、**一致を示す証拠を添える** |
| ARD の有限差分は Jacobian の近似 | **不正確。** CIFAR-10 で 1.85 倍の差。別の量を計算していた | **記述を訂正し、厳密版の数値へ差し替える** |

---

## 5. 新たに発見した実装上の欠陥（自己申告に追加）

ARM 実装の初回実行で、幅 2〜3 まで過剰剪定し制約も満たさなかった。
原因の切り分けのため**推定量の単体検証**を書いた（`src/make52/test_arm_estimator.py`）。

解析的に勾配の分かる問題では、推定量は標本 10 万で解析解に収束する
（相関 1.0000、相対誤差 0.0085）。誤りは使い方にあった。
**ARM は標本ごとに異なる F の値を必要とするが、実装は F をバッチ平均してから渡していた。**
同じ誤りを単体検証で再現すると相関が 1.0000 から 0.6761 に落ちる。

修正後は原著の記述どおりゲートが二値化し、制約も満たす。

これは既報の 3 件（KL の和と平均、GECO 制約のスケール、公表アルゴリズムの退化）と
**同じ型の誤り**であり、原稿の「実装上の失敗様式」節に 4 件目として追加する。

---

## 6. 生成したファイル

### 結果

| ファイル | 内容 |
|---|---|
| `results/make52/stratified_replay.json` | 層別集計、取り逃し 2 件の内訳、層別費用 |
| `results/make52/start_width_ablation.json` | 開始幅アブレーション 100 条件 |
| `results/make52/geco_arm_{MNIST,FashionMNIST,dSprites,CIFAR10}.json` | ARM 版 各 3 seeds |
| `results/make52/geco_arm_all.json` | 上記の統合 |
| `results/make52/ard_native_{4 データ}.json` | ARD 原著対応版 各 3 seeds × 中心化 2 通り |
| `results/make52/ard_native_all.json` | 上記の統合 |
| `results/make52/native_pruned_spec.json` | **枝刈り評価規則（実行前に登録）** |
| `results/make52/ard_pruned_{4 データ}.json` | 4 条件比較 各 3 seeds |
| `results/make52/ard_pruned_all.json` | 上記の統合 |

いずれも真の ELBO、5 epoch ごとの train/validation 曲線、学習時間と評価時間の分離、
出所情報（Python 3.12.3 / PyTorch 2.7.1+cu118 / CUDA 11.8 / cuDNN 9.1.0 /
driver 570.211.01 / RTX 3070）を含む。

### 実装

| ファイル | 内容 |
|---|---|
| `src/make52/start_width_ablation.py` | 開始幅アブレーション |
| `src/make52/geco_arm.py` | GECO + L0-ARM（原著 Eq.12） |
| `src/make52/ard_native.py` | ARD-VAE（原著 Eq.8,9,15,19） |
| `src/make52/test_arm_estimator.py` | ARM 推定量の単体検証 |
| `src/make52/ard_native_pruned.py` | ARD の 4 条件比較（VJP 版 Jacobian を含む） |

### ログ

`results/make52_geco_arm_*.log`、`results/make52_ard_native_*.log`

---

## 7. 残っている作業（本文書の範囲外）

| 事項 | 状態 |
|---|---|
| 補足 ZIP の再梱包と検証 | 未実施（原稿・結果の変更後に行う） |
| 参考文献 30 件の外部書誌照合 | 未実施 |
| 本結果の原稿・返答書への反映 | 未実施（原稿執筆の範囲） |
| §4.1 独立参照 bank による追試 | 未実施（主張範囲の判断による） |
| §4.4 合成データの再実行 | 未実施（補助的な扱いのまま） |
