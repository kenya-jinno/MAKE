# MAKE49 返答書（日本語作業版）

**位置づけ**: これは英語返答書を書くための作業版である。
方針 §9.1 のとおり、**英語返答書は実験と改訂が完了してから過去形で書く**。
本文書は「何を実施し、何が支持され、何が支持されなかったか」を証拠と対応づけて確定するためのものである。

**重要な前提**: 追加検証の結果、**原稿の中心的主張の多くが支持されなかった**。
方針 §9.3 のとおり、これを隠して「全コメントに完全に対応した」とは書かない。
本文の主張をどのように縮小したかを説明する。

---

## 0. Editor 宛カバーレターの骨子

体裁の改善より先に、次の 4 点を述べる。

1. **先行研究との差分の再整理**: NOLTA2025 本体と FONDUE（本文・付録 G・公開コード）を
   一次資料で逐条確認し、既出の着想を明示した。とくに FONDUE 付録 G の Algorithm 3 が
   本研究の活性ベースの読み取りと目的を共有することを**自ら開示**した。
2. **独立した検証設計**: 事前登録した実験設定表（閾値・予算・終了状態・判定規則）を
   結果を見る前に固定し、変更はすべて変更履歴に記録した。
3. **直接比較法の追加**: FONDUE（Algorithm 1）と FONDUE-VAR（Algorithm 3）を再実装し、
   validation MSE/ELBO/下流選択による全探索、粗い探索、固定次元と共通条件で比較した。
4. **理論・診断の過大な主張の修正**: 実装監査で発見した損失規約の誤りにより、
   原稿の AU に関する中心的な観測が交絡していたことが判明した。該当する主張を削除した。

**あわせて述べる**: 追加検証は現行の主張の多くを支持しなかった。
本改訂は貢献の枠組みそのものを「ID 誘導による高速な次元選択法」から
**「VAE の次元選択における診断と落とし穴の実証研究」**へ再構成するものである。

---

## 1. 全 Reviewer に共通して報告すべき、最も重要な修正

### 1-A. 実装監査で損失規約の誤りを発見した（自己申告）

査読対応の準備として実装を全数監査したところ、VAE 損失の KL 項が
**バッチだけでなく潜在次元についても平均されていた**（`torch.mean`）。
再構成項はサンプルあたりの和なので、**実効的な $\beta$ が $\beta/m$** になっていた。
同じ規約が VAE 実験 12 ファイルすべてに存在し、正しい形式は 1 つも無かった。

**影響**: 原稿が報告した「AU が $m$ とともに増加し飽和する」という観測は、
$m$ の効果ではなく $\beta$ の効果である。
規約を修正して $\beta$ を振ると AU は $m$ にほぼ依存せず $\beta$ で決まり、
旧実装の AU 曲線は $\beta_{\mathrm{eff}} = \beta/m$ での読み替えにより **$\pm 1.7$ 以内で再現**できた。

**対応**: 全 VAE 実験を修正規約で再実行した。$\beta$ の値は原稿の値と意味が異なるため、
対応表を補足資料に載せる。AU の $m$ 依存性に基づく記述はすべて削除する。

### 1-D. 再実装中に同型の誤りを再発見した（自己申告）

B7（GECO+L0）の再実装で、再構成制約を**画素平均**で評価していた。
原著 Eq.(8) は画素についての**和**である。入力 784 次元では制約項が 784 分の 1 に潰れ、
L0 項に対して無視できる大きさになる。結果として**品質制約を破ったまま 64 次元から
1 次元まで剪定**した。画素和スケールに修正して解決した。

**これは 1-A（KL の和と平均の取り違え）と同じ型の誤りである。**
和と平均の取り違えは項どうしの相対重みを数百倍ずらすが、
学習は破綻せず「もっともらしい」結果を返すため発見が難しい。
本改訂では、この型の誤りを検出するための単体テストを実装に組み込んだ。

### 1-B. 図の対象とキャプションの不一致（自己申告）

現原稿 Figure 4 の右パネルは **VAE の潜在表現の TwoNN** をプロットしているが、
キャプションと本文は参照 AE（$m_{\mathrm{ref}}$）として記述し、
参照 AE の設計根拠として引用していた。生成スクリプトのデータ参照から確定した。
また同図の $\kappa \approx 10^9$ は計算値ではなくハードコードされた定数であり、
Table 12 の $4.1\times10^7$ と桁が合わない。

**対応**: 図・キャプション・本文を実際にプロットしている量に合わせて書き換え、$\kappa$ を再計算する。

### 1-C. 合成データの前処理（自己申告）

合成多様体データが未標準化で、座標スケールが著しく異方的だった
（Swiss Roll の座標別 std $[6.57, 0.29, 6.99]$）。
多様体間で MSE を比較できず、AU の絶対閾値もスケール依存だった。

**対応**: 標準化してから学習・評価し、Table 4 を再計算した。

**再計算の結果は原稿にとって有利な方向だった。**
未標準化では Swiss Roll・Torus とも AU が 2 で止まり、原稿はこれを
「AU を幾何学的次元と読むことへの疑問」の材料としていた。
標準化すると **AU $=3=d_{\mathrm{emb}}$** となり、位相的必要条件と整合する。
Swiss Roll の第 2 座標は std 0.29（他は 6.57, 6.99）で全分散の約 0.2% しか占めず、
未標準化ではこの座標を無視しても MSE をほぼ保てたためである。

**したがって原稿のトーラス異常は撤回する。** 代わりに
「合成条件では AU が理論どおり機能し、実データでは $\delta$ に支配される」という
**対比**を報告する。

---

## 2. Reviewer 1 への回答（10 項目）

| 番号 | 対応 | 証拠 |
|---|---|---|
| R1-1 Abstract の記号 | 記号を全廃し 200 語以内に圧縮。**数値成果は再評価後の値に差し替え**、旧「60–80% 削減」は削除 | 改訂 Abstract |
| R1-2 「32 や 64」の無出典 | 具体数の一般化を削り、次元選択の先行研究（FONDUE 等）を引用 | Introduction 改訂と新規引用 |
| R1-3 節参照過多 | 19 か所 → 5 か所以内。節参照は構成紹介に集約 | 改訂 Introduction |
| R1-4 導入に結論が混在 | データ別結果・閾値範囲・自然画像の詳細を Results/Discussion へ移動 | 章構成の変更 |
| R1-5 §1.2 の記号説明 | 貢献を文章で記述し、定義は Methods 冒頭の記号表へ集約 | 新 Methods |
| R1-6 グラフ系次元削減 | PCA・Isomap・LLE を背景に追加。Costa & Hero (2004) を graph-based ID 推定の例として引用。**ご指定の DOI 10.1109/ACCESS.2022.3190505 は異常検知が主題**のため、次元削減法としては紹介せず背景上の扱いを限定した | Related Work 追加段落 |
| R1-7 証明末尾の □ | 初出で記号の意味を明記 | 修正箇所 |
| R1-8 CUDA バージョン | **実測値を記録する仕組みを実装**。Python 3.12.3 / PyTorch 2.7.1+cu118 / CUDA 11.8 / cuDNN 9.1.0 / driver 570.211.01 / RTX 3070。**旧結果の実験環境は記録が存在せず復元できないため、その旨を明記**し、再実行した結果の環境を報告する | 環境表、`requirements_freeze.txt` |
| **R1-9 分割・学習曲線・反復数** | **中心的指摘。全面的に実施した。** 公式 train を 90:10 で分割しインデックスを保存、test は最終評価のみ。主要比較は**独立 5 seeds で参照モデルから選択まで全手順を反復**。学習曲線は train（評価モード）/validation を epoch に対して示し、選択済み checkpoint の test 値を点で加えた。**全 30 ランで plateau 判定を満たさず、300 epochs を収束と呼べないことも報告する** | 新 Figure A、分割表、seed 別結果 |
| R1-10 DOI 不統一 | DOI のある文献に付与し、公刊版を優先。DOI の無い文献は安定 URL を統一書式で記載 | 統一参考文献リスト |

**R1-9 への補足**: ご指摘のとおり、旧原稿は分割と反復の扱いが不明確だった。
今回は選択に test を一切使わない設計に改め、学習曲線・seed 別結果・最終 test 値を揃えた。
学習曲線について、全 epoch の test 曲線は test 隔離を損なうため掲載せず、
**train/validation 曲線に選択済み checkpoint の test 値を加える**方式を採った。

---

## 3. Reviewer 2 への回答（4 論点）

### R2-1 NOLTA2025 からの増分が小さい【最重要】

**1. 差分が明瞭でなかったことを認める。**
会議論文と本稿の関係を明示せずに提出したのは著者の不備である。

**2. 既出の着想を明示する。**
NOLTA2025 は MNIST 32×32・クラス別 VAE・240 モデルで、
ID がボトルネック設計の手がかりになること、潜在 ID が入力 ID を下回ることを報告している。
**同会議論文は AU を扱っていない。**

**3. 独立した先行研究を自ら開示する。**
FONDUE（Bonheme & Grzes, arXiv:2209.12806v1）を全文確認した。
とくに**付録 G の Algorithm 3（FONDUE-VAR）は、変数型（active/mixed/passive）に基づく
読み取りで潜在次元を決める手順であり、本研究の活性ベースの診断と目的を共有する**。
原著はこれを学習初期の不安定性を理由に採用していない。
この共通性を Related Work の冒頭で先に述べ、その上で差を説明する。

**4. 実測で支持された結論と、支持されず縮小した主張を示す。**

| 検証した問い | 結果 |
|---|---|
| 長時間学習で読み取りは安定するか（H1） | 個数で読む 2 系統は MNIST の大容量条件で安定化した。**連続量の読み取りは逆に不安定化する条件があった** |
| AU は変数型分類より安定か（H2） | **支持されなかった。** 300 epochs では両者がほぼ同じ値に収束し、早期学習ではむしろ変数型分類のほうが安定な条件があった |
| ID 誘導に探索上の利点があるか | **支持されなかった。** ID 推定を使わない昇順走査が 3 データすべてで同等以上の結果をより安く出した |
| AU に診断としての増分価値があるか | **確認できなかった。** 事前登録の判定規則では「価値あり」となったが、**同数の雑音特徴量を加えた対照と区別できず**、検定に検出力が無かった |

**5. 対応づけ。** 上記は新 Table A（比較）、新 Table D（方法単位の比較）、
新 Figure F（読み取りの epoch 軌跡）、新 Table E（診断の増分）に対応する。

**避けた表現**: 「4 ページから 33 ページに増えた」「seed 数を増やした」だけの強調はしない。
既存の Table 12 や感度分析を新規追加として説明しない。

### R2-2 新アーキテクチャ・目的関数がなく恣意的 heuristic

**一部は受け入れ、一部は代替対応を示す。**

新しいアーキテクチャや目的関数の提案が無いことは事実である。
一方「新構造が無ければ貢献にならない」という含意は受け入れない。
実証研究としての価値を示すため、次を実施した。

- 品質条件 $Q$、停止規則、拡張規則、予算、終了状態を**事前登録**して固定した。
- 閾値（$\delta$・$\epsilon_D$・初期係数 $c$・推定標本数 $n$）の感度を全数検証した。
- 構成要素のアブレーション（ID なし／ID のみ／ID+品質確認／+AU 診断）を実施した。

**その結果、恣意性のご指摘は実測で裏づけられた。**

| 閾値 | 観測 |
|---|---|
| AU の $\delta$ | CIFAR-10 では $\delta=0.001$ で「遮断なし」、$\delta=0.1$ で「遮断あり」と**結論が反転**する |
| 初期係数 $c$ | 既定の $c=2$ は dSprites で品質条件の達成率 0.40 と**失敗**する |
| 推定標本数 $n$ | 推定器の一致判定が $n$ で決まる。$n=1{,}000$ なら 4 データ全通過、$n=10{,}000$ なら MNIST のみ通過 |
| 品質許容誤差 $\epsilon_D$ | dSprites では選択次元が 57.6 から 9.6 へ 6 倍動く |

**対応**: 判定規則 V1/V2 は現行係数では 3 データすべてで機能しないため、
**主要な判定規則から外す**。閾値依存性そのものを実証的な知見として報告する。

### R2-3 全探索以外の既存法との比較がない

**妥当なご指摘であり、全面的に実施した。**

FONDUE（Algorithm 1）と FONDUE-VAR（Algorithm 3）を原著の擬似コードから再実装し、
validation MSE 全探索、validation ELBO 全探索、下流基準の全探索、粗い探索、固定次元、
そして各法に品質条件を付加した版と、共通条件・共通品質尺度・共通費用計上で比較した。

**主要な結果**（MNIST、$\beta=1$、5 seeds）:

| 方法 | 選択 $m$ | val MSE | 総時間 |
|---|---|---|---|
| B6a+Q | 8.0 ± 0.0 | **0.02172** | **126 s** |
| B6a FONDUE | 7.0 ± 0.0 | 0.02193 | **41 s** |
| **提案法（旧版の手順）** | 6.0 ± 0.0 | 0.02244 | **715 s** |

**提案法は全比較法中もっとも高価であり、品質でも劣った。**
FONDUE に品質条件を足した版が、品質・費用の両方で提案法を上回る。

**追加で実施した比較（改訂で新たに加えた）**: ご指摘を受け、
**GECO+L0（De Boom et al., 2020）と ARD-VAE（Saha et al., 2025）も再実装して加えた。**
比較は 4 データセット・5 seeds・**$\beta$ 梯子 5 水準**で行い、計 1,415 本の学習を要した。

この追加により、**本研究の中心的な主張の一つが先行研究に先取りされていたことが判明した**（下記 §3-追補）。

**なお残る不足**: 本再実装は FONDUE の dSprites 公表値（12.2±0.4）を再現しておらず
（本実装 4.2±0.4）、**公表値は参考欄にとどめ、主比較の不足が残ることを明記する**。
この不一致を FONDUE の誤りとしては扱わない。
B7 は原著の L0-ARM ではなく hard concrete 緩和を、
B8 は Student's t ではなく Gaussian 近似を用いており、いずれも逸脱として明記する。

### R2-3 追補【重要】品質条件 $Q$ は GECO に先取りされていた

改訂稿で「品質条件 $Q$ の定式化」を貢献として提示する準備をしていたが、
B7 の再実装により、**これが GECO（De Boom et al., 2020）と同一の設計であることが判明した。**

証拠: B7 は $\beta$ を持たず、制御するのは再構成制約 $\tau$ のみである。
$\tau$ を各 $\beta$ 段の anchor から導いた品質目標（＝本研究の $Q$）に設定すると、
B7 の選択次元は $\tau$ の単調関数として完全に説明できる。

| データ | $Q$ を直接適用（$\beta$=0.25 → 4） | B7 の選択（同） |
|---|---|---|
| MNIST | 9.6 → 3.6 | 11.8 → 3.0 |
| CIFAR-10 | 60.8 → 8.0 | 42.6 → 8.4 |

原著本文も「潜在空間のパラメータ調整は抽象的だが、データ空間の品質指標の調整は具体的で実務的」と、
$\beta$ ではなく品質目標で制御する利点を明示している。

**したがって $Q$ を新規の貢献として提示しない。** 改訂稿では GECO の先行性を明記し、
本研究の寄与は「既存の複数手法を共通条件で評価し、どの判定規則が何に支配されるかを示したこと」に限定する。

### R2-4 Table 4 の数値形式と全体の粗さ

Table 4 を整形し、**誤った elbow の説明（"exactly at $m=d_{\mathrm{emb}}$"）を削除**した。
あわせて合成データの標準化（本書 1-C）の修正後に数値を再計算した。
全図表を元ログから再生成して照合した。

---

## 4. Reviewer 3 への回答（7 項目）

| 番号 | 対応 |
|---|---|
| **R3-1 2×ID・V1・V2 が heuristic** | **ご指摘は実測で裏づけられた。** 係数 2 は dSprites で失敗し（$Q$ 達成率 0.40）、V1/V2 は 3 データすべてで原稿の不変域から外れた（AU/$\hat d$ が MNIST 0.14–0.46、dSprites 0.26–2.05、CIFAR-10 0.39–8.27）。V1 は $m$ が小さければ自動的に通るだけで検出力を持たない。**V1/V2 を主要な判定規則から外し**、閾値依存性を実証的知見として報告する |
| **R3-2 自然画像での検証が弱い** | CIFAR-10 で参照 AE から始まる全手順を実施した。**結果は適用不能である。** 全 5 seeds で ID 推定が信頼性基準を満たさず、手順は適用を見送った。dSprites でも同様で、**3 データ中 2 データで適用できず、成功は開発条件の MNIST のみ**だった。自然画像への有効性は主張しない |
| **R3-3 ID 推定の参照 AE 依存** | 入力空間・PCA 前処理空間・FC-AE（3 容量 × 3 seeds）・推定標本数を比較した。**$\hat d_{\mathrm{ID}}$ は表現によって 1.7〜3.7 倍動く**（dSprites で入力 7.70、PCA 2.68–4.32、参照 AE 2.08–4.56）。手順は窓を $2\hat d$ から決めるため、窓の位置も同じ倍率で動く。Conv-AE 参照は予算の都合で未実施 |
| R3-4 Proposition 2 の限定 | 固定 $\beta$・固定観測分散・Gaussian source の仮定を明記し、**AU との同一視、$\tau_{\mathrm{eff}}$ の循環説明、KL 符号からの方向推論を削除**した。本文の注意書きが既にあることを反論にせず、付録の断定との不整合を具体的に修正した |
| R3-5 baseline 不足 | B1（reconstruction）・B2（ELBO）・B3（下流）を共通グリッド・独立 test で比較した。**選択目的によって最適次元が大きく変わる**（CIFAR-10 で下流基準は常に最大容量 256、再構成基準は 140.8±53.5） |
| R3-6 "conditional" の反復 | 24 回 → 8 回程度に削減。適用条件を Methods に、限界を Discussion に集約 |
| R3-7 擬似コードが必要 | **実際のコードと一致する** Algorithm を掲載する。ID 不安定時（`ID_UNRELIABLE`）、範囲外、予算上限（`BUDGET_EXHAUSTED`）、圧縮候補なし（`NO_COMPACT_ALTERNATIVE_FOUND`）、学習不可能な次元（`DEGENERATE_SELECTION`）の処理を定義した |

---

## 5. 支持されなかった主張の一覧（本文から削除する）

| 削除する主張 | 削除の根拠 |
|---|---|
| 「最大 82%（60–80%）の削減」「高速な選択法」 | 提案法は MNIST で全比較法中もっとも高価（FONDUE の 17.3 倍） |
| 「VAE は 1 候補の学習だけで十分」 | 品質条件の判定に初期窓の全点評価が必要 |
| ID 誘導が探索を効率化する | ID を使わない昇順走査が 3 データで同等以上・より安価 |
| 内在次元から探索窓が導ける | $\hat d_{\mathrm{ID}}$ が表現により 1.7〜3.7 倍変動 |
| AU が余剰次元の遮断を診断する | $\delta$ で結論が反転。増分価値は雑音特徴量と区別できない |
| AU が変数型分類より安定 | 事前登録した検定で支持されず |
| V1/V2 が判定規則として機能する | 3 データすべてで不変域から外れる |
| 「elbow は $m=d_{\mathrm{emb}}$ に厳密に現れる」 | 実測と位相的必要条件を混同 |
| 自然画像への適用可能性 | CIFAR-10・dSprites で適用不能 |

## 6. 支持され、残す貢献

1. ~~品質条件 $Q$ の定式化~~ → **取り下げ。GECO に先取りされている**（R2-3 追補）。
2. **共通条件での既存法の体系的評価**。FONDUE・FONDUE-VAR・GECO/L0・ARD-VAE を、
   共通の品質尺度・共通の費用計上・5 seeds・4 データ・$\beta$ 梯子 5 水準で比較した。
   FONDUE の過小選択、FONDUE-VAR の**データ依存の**不安定性（dSprites で SD 49.5、CIFAR-10 で SD 1.3）、
   `keep_mixed=False` の退化、GECO の制約未充足（dSprites で 25 試行中 0 回充足）を再現可能に示した。
2b. **閾値を持つ手法群と持たない手法の対比**。ARD-VAE は $\beta$ 文脈 5 段すべてで
   完全に同一の値を返す（$\beta$ 幅 0.0）唯一の手法であり、他手法が自由閾値に支配されることを際立たせる。
3. **ID 推定の不安定性の定量化**。表現・標本数・推定器・容量への依存を 4 データで測った。
4. **判定規則が閾値に支配されることの明示**。$\delta$・$\epsilon_D$・$c$・$n$ のいずれもが結論を変える。
5. **合成条件と実データの対比**。標準化した合成多様体では AU が $d_{\mathrm{emb}}$ を正しく反映するが、
   実データでは $\delta$ に支配され増分価値も確認できない。**この対比自体が知見である**（E6）。

## 7. 返答書に書かない・書き方に注意する事項

- 「既に conditional と書いている」「補足資料にある」「結果は理論と整合的」という形の弁明はしない。
- 追加検証が主張を支持しなかったことを隠し、「全コメントに完全に対応した」と書かない。
- FONDUE 公表値を再現できなかったことを、FONDUE 側の誤りとして書かない。
- FONDUE-VAR の不安定性を「一般に不安定」と一般化しない（CIFAR-10 では安定だった）。
- 否定的結論を一般命題に広げない。**すべて $\beta=1$・4 データセットでの観測**である。
- 「事前登録（pre-registered）」は実際の記録がある項目にのみ使う（本改訂では記録がある）。
